"""Notifications context package (Wave 6 — `realtime_notifications` pilot).

Canonical implementation lives in `foms.services.notifications.realtime_notifications`.
Flat `foms.services.realtime_notifications` and root `services.realtime_notifications`
remain compatibility shims (same batch).
"""
