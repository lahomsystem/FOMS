"""Files context package (Wave 6 — `file_utils` helper pilot).

Canonical helpers live in `foms.services.files.file_utils`.
Flat `foms.services.file_utils` and root `services.file_utils` remain compatibility shims.
"""
