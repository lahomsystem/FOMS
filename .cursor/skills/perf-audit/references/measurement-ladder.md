# Measurement Ladder (field > lab)

1. **TTFB** staging — separate server vs client ("느리다≠서버")
2. **EXPLAIN (ANALYZE)** — hot queries, no Seq Scan
3. **Chrome SW** — real browser only (no headless SW)
4. **Tab swap** — mobile shell 10× fragment transition

Do not invent Lighthouse metrics. gstack browse when URL/runtime available.
