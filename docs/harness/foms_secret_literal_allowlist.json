{
  "_packet": "SECRET-02",
  "_comment": "Allowlist for the static credential-literal scan gate (tools/harness/secret_literal_scan.py). Entries record the variable NAME, its FILE, and the RATIONALE only. Credential VALUES are NEVER stored here. A new credential-shaped literal that is not listed here fails the gate (red).",
  "allow": [
    {
      "name": "KAKAO_JS_API_KEY",
      "file": "foms/services/common/geocode_config.py",
      "reason": "Public, domain-restricted Kakao Maps JavaScript client key. It is served to the browser by design (client-side SDK) and is not a secret; the Kakao console pins it to allowed referrer domains. The private REST key (KAKAO_REST_API_KEY) remains env-only (SECRET-01)."
    }
  ]
}
