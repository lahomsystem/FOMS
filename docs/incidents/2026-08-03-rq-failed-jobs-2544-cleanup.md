# 2026-08-03 운영 Redis 실패잡 2,544건 정리

> 유형: 운영 사고  ·  상태: 종결
> 작성 2026-09-07. 사후 등재 문서 — 사고 당시 기록이 아니라 저장소 근거로 재구성했다.

## 1. 요약 5축

| 축 | 내용 |
|---|---|
| 유형 | 운영 사고 — 큐 위생(운영 Redis 실패잡 누적) |
| 원인 축 | 퇴역한 잡이 실패 큐에 남아 누적. 정리 시점 판정은 "전부 퇴역 잡"(`docs/AI_STATUS.md:98`). **누적 경로·시작 시점은 `미상(근거 없음)`** |
| 복구 | 백업 1벌 생성 후 2,544건 정리(`docs/AI_STATUS.md:98`) |
| 구조 변경 여부 | 이 사고에 대응하는 구조 변경을 저장소가 명시한 곳 없음 — `미상(근거 없음)` |
| 재발 여부 | `미상(근거 없음)` — 정리 이후 실패잡 수를 잰 기록이 저장소에 없다 |

## 2. 증상

저장소에 남은 관측은 한 줄이다. 운영 Redis 실패잡이 **2,544건** 쌓여 있었고, 정리 시점
판정은 "전부 퇴역 잡" 이었다(`docs/AI_STATUS.md:98`, 압축본 `docs/AI_STATUS.md:353`).

- 사용자 화면에 어떤 증상으로 나타났는가 — `미상(근거 없음)`
- 누가 언제 발견했는가 — `미상(근거 없음)`
- 이 적재가 워커 처리량·큐 지연에 영향을 줬는가 — `미상(근거 없음)`

이 사고가 사고 원장(`docs/incidents/`) 밖에 있었다는 것 자체는 검토 보고서가 확인했다
(`docs/plans/2026-09-06-foms-system-review-report.md:134` — "13시간 정지·852초·DEAD 1,188·
실패잡 2,544 가 도구 docstring·AI_STATUS·커밋에만", `grep -rlE '852|2,544|13시간|1,188' docs/incidents | wc -l` → 0).
본 문서가 그 공백을 메우는 등재다.

## 3. 타임라인

| 시각 | 사건 | 근거 |
|---|---|---|
| `미상(근거 없음)` | 퇴역 잡이 실패 큐에 쌓이기 시작 | — |
| `미상(근거 없음)` | 2,544건 적재 상태 발견 | — |
| 2026-08-03 (HH:MM `미상(근거 없음)`) | 백업 `/c/tmp/foms-backups/prod-rq-failed-jobs-backup.json` 생성 후 2,544건 정리 | `docs/AI_STATUS.md:98` |
| 2026-08-03 | 같은 항목이 deploy `04f0fc59` 로 "후속 완료" 처리 | `docs/AI_STATUS.md:98` |

## 4. 근본 원인

**단정으로 적을 수 있는 것은 하나다.** 정리 시점의 판정이 "전부 퇴역 잡" 이었다는 것
(`docs/AI_STATUS.md:98`). 즉 큐에 남아 있던 실패잡은 그 시점 코드가 더 이상 실행하지 않는
잡 종류였고, 그래서 재실행이 아니라 삭제가 조치가 됐다.

**단정할 수 없는 것.**

- 어느 배포에서 어떤 잡 종류가 퇴역했고 그 잡들이 왜 실패 큐에 남았는지 — `미상(근거 없음)`
- 적재 기간과 증가 곡선 — `미상(근거 없음)`
- 실패잡이 2,544건까지 아무도 모르게 자란 것이 관측 부재 때문인지 — `미상(근거 없음)`.
  다만 검토 보고서는 워커 계열 전반에 "워크플로 7개에 알림 0" 을 별건으로 확인해 두었다
  (`docs/plans/2026-09-06-foms-system-review-report.md:50`, R3).

## 5. 복구

- 백업: `/c/tmp/foms-backups/prod-rq-failed-jobs-backup.json`(`docs/AI_STATUS.md:98`).
  **저장소 밖 로컬 임시 경로**라 이 문서로는 현재 존재 여부를 확인할 수 없다.
- 정리: 실패잡 2,544건 삭제(`docs/AI_STATUS.md:98`).
- 삭제에 쓴 도구·명령, 실행자, 소요 시간 — 전부 `미상(근거 없음)`.

## 6. 구조 변경

이 사고를 원인으로 지목하고 들어간 구조 변경은 저장소에서 확인되지 않는다 — `미상(근거 없음)`.

같은 `[2026-08-03]` 항목에 병기된 후속 작업은 넷이다(`docs/AI_STATUS.md:98`):
`/erp/history` 301 제거(dTTFB 240→19ms) · CI PG 16→17 · 마이그레이션 체인 왕복 검증 신설 ·
**CI Redis 커버리지**. 이 중 마지막 항목이 본 사고의 후속인지 별건인지는 그 줄만으로는
갈리지 않는다 — `미상(근거 없음)`.

## 7. 재발 여부·남은 것

- 재발 여부 — `미상(근거 없음)`. 정리 이후 운영 실패잡 수를 잰 기록이 저장소에 없다.
- 남은 것 ①: 실패잡 적재를 주기적으로 세어 사람에게 알리는 배선의 유무 — `미상(근거 없음)`.
- 남은 것 ②: 이 사고가 도구 docstring·`AI_STATUS` 에만 있어 "같은 유형이 반복되는가" 를
  기계가 셀 수 없었다(`docs/plans/2026-09-06-foms-system-review-report.md:134`). 본 등재가 그 자리다.

## 8. 근거 앵커

- `docs/AI_STATUS.md:98` — 실패잡 2,544건 정리·백업 경로·"전부 퇴역 잡"·deploy `04f0fc59`·CI Redis 커버리지
- `docs/AI_STATUS.md:353` — 같은 항목의 기록 보관 압축본
- `docs/plans/2026-09-06-foms-system-review-report.md:134` — R8, "운영 사고 4건이 사고 원장 밖"(실패잡 2,544 포함, `docs/incidents` 적중 0)
- `docs/plans/2026-09-06-foms-system-review-report.md:50` — R3, 워커 계열 관측·알림 부재(배경)
