# AI 작업 이력
> 최근 20개만 유지. 이전 기록은 git log 참조.
> 이 파일은 Cursor Hook(session_stop) 또는 Antigravity Workflow에 의해 자동 업데이트됩니다.

| 날짜 | 작업 | 수정 파일 | 커밋 |
|------|------|-----------|------|
| 2026-09-22 | **배포 누락 대조를 세션 시작 훅에 배선** — push 훅은 `git push`·`gh pr merge` 만 잡으므로 **브라우저 Merge 버튼으로 머지하면 아무도 안 본다**(2026-09-22 PR #414 가 그 경로였다). `session_start.py` 가 startup·resume 에서 `check_deploy_drift.py` 를 1회 돌려 누락이면 `[DEPLOY-DRIFT]` 를 주입한다(실측 1.5~2.3초, timeout 10초, 전부 fail-open). **GitHub 예약 작업으로 안 만든 이유**: Railway 토큰은 배포·환경변수 변경까지 되는 권한이라 DB 비번보다 세고, 이 저장소는 "운영 접속 정보를 GitHub 에 두지 않는다"를 이미 결정했다(drift-audit-daily.yml 주석, 등록된 비밀은 스테이징 계정 둘뿐). 로컬 토큰으로 세션 시작에 한 번 보는 쪽이 자격증명을 옮기지 않고 구멍 대부분을 덮는다 — 재발이 확인되면 그때 예약을 따로 결정한다(현재 n=1). 판정 불가(토큰 없음·오프라인)는 조용히 넘기고 hook_log 에만 남긴다 — 세션마다 잡음을 내면 사람이 훅 전체를 무시하게 된다(침묵은 "일치한다"는 주장이 아니다). 대조군 둘: 현재 상태(일치)에서 주입 0, 누락 스텁으로 바꾸면 `[DEPLOY-DRIFT] … 3커밋 뒤` 주입 확인. **부수 효과**: SessionStart 등록이 compact 전용이었어서 SESSION_LOG 가 압축 때만 쌓이고 있었는데, startup·resume 이 붙으면서 파일 docstring 이 원래 말하던 대로 일반 세션도 기록된다 | .claude/hooks/session_start.py, .claude/settings.json | (deploy 대기) |
| 2026-09-22 | **아이폰 마법사 고객명 사전 입력(빠른 시작 시트) 긴급 제거** — 사용자 지시. ＋ 를 탭하면 마법사로 가기 전에 고객명 한 칸짜리 시트를 열고, 거기 적은 값을 세션 저장소(`foms-wizard-quickstart-name`)로 넘겨 1단계에 미리 채우던 흐름을 전량 걷어냈다. `wizard-quickstart.js`(196줄)·전용 테스트(99줄) 삭제, `erp-pro.css` 의 `.foms-quickstart*` 57줄, `wizard.js` 의 세션 저장소 읽기·`applyQuickstartName`·호출 3곳(28줄), `layout_scripts.html` script 태그 1줄 제거 — `quickstart` 전수 grep 잔여 0건. **남긴 것**: `wizard.js` 의 `if (!isIosLike()) focusStepFirstField(...)`. 빠른 시작과 별개 축이고, 아이폰에서 진입 시 미리 포커스를 주면 그 뒤 그 칸을 탭해도 포커스 변화가 없어 자판이 영영 안 올라온다(같이 지우면 원래 되던 동작이 도로 깨진다). 제거 뒤 아이폰은 고객명 칸을 한 번 탭하면 자판이 올라온다(2단계 제품명 칸과 같다). 안드로이드는 원래 이 시트를 안 타서 무변화. 승격은 트리 동일 병합(PR #416, `git diff origin/deploy` 무출력). 검증: `node --check` exit 0 · APP_OK · 마법사 관련 275 passed · pre_push_smoke exit 0 · **스테이징 실브라우저(아이폰 UA 포함)**: script·시트 DOM·CSS 규칙 전부 없음, 마법사 정상 렌더 | static/js/foms/wizard-quickstart.js(삭제), tests/domains/test_wizard_quickstart_sheet.py(삭제), static/js/foms/wizard.js, static/css/foundation/erp-pro.css, templates/partials/shared/layout_scripts.html | **운영 반영** — deploy `b94e9c05f` · production `07b075672`(PR #416) |
| 2026-09-22 | **놓친 배포 감지 도구(check_deploy_drift.py)** — 사용자 보고 "푸시를 railway 웹훅이 안 잡아 자동 배포가 안 된다". 조사: PR #414 가 07:32:19Z 에 production 머지됐는데 Railway 가 **배포를 아예 안 만들었다**. 바로 앞 #412(06:05:18 머지 → 06:05:20 배포)와 바로 뒤 #415(07:45)는 2초 만에 잡혔고, 머지 커밋 `7ac1ee0e` 에 `railway-app` 체크수트도 있다 — **GitHub 은 이벤트를 보냈고 Railway 가 그 한 건을 흘렸다.** 이 세션의 게이트 수정과는 무관함을 네 축으로 확인: ① 커밋 10파일에 railway*.toml·Dockerfile·Procfile 없음(watchPatterns 설정 자체가 없다) ② Railway 는 GitHub App push 이벤트로 배포하므로 Actions 와 무관 ③ 유일한 연결 고리였던 `cancel-in-progress`→cancelled 체크는 발화 안 함(취소된 런 0건, `ci.yml` 은 production push 에 안 돈다) ④ 같은 저장소 내용으로 #415 는 정상 배포. 결과적으로 운영은 `0c9de166`(#414·내 커밋 모두 포함)으로 자가 복구. **문제는 이 누락이 저장소에 아무 신호도 안 남긴다는 것** — CI green, 브랜치 정상, 실패 없음, 1시간 13분간 옛 코드. 그래서 브랜치 머리와 Railway 실제 배포 커밋을 대조하는 읽기 전용 도구를 만들었다(프로젝트 id 를 `railway list --json` 에서 이름으로 찾고 GraphQL 은 id 명시 — 링크 오염 함정 회피, UA 필수). 운영 web / 스테이징은 서비스 이름이 달라(web vs FOMS) 타깃 표로 둔다. 판정 불가(토큰·CLI·API)는 exit 2 로 분리 — 조용한 초록이 이번 사건의 본질이라 0 으로 안 뭉갠다. 대조군 둘: 현재 상태 `[일치] production 0c9de166 / staging 8be4af04`(exit 0), 배포된 적 없는 브랜치로는 `ok=False behind=88` + 안 올라간 커밋 목록. `post_push_watch` 훅의 CI-GATE 안내에 배선(훅 테스트 18 passed) | tools/ops/check_deploy_drift.py(신규), .claude/hooks/post_push_watch.py, AGENTS.md | (deploy 대기) |
| 2026-09-22 | **도면 전달이 실측완료 표시까지 켠다 + deploy 전체 운영 승격** — 사용자 보고: 자가실측·지방 대시보드의 `도면 전달` 버튼이 확인문으로는 "실측을 완료하고 도면 단계로 넘길까요?" 라고 약속하는데 실제로는 단계만 옮기고 `Order.measurement_completed` 는 꺼진 채 남았다. 원인은 이름이 같은 **두 축이 서로를 갱신하지 않는 것**: 버튼(PC)·`실측 완료`(모바일·태블릿)는 모두 `POST /api/orders/<id>/quest/approve` → `COMPLETE_MEASUREMENT`(axis=MAIN, stage 만 씀)로 가고, 체크박스는 `POST /api/update_regional_status` 로 평면 컬럼만 쓴다. 수정: 체크리스트 write 3동작(컬럼·OrderEvent·원장)을 공용 writer `mark_checklist_flag` 로 뽑아 체크박스 경로를 치환(행동 불변)하고, quest 라우트가 전이 성공 직후 **같은 tx** 에서 그 writer 를 부른다 — 조건은 `auto_transitioned`(replay 제외)·MEASURE 단계·`is_regional or is_self_measurement`(체크리스트 API `_order_or_404` 와 같은 잣대). 서버 한 곳이라 PC·모바일·태블릿이 함께 고쳐진다. 감사는 원장과 같은 `change_set` 으로 `ORDER_CHECKLIST_UPDATED`. 운영 사전 측정(읽기전용): 백필 대상 **0건** — 지방·자가실측 83건 중 단계가 실측을 지났는데 표시가 꺼진 건 없다(음성 대조군: 같은 조건의 일반 ERP 주문 2,021건은 체크박스가 없어 대상 밖). 표시만 켜진 2건(#4561·#4614)은 AS 까지 끝난 완료 건이라 손대지 않았다(AS 전이가 stage 를 안 건드리는 설계의 정상 잔여물). **승격**: deploy↔production 이 공통 조상 `77fc7cb4b` 에서 갈라져 직접 PR 이 충돌 33건(전부 cherry-pick 사본 vs 원본 커밋의 구조 충돌)을 내서, 병합 커밋 트리를 deploy 트리와 바이트 단위로 동일하게 고정해 승격했다(PR #414). 손실 없음 확인: production 고유 23줄(AI_CHANGELOG 8·AI_STATUS 14·DECISIONS 1)이 전부 deploy 에 갱신된 형태로 존재, 삭제 15파일은 `01ca2daf` 리서치 센터 철거분. 검증: 신규 계약 4건(자가실측·지방 각 켜짐 / 일반 ERP 안 켜짐=음성 대조군 / 이미 켜진 건 원장 중복 없음) · pre_push_smoke EXIT 0 · CI 전체 green · **스테이징 실브라우저**: 시드 #4750 클릭 → stage MEASURE→DRAWING, flag false→true, OrderEvent 3종 + 원장 1행, 화면 체크 켜짐 확인 후 시드 정리 | foms/services/orders/regional_checklist.py(신규), foms/api/quest.py, foms/api/orders/regional.py, tests/domains/test_measurement_drawing_transfer_button.py, docs/plans/2026-09-22-drawing-transfer-marks-measurement-complete-spec.md, foms_order_mutation_writer_inventory.json | **운영 반영** — deploy `172713d09` · production `7ac1ee0e0`(PR #414) |
| 2026-09-22 | **푸시가 느려지는 원인 정밀 계측 + 게이트 목록 폐기** — 사용자 보고 "deploy·production 푸시가 갈수록 오래 걸린다". 계측 결과 CI 는 병목이 아니었다: 러너 큐 대기 중앙값 0초, FOMS CI 208초, 워크플로 4개 병렬. 진짜 비용은 **재시도** — 최근 155 사이클 중 39개 red(25%)이고, red 원인 20건을 전수로 보니 **전부** pre_push_smoke 타깃 배열 밖 파일이었다(핀·캐시체인·레지스트리·인벤토리 계약). 게이트가 느려서가 아니라 목록이 좁아서 못 잡았다. 더 큰 몸통은 따로 있었다: 이 체크아웃이 origin/deploy 와 **ahead 86 / behind 359, 분기 2026-09-06**(16일) 상태로 갈라져 559 파일이 달랐고, 로컬 86커밋 다수가 `…동기화` 커밋이었다. 음성 대조군으로 origin/deploy 정본 워크트리에서 같은 스위트를 돌리니 **10,204 passed / 0 failed / 104초** — 로컬에서 보이던 19 fail 은 전부 묵은 트리가 만든 허상이었다. 조치: ① 백업 브랜치 `backup/deploy-20260922`(88커밋 + 미커밋 14파일)로 전량 보존 후 로컬 deploy 를 origin 과 일치시켰다(로컬 전용 14파일은 상류가 의도적으로 삭제한 것 — `01ca2daf4` 리서치 센터 철거, `7e687d378`). ② 기본 게이트에서 33타깃 배열을 없애고 전체 스위트 `-n auto --dist loadfile` 로 바꿨다 — 구 게이트 130초/379개 → 신 게이트 130초/10,364개(27배). ③ `tests/visual` 도 목록 없이 통째로 돌린다: 브라우저 픽스처 테스트를 conftest 가 error 대신 skip 으로 떨어뜨린다(양성 대조군 — playwright 켜면 3 passed 로 정상 실행). ④ 인벤토리는 `refresh_inventories.py` 가 먼저 재생성하되 `lineno` 만 밀린 변화는 되돌린다 — 무조건 재생성하면 깨끗한 트리에서도 3파일 68줄이 바뀌어 공유 워킹트리 충돌이 된다(`--check` 는 lineno 까지 봐서 초록 트리에서 exit 1 을 내므로 게이트로 못 쓴다). ⑤ ci·harness-ci·postgres-lane 에 `concurrency` + `cancel-in-progress`(12초 간격 푸시 2건이 각각 풀 CI 를 끝까지 태운 사례). 별건 분리: drift-audit-daily 9/9 red 는 워크플로 고장이 아니라 운영 데이터 드리프트 보고다(ERP flat 938 · 기준선 924 · PAYMENT_AMOUNT_DRIFT=921 · AS 축 1) — 손대지 않았다. 게이트 검증: pre_push_smoke EXIT 0 · 130초 · 10,204 + 160 passed · 인벤토리 churn 0 | pre_push_smoke.ps1, refresh_inventories.py(신규), tests/visual/conftest.py, ci.yml, harness-ci.yml, postgres-lane.yml, PRE_PUSH_SMOKE.md, AGENTS.md, DECISIONS.md | (deploy 대기) |
| 2026-09-22 | **직권취소 인식 + 워크벤치 시각 표기 4건** — 사용자 실화면 보고 4개. ① 구매확정 뒤 판매자센터에서 취소한 집이 옛 주문 정리 띠에 `반품 접수` 로 남았다: 네이버는 그 건을 `claimType=ADMIN_CANCEL` · `claimStatus=ADMIN_CANCEL_DONE` 로 주는데 **저장소 어디에도 그 두 값이 없었다**(운영 12건이 "살아 있는 결제"로 읽힘). 공식 답변 #2028·#2168 근거로 라벨(`직권취소 완료`)·단계(done)·잠금·종류 별칭(ADMIN_CANCEL→CANCEL, 상태 접두어 폴백 포함) 네 축에 등재. ② 띠의 `전부 다시 읽기` 가 "(끝나면 새로고침)"이라 적고 아무도 새로 그리지 않던 것 → 응답에 `link_ids`·`since` 를 실어 전체 다시 읽기와 **같은 감시 함수**로 끝을 알아채고 화면을 다시 그린다. ③ 띠의 `마지막으로 읽은 때` 가 naive UTC 원문이라 9시간 전을 말했다(13:22 인데 04:18) → pane 과 같은 `_dispatch_time_text` 로 KST. ④ 시각 표기 전수 점검: 워크벤치·수집·트리아지 템플릿의 시각 출력 14곳을 생산자까지 따라가 확인 — 이미 KST 인 곳 11, 고친 곳 3(띠 read_at, 워터마크 `last_run_at`/`last_success_to` 3자리, 휴지통 `deleted_at`). 알림 payload·모바일 목록은 `Notification.to_dict` 가 이미 KST. 운영 실데이터 검증: 주문 #5198 의 NEW 4링크가 `직권취소 완료 / done / CANCEL / blocks=True`, REPAY 2링크는 살아 있음. 게이트: APP_OK · node --check · integrations+domains 9857 passed · pre_push_smoke EXIT 0(33타깃). | mapping.py, naver_ingest.py, naver-workbench.js, naver_workbench.html, naver_ingest.html, trash.html, test_naver_admin_cancel_and_time.py(신규) | (deploy 대기) |
| 2026-09-22 | **모바일 주문 삭제(MOBILE-DELETE-01)** — 모바일 주문 상세에서 ADMIN·CS·영업이 주문을 휴지통으로 보낸다. 실수 방지 4겹: 헤더 ⋯ 메뉴 안에만 노출 → 대상 카드(고객·상품·번호·단계·담당) → 사유 칩 3종(고객 취소·잘못 입력·직접 입력, 직접 입력은 메모 필수) → **길게 눌러 1.5초**(손 떼면 취소) → 목록 복귀 + **5초 되돌리기** 토스트(sessionStorage 로 페이지를 넘겨 전달). 서버: 새 정책 `ORDER_SOFT_DELETE`(teams CS/SALES, MEASURE→SALES 정규화, MANAGER 통과, VIEWER deny) + manifest 2건. `POST /api/orders/<id>/mobile-delete`·`mobile-restore` 는 기존 `soft_delete_order`·`record_action_reason`(새 사유 코드 없음)·생산 변경 알림·대시보드 캐시 무효화·legacy `status=DELETED` 미러(PC 휴지통 호환) 순서를 그대로 쓴다. 단계 가드는 서버 한 곳(`stage_guard_for_order`): 접수~도면 free / 고객컨펌~시공 warn(배너+사유 필수) / CS·완료 blocked(409, PC 전용). 화면 노출은 같은 판정값(`mobile_delete.can`)을 서버가 내려 **화면이 보여주는 것 == 서버가 허용하는 것**. PC bulk/trash 권한은 무변경. 검증: APP_OK · 도메인 계약 28 passed · PG 레인 3 passed(로컬 17 클러스터) · 관련 셸·핀 계약 815 passed · `node --check` exit 0 | order_mutation_policy.py, foms_order_mutation_policy_manifest.json, mobile_delete.py, orders/__init__.py, dashboard.py, erp_mobile_shell_header.html, order_delete_mobile_sheet.html, order_detail_mobile_v2.html, mobile-detail-delete.js, foms-mobile-delete.css, foms-shell.css, foms-mobile-surfaces.css, layout_head.html, 테스트 2개 + 핀 4개 | deploy 3b5f67d3d…cd73b47c7 → PR #411 · production 3d09e1a38 |
| 2026-09-22 | **구매확정 건에 반품 접수 버튼을 열지 않는다** — 사용자 보고: 이가령 집(2026090895141851, 상품주문 4건)에서 반품 접수가 네이버 400 `주문상태 확인 필요(반품 불가능 주문상태)` 로 실패. 규격 확인 결과 **API 경로가 없다**: 반품 요청 endpoint 문서가 진입 상태를 발송완료·배송중·배송완료로 한정하고, 공식 답변 (commerce-api Discussions #1618, 커머스API 권현철)이 "취소 요청/승인 API 는 구매확정 이전 주문만, 구매확정 후 취소는 판매자센터 [판매관리 > 구매확정 내역]"이라고 못박는다. 우리 결함은 `is_return_pending` 이 발송 여부·우리 멱등 표식만 봐서 **보낼 수 없는 건에 버튼이 열려 있던 것**. 새 술어 `is_purchase_decided`(사본 컬럼 → 스냅샷 폴백)를 그 술어 안에 넣어 버튼·모달 건수·서버 todo 를 한 번에 닫고, `request_return` 은 조용한 빈 결과 대신 다음 행동을 적어 거절한다. pane 은 `purchase_decided_count` 로 "구매확정된 주문 N건이라 네이버가 반품 접수를 받지 않습니다 — 판매자센터 [판매관리 > 구매확정 내역]에서 …" 라고 적는다. 운영 실행 4건으로 술어 검증(전부 decided=True·sendable=False). 게이트: APP_OK · services/integrations 1993 passed · pre_push_smoke EXIT 0(33타깃). | fulfillment.py, naver_ingest.py, naver_workbench_pane.html, test_naver_purchase_decided_return_block.py(신규) | (deploy 대기) |
| 2026-09-22 | **시공일 지난 적체 주문 일괄 완료 도구** — 도면 단계 실사용 전, 실측 853·도면 100 타일에 이미 끝난 주문이 섞여 진짜 도면 대상이 안 보였다. 운영 실측: 시공일 +7일 경과 후보 401(main 289 · AS 탭 건 112 · stage=AS_RECEIVED 80 은 대상 밖), 시공일 없는 실측 306 은 자동 판정 금지(검토 CSV). 기존 「단계 강제 변경」 일괄 경로는 `order.status` 를 덮어 AS 대시보드 증발(08-14 사고)이라 **전용 ops 스크립트** — `main` 은 stage+status, `as_stage_only` 는 stage 만(status·as_lifecycle·as_axis_status 불변). plan(read-only, 제외 사유 5종 전량 기록)→apply(청크 500·stale 건너뜀·workflow dict 스냅샷·이벤트+감사행)→rollback(dict 통째 복원). `--expect-env` TESTCLR 지문 가드. 스테이징 리허설 217건 apply→rollback 왕복 완전 일치, AS 지문 불변. 사용자 결정: AS 건 B안(stage 만)·quest 그대로·운영 apply 별도 승인 | tools/ops/bulk_complete_past_construction{,_core}.py, tests/domains/test_bulk_complete_past_construction_plan.py, tests/postgres/test_bulk_complete_past_construction_pg.py, docs/plans/2026-09-22-past-construction-bulk-complete-spec.md | 99a634ab2 |
| 2026-09-21 | **관리자 전 단계 강제 변경(ADMIN-OVERRIDE-01)** — 사용자 지시 "관리자는 모든 단계를 변경 할 수 있어야 돼", 범위는 삭제까지. 착수 전 실측으로 관리자가 막히던 자리 10곳을 확정했다(강제 변경이 본공정 8단계로 제한·일반 화면 역행 403·완료 409·도면/컨펌/생산/CS 승인·보류·시공 증빙). 설계의 핵심은 **두 축 분리**다: 권한 축 `admin_override`(ADMIN + 사유 필수, 본문 키 — 영수증 request_hash 가 본문만 해싱해 헤더로 실으면 평소 요청과 뚫은 요청이 같은 해시로 서로를 덮는다)는 업무 게이트만 풀고, 정합 축(If-Match·잠금 아래 expected_from·to_values·confirm·빈 사유·동일 단계·1000건 상한·삭제 주문·중복 발급)은 **관리자여도 안 풀린다**. 기존 `emergency_override`(매니저의 도메인 소유 넘기)와는 의미를 섞지 않았다. AS 3종 목표는 raw stage 쓰기 대신 AS 상태기계 명령으로 매핑했고(진행 중 AS→접수는 모델에 길이 없어 권한으로도 못 간다), 삭제 목표는 휴지통 미러 4종을 신규 `trash_mirror.py` 로 모아 복구까지 한 계약으로 묶었다. 완료는 신규 `cs_complete_service.py` 한 길로 모아 상태만 COMPLETED 이고 뒤가 빈 주문을 막았다. CEO 판정 fix — P0 2건: 일괄 강제 완료가 한 건 실패 시 앞 건을 조용히 되감던 것(전체 롤백으로), 완료 목표를 전이 밖으로 빼면서 빈 사유 가드를 잃은 것(테스트 7793건으로도 안 잡혔다). 그 밖 P1·P2 10건(단건 동일 단계 가드·모달 완료 목표 미탑재·거부 시도 감사 비대칭·뚫은 완료의 상태 감사 누락·사유 시트 빈 입력·생산 3라우트 배선 누락). 통합 검증자가 잡은 큰 것: **바뀐 JS 6개가 옛 자산 핀으로 실려 서버는 뚫는데 화면에 재시도가 안 뜨던 상태** — 핀을 올리고 재시도 배선 JS 를 싣는 템플릿 줄 전수 가드를 넣었다. 게이트 총괄 재실행: APP_OK · domains 7815 passed · contracts+harness+services+security 2606 passed · pre_push_smoke EXIT 0(33타깃). **운영 영향: 매니저는 강제 변경으로 완료를 만들던 길을 잃는다(미결 — 사용자 확인).** 절차 기록: 동결 뒤 워커에게 메시지를 보내면 워커가 다시 깨어나 편집한다 — 총괄이 네 번 깨워 8분 게이트가 두 번 헛돌았다. 원장 `docs/plans/2026-09-21-admin-full-stage-control-ledger.md` | admin_override.py·trash_mirror.py·cs_complete_service.py·stage_override_targets.py·stage_override_admin.py·draftsman_receipt_authz.py(신규), stage_override.py, status.py, field_update.py, production/orders.py, construction/orders.py, cs/complete.py, quest.py, erp_orders_draftsman.py, as_cycle_service.py, erp-stage-override.js, foms-admin-override.js, erp-order-shared.js 외 신규 테스트 6파일 | (deploy 대기) |
| 2026-09-21 | **일부만 취소·반품된 주문 띠(워크벤치 처리 탭)** — 사용자 보고: "네이버엔 반품 2건인데 FOMS 엔 1건". 운영 DB 대조 결과 두 건 다 수집·판정·알림까지 정상이었고(#5096 정재일 · #5268 김현정, 둘 다 `RETURN_REQUEST`), 안 보인 것은 **유령 띠의 모집단**이었다 — 그 띠는 "붙은 링크가 전부 취소"만 세는데 #5268 은 추가결제 집 6링크가 살아 있어 5/11 로 빠졌다. 유령 판정은 한 글자도 안 건드리고 띠를 하나 더 둔다: 집 하나가 통째로 취소·반품 + 같은 주문에 살아 있는 링크 + 그 클레임이 확정 전. 셋째 조건이 없으면 운영 실조회로 24건이 올라오는데 대부분 재결제로 정리된 정상 모양이고 그 정리는 옛 주문 정리 띠가 이미 말한다(확정 전은 1건). 링크 읽기는 사본 컬럼 한 벌(`_mirror_link_rows`)로 묶어 유령 띠와 같은 리더를 쓴다 (raw_snapshot TOAST 회피). 불가역 버튼 없음 — 승인·거부는 그 집 pane. 게이트: APP_OK · services/integrations 1989 passed · pre_push_smoke EXIT 0(33타깃). | ghost_orders.py, naver_ingest.py, naver_workbench.html, naver-workbench.css, test_naver_partial_claim_band.py(신규), foms_order_mutation_writer_inventory.json | (deploy 대기) |
| 2026-09-20 | **파이프라인 잔여 결함 4묶음(PIPELINE-GAP-02)** — 사용자가 고른 네 묶음: 누르면 거부당하는 버튼 · 마지막 단계·게이트 구멍 · 기록이 안 남는 전이 · 모바일·말투. ① 경리팀(ACCOUNTING)이 승인하면 슬롯이 '경리팀' 칸에 들어가 CS 필수 승인이 영원히 미달이던 것 → 슬롯 키를 필수 팀으로 고르고 누른 팀은 `by_team` 으로 보존. ② 실측 3보드 [완료] 가 CS 확인·보류·AS 게이트를 건너뛰고 주문을 완료로 만들던 우회로를 닫았다 — 신규 `complete_path_policy.py`(추가 쿼리 0)가 화면·서버 한 술어를 쓰고, CS 단계면 `cs/complete`, 아니면 사유와 '승인하러 가기' 딥링크. 리뷰 P1: 우회 진입점이 `update_order_field` 말고 `update_order_status`·`bulk_update_order_status` 두 곳 더 있었다(셋 다 409 `USE_CS_COMPLETE`). ③ 시공 증빙 게이트 기본 ON + `missing` 을 사람 말('완료 사진 2장'·'고객 서명')로. ④ 도면 수령 확정에 새 명령 `DRAWING_RECEIPT_CONFIRM`, 시공 불가는 단일 명령 다중 to_values 로 엔진 전이 — 도면만 TRANSFERRED 이고 단계가 다르면 되돌리지 않고 도면 축만 확정(옛 조용한 역행 제거), 전이 실패 시 파일 정리가 일어나지 않는 순서로 고정. ⑤ 쓰이지 않던 `confirm/customer` 삭제(2026-02-07 출생 시엔 생산으로 옮겼으나 02-13 결정으로 기록 전용이 됐고 화면 호출자는 출생부터 0건 — 조사 결과 원장 §2). ⑥ 모바일 v2·v3 에 제작 시작/완료·수정 제작·제작 취소·완료 취소·시공 불가. 사유 입력은 공용 시트(prompt 금지). 리뷰 P1: v3 시공 버튼 술어가 서버보다 좁아 CS·영업팀에 버튼이 0개였다 → `can_act_construction` 으로 통일. v3 진입 관측은 하루 1행·별도 세션. ⑦ 모바일 단계 배지 CS→'CS', object.html 라벨표 보강. CEO 멀티 에이전트(설계 1 + 워커 4 + 통합 + 리뷰 2 + 판정 + fix). 게이트 총괄 재실행: APP_OK · domains 7700 passed · contracts+harness+services+security 2606 passed · pre_push_smoke EXIT 0(33타깃) · 인벤토리 4종 --check 0. **운영 영향: 실측 보드 완료가 CS 단계 밖에서 막히고, 시공 완료에 사진·서명이 필수가 된다(배포 즉시).** 원장 `docs/plans/2026-09-20-pipeline-gap-batch2-ledger.md` | complete_path_policy.py(신규), drawing_receipt_command.py(신규), quest_approve_authz.py, quest.py, field_update.py, status.py, construction/orders.py, erp_orders_draftsman.py, cs/confirm.py(삭제), erp_permissions.py, feature_flags.py, erp_mobile_order_display.py, mobile_queue.html×2, persona_home_production/construction.html, foms_reason_sheet.*(신규), dashboard_grid.html, tablet_dashboard_sheet.html 외 신규 테스트 5파일 | (deploy 대기) |
| 2026-09-20 | **실측 막다른 길 해소 + 이월 결함 6건(MEASURE-DEADEND-01)** — 제보: PC 에서 실측 완료를 눌렀는데 도면으로 안 넘어간다(이영아). 원인은 관리자 강제 단계 변경(regress)이 quest 를 되돌리지 않는 것 — 완료 quest 가 남고 앞 배치가 그것을 완료 배지로 그리면서 승인 버튼이 사라져 막다른 길이 됐다. 스테이징 HTTP 로 `OrderEvent` 원문 확인(승인 10초 뒤 regress), 같은 모양 스테이징 4건·운영 5건(읽기 전용 1회, 전부 실측). 사용자 결정대로 둘 다: regress 가 그 단계 quest 를 다시 OPEN 으로 돌리고(`reopened_by_override`), 남은 건은 완료 배지 옆 [다음 단계로 넘기기] 재전이 버튼(PC 그리드·모바일 카드·모바일 상세)으로 사람이 넘긴다 — 재전이는 승인 기록을 덮어쓰지 않는다(옛 경로는 승인자를 actor 로 바꿨다). 함께 처리: 컨펌 quest 없는 주문의 제작 시작 409(`reason QUEST_MISSING`) · `POST /quest`·`PUT /quest/status` 저장 버그(flag_modified)와 수동 COMPLETED 권한(ADMIN/MANAGER+사유) · 팀 승인 버튼 노출을 서버 술어(`approvable_teams`)와 같게 하고 모바일에 팀 승인 신설 · 360° 타임라인이 엔진 전이를 읽고 이벤트 라벨 7종 등재 · 주문 영업 담당(OrderAssignment id 축)의 도면 수령 확정. CEO 멀티 에이전트(설계 1 + 워커 4 + 통합 + 리뷰 2 + 판정 + fix). 리뷰 P2: 같은 단계에 완료·열린 quest 가 함께 있으면 화면과 서버가 다른 quest 를 잡던 것 → `find_stage_quest_for_approve`. 게이트 총괄 재실행: APP_OK · tests/domains 7641 passed · contracts+harness 624 passed · pre_push_smoke EXIT 0(33타깃). 원장 `docs/plans/2026-09-20-measure-deadend-and-deferred-defects-ledger.md` | quest.py, stage_override.py, quest_transition_service.py, quest_approve_authz.py, quest_approve_cta.py, erp_quest_display.py, production/orders.py, erp_orders_draftsman.py, order_timeline_v3.py, order_event_display.py, dashboard_grid.html, order_detail_mobile_v2.html, erp_mobile_queue_card_v2.html, erp-dashboard-quest.js, erp-quest-approve.js 외 신규 테스트 10파일 | (deploy 대기) |
| 2026-09-20 | **고객 컨펌 → 생산 워크플로 정정(CONFIRM-FLOW-01)** — 운영 제보 4건: 고객컨펌 승인 뒤 배지 없음 · 이다은 주문이 생산 탭에서 `필수 승인이 완료되지 않아 전이할 수 없습니다` · 모바일 고객 컨펌이 상세 링크 · 전체 프로세스 검수. 원인은 "승인 완료" 판정이 세 곳에서 달랐던 것(라우트·생산 보드 표시는 담당자 승인을 읽고, 생산 API 게이트 `check_quest_approvals_complete` 는 team_approvals 만) + 표시가 COMPLETED quest 를 버림 + `is_command_required_stage` 에 CONFIRM 잔존. 사용자 결정: 컨펌 승인 즉시 생산 단계로(새 명령 `CUSTOMER_CONFIRM`), 생산 보드 제작대기/제작중 = 단계 + current run, 제작 시작 = run 발급·제작 취소 = run 종결(단계 유지). 판정 SSOT 모드 인지형, stale 재요청 409 `ALREADY_TRANSITIONED`(CEO P1: 연타가 PRODUCTION quest 를 만들어 제작 완료를 잠갔다), 완료 배지 3표면, 모바일 인라인 승인, PC 승인 확인창·연타 잠금, SQLite 레인 `sqlite_where` 부분 유니크. CEO 멀티 에이전트(설계 1 + 워커 4 + 통합 + 리뷰 2 + 판정, fix 는 한도로 총괄 직접). 원장 `docs/plans/2026-09-17-confirm-to-production-workflow-ledger.md` | erp_policy_quests.py, quest_transition_service.py, order_transition_service.py, quest.py, production/orders.py, production_read_model.py, production_dashboard_display.py, erp_quest_display.py, quest_approve_cta.py, dashboard_grid.html, erp_mobile_queue_card_v2.html, order_detail_mobile_v2.html, erp-dashboard-quest.js, models.py 외 | (deploy 대기) |
| 2026-09-16 | **원격 브랜치 236개 → 2개 + 주간 리서치 자동화 제거** — 사용자 질문("푸쉬할 때마다 브랜치가 새로 생기는 게 꼭 필요한가")에서 시작. 승격 스크립트가 `promote/own-<stamp>` 를 만드는 것 자체는 필요하다(production 은 PR 로만 들어간다) — 문제는 병합 뒤에도 남는다는 것. PR 상태로 분류해 merged 220개 삭제, 저장소 옵션 `delete_branch_on_merge=true` 로 재발 차단. 남은 17개는 커밋 제목을 deploy·production 전 이력과 대조해 판정 — 실제 작업은 전부 반영돼 있었고 고유분은 승격용 merge 커밋 3개·인벤토리 재생성 4개·빈 CI 트리거 2개뿐이라 삭제. `b2b-portfolio`·`feature/three-depth-options` 는 같은 tsx 97줄 1커밋(Flask+Vanilla JS 스택 밖)이라 삭제. `automation/weekly-coding-research-center` 는 워크플로가 매주 월 01:15 UTC 에 재생성하던 것 — 실제 역할은 RSS 59개(GitHub Changelog·SDK 릴리스·PostgreSQL News·arXiv) 수집 후 마크다운 표 작성이 전부이고 FOMS 코드는 안 읽는다. 11주 연속 success 였지만 PR #1 은 한 번도 병합 안 됨, 산출물은 HYPOTHESIS_BACKLOG 꼬리 8줄 중 5줄이 같은 논문 제목 중복. 워크플로·`tools/research_center/` 8파일·출력 문서(RADAR·HYPOTHESIS_BACKLOG·research/)까지 제거하고 계약 테스트 허용목록·tools/README·file_size_baseline 의 죽은 참조도 정리(16파일 2,830줄). **부수 발견 2건**: ① 메인 체크아웃 deploy 가 갈라져(로컬 68 / 뒤처짐 310) 거기서 돌린 pre_push_smoke 가 **21 targets** 였다 — origin/deploy 기준 워크트리는 33 targets. 뒤처진 체크아웃의 초록은 초록이 아니다. ② 그 68개에 묶여 push 안 된 기록 2건이 저장소에 없었다: 2026-09-08 워커 자멸 사고(감시 루프 코드는 production 까지 들어갔는데 사고 기록만 없었다)와 AS 전달 배정 원장의 "푸시 대기"(실제로는 PR #338 병합 완료). 둘 다 복원. 메인 트리 정렬은 **하지 않았다** — 다른 세션의 미커밋 12개(도면·마법사)가 살아 있어 reset --hard 가 지운다 | .github/workflows/coding-research-center-weekly.yml(삭제), tools/research_center/(삭제), docs/evolution/{RADAR,HYPOTHESIS_BACKLOG}.md(삭제), docs/incidents/2026-09-08-worker-self-kill-on-redis-restart.md(신규), docs/plans/2026-09-09-as-sales-delivery-ledger.md, tests/contracts/runtime/foms_namespace_surface_tests.py, tests/harness/file_size_baseline.json, tools/README.md | deploy `01ca2daf4`·`3a48b6bf9` |
| 2026-09-16 | **도크 핫픽스 2건 — 취소된 집을 '이번 주문' 으로 부르던 것 + 수식어 옵션 키 자동입력** — ① 사용자 제보(#5158 김선미): 재결제가 **두 번** 일어난 주문에서 도크가 09-14 에 취소 확정된 집(…5051)을 `이번 주문(재결제)` 라 부르고 그 금액 1,134,200원을 청구액으로 보여줬다. 운영 실측 세 집 — NEW 9건 전부 CANCEL_DONE(1,932,800) · REPAY 5건 전부 CANCEL_DONE(1,134,200) · REPAY 3건 생존(1,034,800). 원인은 죽은 집 판정이 `superseded = has_repay and relation == "NEW"` 한 줄, **관계 한 축뿐**이라 클레임이 들어가지 않은 것. `_deposit_target` 도 같은 축을 읽어 금액 카드가 2,169,000원을 말했다. 수정 — `_household_settled`(그 집 상품주문이 전부 `phase=done` + MONEY_BACK_CLAIM_KINDS)를 죽은 집 축에 더하고(`superseded = settled or 기존식`), 라벨은 `취소된 결제`·note 는 `_SETTLED_NOTE` 로 갈라 쓴다(대체된 집과 취소된 집은 담당자가 할 일이 다르다). 살아 있는 재결제가 둘이면 마지막만 `이번 주문(재결제)`. **금액 카드는 무수정** — 판정 한 곳을 고치니 1,034,800원으로 따라 맞았다. 집별 클레임은 링크 루프에서 한 번만 모은다(`claims_by_no`). ② 사용자 제보(#5321 하반 침대붙박이장): `전부 넣기` 가 색상만 조용히 건너뛰었다. 옵션 원문이 `제품: 하반 침대 붙박이장 30cm / 붙박이장 색상: 클린 화이트` 로 키가 **`붙박이장 색상`** 이라 `COPY_TARGET_BY_KEY` 정확 일치가 빗나갔고, 칩에 `data-naver-dock-target` 이 안 붙어 `전부 넣기` 모집단에서 빠졌다. `resolve_copy_target` 신설 — 정확 일치 → 괄호 뗀 뒤 일치 → **마지막 낱말** 일치(`붙박이장 색상`→`색상`). 넓히면서 막은 것 2가지: 포함 검색 금지(`색상표`·`색상 선택` 제외), 짝 불일치로 키가 통째로 남은 조각(`색상 ／ 사이즈`)에는 꼬리 규칙 미적용 — **기존 계약 테스트가 이 회귀를 실제로 잡았다**. 검증: 신규 계약 테스트 11건(도크 8 — #5158 재현·금액·음성 대조군 4종 / 옵션 키 3 — 재현·오탐·괄호), 수정 전 코드에서 red 확인. integrations 1,982 · domains+contracts 7,533(5 skipped) · pre_push_smoke exit 0 · APP_OK. 화면·자산 핀 무변경 | dock.py, test_naver_dock_settled_household.py(신규), test_naver_dock_autofill_map.py | **운영 반영 완료** — deploy `4fc0ed0f4`·`732155144` · PR #388 · production `042543019` |
| 2026-09-15 | **주소가 긴 초안이 모바일 홈을 통째로 밀던 것 + deploy CI 복구(PR #387 · production `b7a6f14e3`)** — 사용자 제보: 이어쓰기 띠를 펼치면 홈 정렬이 틀어진다. **운영·스테이징 공통, 주소가 길 때만**. 모바일 홈 본문은 `display: grid`(`foms-shell.css:393`)이고 그리드 아이템 기본값은 `min-width: auto` 라 **내용 최소 폭 아래로 못 줄어든다.** 띠 안의 주소가 `white-space: nowrap` 이라 긴 주소 하나가 최소 폭을 화면 밖으로 밀고, 줄어들 수 없으니 **그리드 칼럼째 넓어져 홈 전체가 오른쪽으로 밀렸다.** 타워 CSS 는 이미 이 계약을 알고 자식 5개에 `min-width: 0` 을 걸고 주석까지 달아 뒀는데(`dashboard-control-tower.css:18`) 나중에 들어온 이 띠만 명단에서 빠져 있었다. 수정은 컴포넌트 CSS 에 `min-width:0; max-width:100%` (타워·큐 두 분기에 함께 적용) + 긴 고객명용 말줄임 + 핀 상향. **재현 실측**: 실제 파셜·CSS·JS 에 **홈과 같은 그리드 부모**를 씌운 harness(390px, 초안 10건·끝 1건 긴 주소)에서 문서 폭 **710 → 390**. 그리드 부모 없이 만든 첫 harness 에서는 재현되지 않았다 — 부모가 블록이라 제약이 없었고, 그게 이 결함이 컴포넌트 단독 점검을 빠져나간 이유다. **함께**: deploy CI 의 `test` 가 `a5436463e`(빠른 시작 시트, 타 세션)가 `erp-pro.css` 핀을 올리면서 리터럴 핀을 박아 둔 `test_tablet_rail_contract.py` 를 안 고쳐 빨갛던 것을 복구했다. 값만 바꿔 초록을 만들지 않고 **진짜 불변식**(자식 @import 핀만 새로우면 부모가 캐시에 남아 아무도 새 URL 을 못 본다 → "부모 핀 ≥ 자식 핀")으로 바꿨고 음성 대조군으로 방향을 확인했다. 초안 띠 핀 테스트도 같은 이유로 리터럴 → "핀 존재 + 전체/조각 일치"로. 검증: APP_OK · `pytest -n auto` **9,749 passed 0 failed** · pre_push_smoke exit 0 · PR 검사 4종 green | foms-draft-resume.css, dashboard.html, dashboard_main.html, test_draft_resume_render.py, test_erp_order_draft_list.py, test_tablet_rail_contract.py | **운영 반영 완료** — PR #387 · production `b7a6f14e3` |
| 2026-09-15 | **아이폰 마법사 자판 — 원인 제거로 해결(PR #386 · production `406ea4120`)** — 앞선 세 번(PR #383·#384·#385)이 전부 빗나갔고, 사용자의 한마디 "근데 왜 다음 단계 제품명은 될까?" 가 답이었다. 사파리는 **제스처 안에서 일어난 진짜 포커스 변화**에만 자판을 올린다 — 제품명은 "다음" 버튼 탭(제스처) + 아직 포커스가 없던 칸(변화)이라 둘 다 만족하고, 고객명은 진입 때 **우리가 미리 커서를 놔둬서** 탭해도 변화가 없다. 즉 **우리가 넣은 편의 기능이 원래 되던 동작을 망가뜨리고 있었다.** 강제 `blur()`→`focus()` 로 되살리려 한 세 번이 전부 재주였고 옳은 수는 반대였다: **iOS 에서는 진입 자동 포커스를 하지 않는다**(`isIosLike()` 분기, 판별은 `estimate-preview.js:147`·`mobile-push.js:57` 선례 그대로). `armEntryKeyboard`·`bindKeyboardRescue` 는 전부 걷어냈다(승격 트리 잔존 0줄). 단계 전환 포커스는 그대로 둔다. 아이폰이 잃는 것은 "커서가 미리 놓여 있는 것" 하나, 얻는 것은 탭 한 번에 실제로 입력이 되는 것. **실기기 확인 완료**("포커스 없고 고객명 클릭 했을 때 자판 올라 와"). **정정**: PR #385 에서 `visualViewport` 736 → 435 를 자판이 올라온 증거로 제시했는데 사용자 확인 결과 그때 자판은 없었다 — 그 판정도, 그 위에 "해결됐다"고 한 보고도 틀렸다. 그 사이 운영에는 잘못된 판이 올라가 아이폰 사용자가 고객명 칸에 입력을 못 했고, 이 PR 이 그것을 덮었다. 검증: APP_OK · `pytest -n auto` **9,748 passed 0 failed** · pre_push_smoke exit 0 · PR 검사 4종 green · 운영 서빙 자산에 `isIosLike` 확인 | wizard.js, wizard_shell.html, erp_order_js.html, test_wizard_focus_contract.py | **운영 반영 완료** — PR #386 · production `406ea4120` |
| 2026-09-15 | **아이폰 마법사 자판 최종 해결(PR #385 · production `8a090bd14`)** — 원격 추측으로 두 번(PR #383·#384) 빗나간 뒤, 임시 진단 패널(`?kbdebug=1`)을 붙여 **기기 로그를 받아** 확정했다. 로그가 두 가지를 갈랐다: ① **사파리는 포커스를 거부하지 않는다 — 자판만 안 올린다**(제스처 없는 `focus()` 도 `activeElement` 는 바뀐다). 그래서 "포커스가 먹었는지 보고 실패했을 때만 대비책"(#383) 구조는 대비책이 아예 안 걸렸다. ② **이미 포커스된 칸을 직접 탭해도 자판이 안 올라온다**(포커스 변화가 없으니 올릴 이유가 없다). 진입 시 커서를 미리 놓기 때문에 **사용자의 첫 탭이 하필 그 경우**가 되고, #384 코드는 그 탭을 "직접 겨눴다"로 보고 물러나기까지 했다 — 실사용에서 계속 안 떴던 이유다. 수정: 첫 탭 예약에서 겨눈 것이 **그 칸 자신**이면 물러나지 않고 `blur()`→`focus()` 로 끌어올리고, 예약이 한 번에 풀리므로 그 뒤로도 사는 구조(`bindKeyboardRescue`)를 따로 걸었다 — 그 칸 재탭 시 포커스는 있는데 자판이 내려가 있으면 다시 잡는다. **자판이 내려가 있을 때만** 개입한다(입력 중 `blur→focus` 는 캐럿을 튀게 한다). 판정은 `visualViewport` 높이이고 (자판을 묻는 API 는 없다) 그 수단이 없으면 아예 개입하지 않는다. **실기기 확인 완료**(736 → 435 로 자판 올라옴, 입력 중 캐럿 안 튐). 진단 패널은 역할을 다해 전부 걷어냈다 — 승격 트리에서 `kbdebug` 잔존 0줄 확인. 검증: APP_OK · `pytest -n auto` **9,751 passed 0 failed** · pre_push_smoke exit 0 · PR 검사 4종 green · 운영 서빙 자산에 `bindKeyboardRescue` 확인. **남는 한계**: 진입 즉시 탭 없이 자판이 뜨는 것은 아이폰에서 원리상 불가능하다(새 문서 이동이라 제스처가 안 넘어온다). 오버레이 구조 변경은 별도 건 | wizard.js, foms-wizard.css, wizard_shell.html, erp_order_js.html, test_wizard_focus_contract.py | **운영 반영 완료** — PR #385 · production `8a090bd14` |
| 2026-09-15 | **아이폰 진입 키보드 2차 운영 반영(PR #384 · production `6f4f319b0`)** — 1차(PR #383)가 아이폰에서 여전히 안 먹었다. **내 판정이 틀렸다**: 사파리가 제스처 없는 `focus()` 를 *거부한다*고 봤는데 실제로는 **포커스는 주고 키보드만 안 띄운다.** 그래서 두 겹으로 막혀 있었다 — (1) 진입 포커스가 `document.activeElement === el` 로 성공 판정돼 첫 탭 대비책이 **아예 안 걸렸고**, (2) 걸렸어도 핸들러가 부르는 `focusStepFirstField` 의 "이미 포커스돼 있으면 덮어쓰지 않는다" 가드에 막혔을 것이다. 사용자 재제보 "고객명은 안 되는데 제품명은 된다" 가 이 판정과 정확히 맞물린다(제품명은 "다음" 탭 제스처 안). 수정: 첫 탭 예약을 **조건 없이** 걸고, 탭 안에서 **`blur()` → `focus()`** 로 키보드를 끌어올린다(이미 포커스된 칸에 `focus()` 만 다시 부르면 아무 일도 없다 — 1차에 빠져 있던 결정적 한 줄). 청취를 `document` 캡처로 옮겨 루트 밖 탭도 세고, 손가락이 10px 넘게 움직인 `touchend` 는 스크롤로 보고 기회를 안 쓴다(스크롤마다 키보드가 튀면 더 나쁘다). 다른 칸·버튼을 겨눈 탭이거나 사용자가 스스로 커서를 놓으면 예약을 푼다. 안드로이드는 이미 키보드가 떠 있어 무해하다. **이것도 우회다** — 진입 즉시 탭 없이 키보드가 뜨는 것은 아이폰에서 원리상 불가능하고, 그러려면 마법사를 새 페이지가 아니라 같은 화면 위 **오버레이**로 여는 구조 변경(라우팅·초안 복구·뒤로가기)이 필요하다. 검증: 승격 트리 APP_OK · `pytest -n auto` **9,749 passed 0 failed** · pre_push_smoke exit 0 · PR 검사 4종 green. 계약 테스트를 새 구조로 고치고 2건 추가(스크롤에 안 튐·blur 를 거침). **미확인**: 실기기 확인 전이다 | wizard.js, wizard_shell.html, erp_order_js.html, test_wizard_focus_contract.py | **운영 반영 완료** — PR #384 · production `6f4f319b0` |
| 2026-09-15 | **아이폰 마법사 진입 포커스 운영 반영(PR #383 · production `e1ce68a78`)** — 사용자 제보: 안드로이드는 1단계 고객명 칸에 커서가 놓이는데 아이폰은 아무 일도 없다. **코드 결함이 아니라 플랫폼 규칙이었다** — 아이폰 사파리는 사용자 활성화(제스처) 없이는 입력 칸에 포커스를 아예 주지 않는다. 마법사는 `?open=erp-order&wizard=1` 로 **새 문서를 여는 일반 이동**이라 진입 시점에 제스처가 없다(FAB 탭 활성화는 이전 문서 소속). 안드로이드 크롬은 같은 자리에서 준다. 같은 규칙의 앞뒷면으로 설명이 맞아떨어진다 — **단계 전환은 아이폰에서도 된다**(버튼 탭 제스처 안 동기 호출). 다른 범인도 훑었다: `voice-input.js` 의 입력칸 재배치(`resolveWrap`)와 `wizard-enrich.js` 의 `focus()` 3곳은 전부 클릭 핸들러 안이라 진입 무관, 두 스크립트의 `readyState` 분기도 로드 순서가 고정이라 경합 없음. **수정**: `focus()` 호출만으로 성공을 단정하지 않고 `document.activeElement` 로 실제로 먹었는지 보고, **안 먹었을 때만**(=아이폰) 사용자의 첫 탭을 빌려 첫 칸에 커서를 놓는다(제스처 안 동기 호출이라 키보드까지 올라온다). 탭이 이미 다른 칸·버튼·링크·라벨을 겨눴으면 그 의도가 우선이라 물러나고, 사용자가 스스로 커서를 놓으면(`focusin`) 해제한다. 안드로이드는 진입 포커스가 성공하므로 대비책이 아예 안 걸린다. 근본 해법(마법사를 같은 문서 안 오버레이로 열어 여는 제스처 안에서 focus)은 라우팅 구조 변경이라 범위 밖. 소스 계약 테스트 5건 신설 — 실기기에서만 드러나는 규칙이라 한 줄만 옮겨도 조용히 되돌아간다(특히 `setStep` 과 `focus` 사이에 타이머·await 가 끼는 것을 막는다). 검증: 승격 트리 APP_OK · `pytest -n auto` **9,748 passed 0 failed** · pre_push_smoke exit 0 · PR 검사 4종 green(perf-gate 포함). **미확인**: 진단은 플랫폼 규칙과 코드 대조로 한 것이고 실제 아이폰 확인은 사용자 몫으로 남았다 | wizard.js, wizard_shell.html, erp_order_js.html, test_wizard_focus_contract.py | **운영 반영 완료** — PR #383 · production `e1ce68a78` |
| 2026-09-15 | **모바일 제보 3건 운영 반영(PR #382 · production `49cb68247`)** — ① **주문 마법사 입력 동선**: 진입 시 고객명 칸에 커서, "다음"으로 넘기면 그 단계 첫 칸에 포커스(버튼 탭 제스처 안 **동기** 호출이라 아이폰에서도 키보드가 올라온다 — setTimeout 뒤로 미루면 캐럿만 움직인다). 칸을 하드코딩하지 않고 "그 단계에서 보이는 첫 입력 칸"을 찾는다. 날짜·시간 계열은 제외(자동 포커스가 네이티브 피커를 열어 화면 절반을 덮는 단말이 있고 3단계 첫 칸이 실측일). **진입 즉시 키보드는 아이폰에서 불가능**하다 — `?open=erp-order&wizard=1` 은 페이지를 통째로 새로 여는 일반 이동이라 FAB 탭 제스처가 새 문서로 넘어오지 않고, iOS 사파리는 제스처 안 동기 `focus()` 에서만 키보드를 올린다(`autofocus` 도 같다). 되게 하려면 마법사를 같은 문서 안 오버레이로 여는 라우팅 변경이 필요해 범위 밖으로 뒀다. ② **규격 가로 복합 표기**: `4120+4121+2354`·`5700(2402+1864+1638)` 를 받고 합계는 기존 SSOT `evalSpecWidthMm` 을 **호출**한다(마법사에서 재구현 금지). 가로는 별도 1줄 전체폭, 아랫줄에 깊이·높이·삭제. ③ **다크모드 입력칸**: `/edit/<id>` PUSH 재발송 사유 칸 글자가 안 보였다. `style-pro-max.css` 가 `.form-control` 배경만 `#F2F2F7`(포커스 `white`)로 `!important` 고정하는데 글자색은 부트스트랩 `--bs-body-color` 를 따라가 다크에서 대비 2.09:1 이 됐다. **맞불 `!important` 대신 지고 있던 선언의 값을 토큰화** (`--foms-input-bg`/`-focus`/`-border`) — 캐스케이드 승자가 그대로라 새 회귀가 구조적으로 없다(맞불 안은 실제로 포커스 테두리 브랜드색과 `dashboard-grid.css:284` 를 밀어냈다). 라이트 값은 기존 하드코딩과 동일 → 라이트 무변경. `style-pro-max.css` 는 `?v=` 핀이 아예 없어서 함께 붙였다. ④ **초안 삭제 신설**: 이어쓰기 띠가 보여주기만 했다. 서버 `DELETE /order-draft?key=` 는 이미 있어 화면만 붙였고, 되돌릴 수 없는 삭제라 확인창·요청 전 잠금(연타 2건이 나가면 목록이 밀려 옆 초안까지 지운다)·성공/실패 경로 분리·조각 교체 뒤 늦은 응답의 옛 DOM 보호를 함께 넣었다. **실화면에서 잡아 고친 것 3건**(워크플로 보고만 믿었으면 전부 놓쳤다): (a) 깊이 칸이 48px 로 눌려 마이크만 남아 있었다 — `grid-column: 1 / -1` 은 명시 2열까지만 덮는데 삭제 버튼이 만든 3번째 암시 열이 1행에 비어 깊이 칸이 그리로 자동 배치됐다. 세 칸의 행을 명시. (b) **핀 함정에 당했다** — 같은 `?v=20260915a` 로 CSS 내용만 바꿔 서버엔 새 파일, 화면은 옛 격자였다(핀 `b`→`c` 로 해소). (c) `57OO`(알파벳 O)가 조용히 **57mm** 로 읽혔다 — SSOT 가 첫 숫자만 집어내므로 경고가 아니라 그럴듯한 오답이다. 순수 숫자가 아니면 읽어 낸 총합을 항상 보인다. 또 총괄 보고의 "`abc` 입력 시 경고" 는 **사실이 아니었다**(경고가 복합 표기일 때만 걸려 있었다) — 실화면으로 정정. 검증: 승격 트리 APP_OK · `pytest -n auto` 9,737 passed(잔여 3건은 내 커밋 없는 `origin/production` 에서도 동일 재현 — 알림톡 목록, 무관) · pre_push_smoke exit 0 · PR 검사 4종 green(perf-gate 포함) · 스테이징 실화면(390px, 다크/라이트) 확인. 회귀 테스트 신설: 다크 토큰화 되돌림 감지 2건, 초안 삭제 연타 1건·무음 실패 금지·0건이면 띠 사라짐. **미확인 1건**: 초안 삭제의 버튼·확인창은 측정 계정이 모바일 v2 셸 코호트가 아니라 실화면으로 못 봤다(서버 절반은 실화면 확인: 수동 CSRF 헤더 없이 `DELETE` → `{success:true}`, 목록 1→0) | wizard.js, foms-wizard.css, product-item.js, step2_products.html, wizard_shell.html, erp_order_js.html, foms-tokens.css, style-pro-max.css, layout_head.html, draft_resume.js, foms-draft-resume.css, 테스트 4개 | **운영 반영 완료** — PR #382 · production `49cb68247` |
| 2026-09-14 | **retention purge 가 운영에서 여전히 안 돌고 있었다 — 판정 착오 정정** — 09-12 승격 뒤 배포 메타(`serviceManifest.deploy.startCommand`·`fileServiceManifest`)에 새 명령이 들어간 것을 보고 'toml 이 정본이라 대시보드 필드가 옛 값이어도 무해'로 보고했는데 **틀렸다**. Railway 는 `railwayConfigFile` 이 걸려 있어도 **대시보드 `serviceInstance.startCommand` 를 실행한다**. 증거: 운영 cron 09-13 17:00 UTC 실행 로그가 `Starting Container` → `[cleanup_order_drafts] mode=execute scanned=0 deleted=0` → `Stopping Container` 뿐(purge 두 줄 없음), `order_mutation_receipts` 8,686행·7일 초과 7,053행·최초 행 2026-08-03 그대로. 조치: `tools/ops/railway_configure_cron_service.py` 로 운영·스테이징 두 FOMS-cron 의 startCommand 를 체인 명령으로 갱신하고 재배포(GraphQL `serviceInstanceUpdate`). 다음 실행(KST 02:00)에 만료 영수증 약 7,000행이 정리될 예정. 함께 확인된 정상 동작: 썸네일 제외 — 최근 2일 신규 `thumb_` 열람 행 0건. 감사 로그 4종은 보존기간 초과 0행(설계대로). 교훈: **배포 메타만 보고 반영 판정을 하지 않는다 — 실행 로그나 그 명령이 만드는 데이터 변화로 판정한다** | (저장소 변경 없음 — Railway 서비스 설정) | 운영·스테이징 cron 설정 갱신 |
| 2026-09-14 | **보낼 것을 다 보낸 집이 `보낼 수 없음` 으로 뜨던 것** — 사용자 제보(#5245 이지학). 운영 실측: 네이버 주문 2026091065471311 의 상품주문 5건 중 4건이 16:50 에 나갔고(링크 2317·2318·2320·2321, `productOrderStatus=DELIVERING`), 남은 1건(2319)은 구매자가 09-10 에 취소 확정한 건(`claimStatus=CANCEL_DONE`)이다 — **보낼 것은 다 보낸 상태**였다. 그런데 띠는 `보낼 상품주문 0건` 옆에 `보낼 수 없음 — 판매자센터에서 처리하세요` 라고 적었다. 0건인데 못 보낸다는 모순이고 할 일 없는 담당자를 판매자센터로 보내는 문장이다. 원인은 `_build_target` 이 `pending`(미발송 링크)만 보고 state 를 가른 것 — 2026-09-14 에 클레임 라인을 대상에서 빼면서 `sendable=0 · pending>0` 이라는 새 상태가 생겼고 그게 `blocked` 로 떨어졌다. 수정: **나간 건이 있고 남은 것이 전부 클레임이면 끝난 집**(`settled_by_claim`) — state 를 sent 로 두고 사유를 지우며, 화면 세 곳(워크벤치 띠·실측 대시보드 띠·폴링 `paintRow`)이 같은 문장으로 `취소·반품 N건은 빼고 보냈습니다` 를 덧붙인다. **나간 게 하나도 없는 집은 그대로 `보낼 수 없음`**(음성 대조군 테스트). 새 테스트 2건이 수정 전 코드에서 실제로 red 임을 확인했다. 검증: integrations 1,971 · domains+contracts+harness 8,050(5 skipped) · pre_push_smoke exit 0 · APP_OK | bulk_dispatch.py, naver_workbench.html, naver_dispatch_strip.html, naver_bulk_dispatch_button.html, test_naver_partial_claim_dispatch.py | **운영 반영 완료** — deploy `1d1c3fd39` · PR #380 · production `8feb05a47` |
| 2026-09-14 | **실측 대시보드 → 도면 전달 버튼(STATE-CONTROLS-04)** — 자가·지방 실측 보드의 미완료 행 5곳에 본공정 stage 를 MEASURE→DRAWING 으로 넘기는 버튼. 새 API 없이 기존 `POST /api/orders/<id>/quest/approve` 재사용. 노출·활성 잣대는 서버 한 곳(`drawing_transfer_cta`)에 모으고, `foms/api/quest.py` 의 권한 술어를 `quest_approve_authz.py` 로 추출해 화면과 라우트가 **같은 함수**를 부르게 했다(순수 리팩터, 옛 import 이름 유지). 리뷰 fix 반영: AS 접수/진행 중 행 숨김(AS 전이는 stage 를 안 건드려 MEASURE 로 남고 상차알림 버킷이 AS 행을 다시 담는다), 도면 **파일** 뷰어와 아이콘·톤 분리(오클릭이 stage 전이), 세션 만료 시 `res.json()` 이 로그인 HTML 을 물어 `Unexpected token '<'` 가 뜨던 것, 스택 gap 중복 4px. 원장 `docs/plans/2026-09-14-measure-to-drawing-transfer-ledger.md`. 검증: APP_OK · 게이트 130 passed · 신규 계약 52 passed · `node --check` exit 0 · pre_push_smoke exit 0 | quest_approve_authz.py, drawing_transfer_cta.py, measurement/dashboard.py, quest.py, status_select_options.html, self_measurement_dashboard.html, regional_dashboard.html, drawing-transfer-btn.js, complete-order-btn.css, test_measurement_drawing_transfer_button.py | deploy 대기 |
| 2026-09-14 | **집 화면 단건 발송도 부분 클레임 집에서 열린다(NVCLAIM-PARTIAL-02 후속)** — 벌크 띠는 남은 상품주문을 보내게 됐는데 집 pane 의 단건 `발송처리` 는 그대로 잠겨 같은 집을 두 화면이 다르게 판정했다(#5245 이지학). 원인은 pane 의 `locked = household_claimed or household_canceled` 한 값을 주문 만들기·발주확인·발송처리·취소 **버튼 4개가 함께** 읽는 구조다 — 클레임 축을 그냥 풀면 발송과 무관한 버튼 3개가 같이 열린다. 수정: 서버 그룹 뷰에 `dispatch_claim_locked`(**보낼 것이 하나도 없을 때만 참**)·`dispatch_claim_excluded_count` 를 내고(술어는 서버 `dispatch_order` 와 같은 `claim_blocked_rows` 한 벌), `dispatch_pending_count` 에서 클레임 라인을 빼 모달 재진술 건수를 서버가 실제 보낼 수와 맞추고, pane 은 `dispatch_locked` 로만 발송을 잠근다(나머지 3개는 `locked` 그대로). `이미 발송처리된 N건` 과 `클레임이 걸린 N건은 빼고 보냅니다` 를 갈라 썼다 — 합쳐 세면 나가지도 않은 건을 나갔다고 말한다. 계약 테스트 3건(형제 하나 클레임이면 열림+제외 문구 · 큐 밖 형제 클레임도 세어서 뺌 · **집 전부 클레임이면 여전히 잠김** 음성 대조군). 리뷰 P2 10건은 앞선 봉합에서 이미 반영돼 있었음을 코드로 확인(사유 문구 분리·paintRow 문장·지연 import·actor_name User.name·브리프 at 표기·권한 테스트 등). 검증: integrations 1,969 · domains+contracts+harness 7,998(5 skipped) · pre_push_smoke exit 0 · APP_OK. 자산 핀 20260914a→20260914b(테스트 7곳) | naver_ingest.py, naver_workbench_pane.html, naver_workbench.html, test_naver_workbench.py 외 테스트 7개, foms_failopen_inventory.json | **운영 반영 완료** — deploy `29ff30c13` · PR #378 · production `019187522` |
| 2026-09-14 | **erporder 첨부 미리보기 — 이미지 클릭에 전체화면 뷰어** — 사용자 제보: 주문 상세에서 도면 첨부를 미리보기로 열면 모달 안에서 작게 눌려 치수 글자를 읽을 수 없다. 원인은 두 겹이었다. ① 데스크톱 스테이지가 `max-height: min(60vh, 520px)` + `overflow: hidden` 이라 2000px 도면이 520px 로 축소된다(`foms-form-field.css:644-667`). ② 이미지 클릭이 모달 **안에서만** transform scale 을 토글해(`attachment-preview-zoom.js`) 커져도 스테이지 밖으로 못 나가고 잘린다. 큐 카드 갤러리·대시보드·모바일 읽기전용은 이미 전체화면 `GlobalImageViewer` 로 가는데 **데스크톱 erporder 상세만 예외**로 남아 있었다. 수정 — `fomsOpenAttachmentPreviewFullscreen` 진입점을 zoom 모듈에 신설하고 호출부 2곳(`erp-order-shared.js`·`erp-attachment-preview-open.js`)이 `fullscreen` resolver 를 넘긴다(클릭 시점 평가라 첨부 삭제·재로딩에도 최신 목록). 모달과 뷰어가 겹치는 동안의 생명주기 3건을 함께 처리: **Bootstrap 포커스 트랩**이 뷰어 버튼으로 간 포커스를 모달로 되끌어오는 것을 캡처 `focusin` 으로 끊고, **ESC** 는 캡처 `keydown` + `stopPropagation` 으로 뷰어만 닫고 모달을 남기며, 뷰어 `close()` 가 지우는 **배경 스크롤 잠금**을 모달이 살아 있으면 복구한다(ESC 는 동기, X·배드드롭은 MutationObserver). 좌우 화살표 목록은 갤러리와 같은 순서(실측·도면·시공·AS)로 세우고 잣대도 갤러리 카드와 같은 `file_type === 'image'` 로 맞췄다 — 화면에 문서로 보이는 첨부가 뷰어 사이에 끼지 않는다. 뷰어가 없거나 목록이 비면 기존 모달 안 2배 확대로 폴백(견적 미리보기 무변경). CEO 총괄 멀티 에이전트 10명(설계·워커 4·통합·리뷰 2·판정). **함정 2건**: ⑴ 리팩터로 `isSignedStorageUrl(a.view_url)` → `a && a.view_url` 이 되어 `test_file_url_lifecycle.py` 의 문자열 계약이 깨졌는데 이 테스트는 **pre_push_smoke 33타깃 밖**이라 로컬 게이트는 초록이었고 CI 에서만 red 였다(가드는 함수 첫 줄로 옮기고 표현식은 계약대로 복원). ⑵ 승격 헬퍼가 `INCOMPLETE: missing baseline deps=10` 을 냈지만 production 기준 워크트리에서 실제 cherry-pick 은 충돌 0 — 그 10개는 같은 파일의 자산 핀 줄만 겹친 타 세션 커밋이었다. 검증: 계약 테스트 11건 신설 · production 기준 전체 스위트 10,062 passed · pre_push_smoke 33타깃 806 passed exit 0 · APP_OK · **스테이징 실화면 확인**(주문 4388, 2000x1375 테스트 도면 업로드 후 정리): 모달 753x520 → 전체화면 1178x810 으로 치수 글자 가독, 화살표 3/6→4/6 이동 시 포커스가 뷰어에 남음, ESC 1회에 뷰어만 닫히고 모달·스크롤 잠금 복구, 콘솔 오류 0 | static/js/foms/attachment-preview-zoom.js, static/js/orders/erp-order-shared.js, static/js/foms/erp-attachment-preview-open.js, 템플릿 5곳 자산 핀 `20260914a`, tests/domains/test_attachment_preview_fullscreen.py, tests/domains/test_erp_order_shared_form_scripts.py | **운영 반영 완료** — deploy `bcc556435`·`aaf0889df` · PR #376 · production `c0592d343` |
| 2026-09-14 | **네이버 재결제 예정 표시 + 부분 클레임 집 발송** — ① 유령 주문 띠에 사람이 켜는 `재결제 예정` 상태 신설. 저장 `Order.structured_data['naver_repay_expected'] = {at, by, by_name, note}`(at 은 KST 표시 문자열), 켜지면 휴지통 잠금(`can_discard=False` + 사유 문장), 판정 순서 `살아 있는 결제 > 확정 전 > 재결제 표시`, 자동 해제 3경로(`run_reconcile` 승계·폐기, attach 라우트)+수동 해제(모집단 밖에서도 가능 — 클레임 거부로 모집단을 벗어나면 푸는 길이 사라지기 때문), 라우트 `POST /admin/naver-ingest/ghost/<id>/repay-expected`(ADMIN·MANAGER·STAFF), 감사 `NAVER_INGEST_GHOST_REPAY_EXPECTED`. ② 구매자 부분 클레임 집도 나머지 발송 — `claim_blocked_rows` SSOT 로 발송 대상·발주확인 선행에서 제외하고, **네이버 호출 직전에 payload id × 클레임 id 교집합을 다시 세어** 새면 보내지 않는다(불가역 경로 마지막 문). 집 전부 클레임이면 종전 문구·표식으로 거절, 집 단위 취소 표식·취소 실패 잔존·`close_now` 는 불변. 화면은 `클레임 N건은 빼고 보냅니다` + 제외 뒤 건수(워크벤치·실측 대시보드 양쪽). 새 라우트라 감사 라벨표·write guard·mutation policy 매니페스트 3곳 등재 동반, 자산 핀 20260911a→20260914a(테스트 7곳). CEO 총괄 멀티 에이전트 11 에이전트(설계1·워커5·통합1·리뷰2·판정1·봉합1), 판정 fix → P1 4건 봉합 후 green. 검증: 신규 계약 테스트 4파일(부분 클레임 발송 15건 — payload 직접 확인·음성 대조군 포함) · tests/services/integrations 1,968 · domains+contracts+harness 7,939 · 승격 트리 integrations+harness 2,439 · pre_push_smoke exit 0 · APP_OK · node --check exit 0. 미해결(별건): pane 단건 발송 버튼은 `household_claimed` 가 버튼 4개 판정을 함께 물어 열지 않았다 | ghost_orders.py, repay_reconcile.py, fulfillment.py, bulk_dispatch.py, naver_ingest.py, audit_message_display.py, naver_workbench.html, naver_workbench_pane.html, naver_dispatch_strip.html, naver_bulk_dispatch_button.html, naver-workbench.{js,css}, 매니페스트·인벤토리 5종, 테스트 12개 | **운영 반영 완료** — deploy `a79867179` · PR #375 · production `1b0091286` |
| 2026-09-13 | **실측·고객컨펌 승인 팀 정정 + 화면·서버 잣대 통일** — 운영 제보: 영업팀 강민경(`team=SALES`)이 라홈 주문의 **실측 완료**를 누르면 `현재 단계 승인 권한이 없는 팀입니다` 로 거부당한다. 운영 DB 읽기 전용 확인 결과 주문 #4596 의 실측 quest 가 `required_approvals=['CS']` 였고, `team_has_capability('SALES', ['CS'])` 가 403 을 냈다. **두 가지가 동시에 틀렸다.** ① 규칙이 업무와 달랐다 — 사용자 확인으로 실측 완료의 주관 팀은 **CS(자가실측)와 영업(방문실측) 둘 다**인데, `data/erp_quest_templates.json` 은 `["SALES"]`(CS 배제), 라홈 분기(`erp_policy_quests.py`)는 `["CS"]`(영업 배제)로 각각 한 팀을 잘라냈다. 실측 단계 물량 1위가 라홈(344건)이라 **가장 많은 주문을 영업이 못 눌렀다.** ② 화면과 서버가 다른 잣대를 썼다 — 서버는 quest 의 `required_approvals` 로 판정하는데 화면은 `can_edit_erp` 또는 도메인 권한만 봐서 `required_approvals` 를 **아예 안 봤다.** 그래서 서버가 거부할 버튼을 화면이 내밀었다(이 저장소가 여러 번 데인 "한 화면 두 말"). 수정 ⓐ MEASURE·CONFIRM 필수 승인 팀 → `["CS","SALES"]`(고객컨펌도 같은 규칙이라는 사용자 확인). ⓑ `resolve_required_approval_teams()` SSOT 신설 — 흩어져 있던 세 술어(생성 `foms/api/quest.py`·표시 `erp_quest_display.py`·감사 `audit_order_quests.py`)를 한 함수에 모았다. **저장값이 정책보다 좁으면 정책이 이긴다** → 이미 저장된 `['CS']` 84건도 마이그레이션 없이 오늘 바로 풀리고, quest 가 다시 저장될 때 자연히 정합해진다. ⓒ 라홈 분기는 `owner_team=CS` 만 남겼다 — 주관 팀(표시·배정)과 승인 가능 팀은 **다른 축**이다. ⓓ `can_assignee_approve` 에 팀 술어를 AND 하고 템플릿 2곳(모바일 상세·큐 카드)에서 `can_edit_erp or` 를 제거해 **화면이 보여주는 것 == 서버가 허용하는 것**으로 맞췄다(못 누를 사람에겐 버튼이 안 보인다). 계약 테스트 8건 신설 — 라홈/하우드 × SALES/CS/PRODUCTION/DRAWING 전수 대조(`_server_allows` vs `_screen_shows`), 운영 재현 케이스(#4596 SALES/MANAGER), 음성 대조군(PRODUCTION 은 양쪽 다 거부). 기존 테스트 2건은 **서버가 403 을 낼 조합을 통과로 못 박고 있어** 새 계약으로 고쳤다(CONSTRUCTION 사용자가 승인 버튼을 본다고 단언하던 것 포함). 검증: APP_OK · `tests/domains tests/services` 8,606 passed · pre_push_smoke **exit 0**. 게이트는 **격리 워크트리**에서 돌렸다 — 공유 워킹트리 첫 실행의 red(`test_pac_b1_partials_shared_html_exact_allowlist`)는 타 세션 미추적 파일 `channel_mark.html` 탓이고 내 변경 밖이다. 그 격리 실행이 실제 결함 1건도 잡았다: quest.py 의 `flag_modified` 가 3줄 밀려 writer 인벤토리 고정 lineno(505)가 틀어진 것 → 재생성해 508 로 갱신 | erp_quest_templates.json, erp_policy_quests.py, quest.py, erp_quest_display.py, audit_order_quests.py, order_detail_mobile_v2.html, erp_mobile_queue_card_v2.html, 테스트 3개, SPEC | **운영 반영 완료** — PR #373 · production `c140007c4` |
| 2026-09-13 | **리본 변경 줄을 리본 전체 폭으로** — 사용자 제보: 뒤로가기 칼럼이 생기면서 변경 줄이 안쪽 칼럼에 갇혀 왼쪽 여백이 크게 남았다. 변경 줄을 리본 그리드의 직계 자식으로 옮기고 `grid-column: 1 / -1` 로 전 칼럼을 쓰게 했다(행 간격은 그리드 gap 이 주므로 margin-top 제거 — 두 번 벌어지던 것도 함께 사라짐). 자산 본문이 바뀌었으므로 자물쇠 계약대로 핀 전수 범프 `20260913a`→`20260913b`(자식 @import·공유 화면 2곳·부모 번들 링크 2곳·핀 계약 테스트 4곳). 검증: 도면 관련 251 passed · pre_push_smoke 806 passed exit 0 · 로컬 실렌더 확인 | templates/drawing/partials/workbench_mobile_handoff.html, static/css/components/foms-drawing-mobile.css, static/css/foundation/foms-mobile-surfaces.css, templates/partials/shared/layout_head.html, templates/orders/share_view.html, templates/orders/share_bundle_view.html, 핀 계약 테스트 4곳 | deploy 대기 |
| 2026-09-13 | **도면 모바일 제보 3건 — 핀 미범프·숨은 스크롤 대상·뒤로가기 부재** — 운영 안드로이드 제보(주문 #5082). CEO 총괄 멀티 에이전트 8명. ① **리본 변경 줄에 CSS 가 안 붙음**: 전날 `foms-drawing-mobile.css` 본문에 새 규칙을 넣고 `?v=` 핀을 안 올렸다. `app_factory.py` 가 `?v=` 붙은 정적 자산에 `public, max-age=86400` 을 박으므로 이미 받아 둔 기기는 24시간 옛 CSS 를 쓴다(마크업만 새 것). **핵심 함정: 자식 @import 핀만 올리면 소용없다** — 캐시에 남은 옛 부모 번들(`foms-mobile-surfaces.css`)이 옛 자식 URL 을 물고 있어서, 부모 `<link>` 핀(layout_head 2곳)까지 같이 올려야 도달한다. 공유 화면 2곳·핀을 박은 계약 테스트 5곳 포함 전수 `20260913a`. ② **`변경 보기` 무반응**: 같은 캐시 문제로 옛 JS 가 돌던 것에 더해, `focusTimeline()` 이 첫 후보 `#dwOrderChangeFeed`(데스크톱 본문 `d-none d-lg-block` 안)로 `scrollIntoView` 하고 **무조건 return** 했다 — 폰에서는 숨은 요소라 아무 일도 안 일어나고 진짜 도착지인 모바일 말풍선에 영영 못 간다. 후보 순회로 바꾸고 `offsetParent` 기준 가시성 판정(`position:fixed` 예외)으로 숨은 후보는 건너뛴다. 도착지가 없으면 조용히 끝내지 않고 토스트. ③ **뒤로가기**: 상태 리본 그리드에 칼럼 하나를 더해 아이콘 링크(도면 작업실 목록)를 넣었다 — 새 블록 0개, 음수 마진으로 리본 높이 증가 0, 터치 44px. 셸의 `data-foms-shell-back`(history.back)은 쓰지 않는다(도면 페이저를 누르면 목록이 아니라 직전 도면으로 가고 딥링크 진입자는 앱 밖으로 나간다). ④ 재발 방지: 본문 sha256 과 핀을 한 쌍으로 묶은 **자물쇠 테스트**(본문만 고치고 핀을 두면 실패) + 핀 전수 스캔(정규식, 하드코딩 목록 아님). 검증: 신규 16건 + 기존 계약 13건 포함 도면 관련 262 passed · pre_push_smoke 806 passed exit 0 · APP_OK · node --check · 로컬 실렌더 3컷 확인 | static/css/foundation/foms-mobile-surfaces.css, static/css/components/foms-drawing-mobile.css, static/js/drawing/order-change-banner.js, templates/partials/shared/layout_head.html, templates/drawing/partials/workbench_detail_body.html, templates/drawing/partials/workbench_mobile_handoff.html, templates/orders/share_view.html, templates/orders/share_bundle_view.html, tests/domains/test_drawing_mobile_asset_pin_freshness.py, tests/domains/test_drawing_order_change_focus_target.py, tests/domains/test_drawing_mobile_back_to_workbench.py, 핀 계약 테스트 5곳 | deploy 대기 |
| 2026-09-13 | **모바일 이미지 미리보기 2건(사용자 제보)** — ① **실측 탭 주문카드: 사진을 넘기다 X 를 누르면 두 번 눌러야 닫힘**. 재현(Chromium 터치 에뮬레이션, 실제 `/erp/measurement` 페이지)으로 첫 탭에 `touchstart`/`touchend` 는 오는데 **`click` 이 아예 합성되지 않는 것**을 확인했고, 스와이프 종료 후 지연을 0~900ms 로 훑어 **약 300~500ms 창**에서만 발생함을 특정했다. 원인은 GlobalImageViewer 의 넘김 분기가 `touchmove` 를 소비하지 않는 것 — `#global-viewer-stage { touch-action: none }` 만으로는 브라우저의 스크롤/플링 제스처 인식을 못 막고, 플링 뒤 탭은 '플링 취소 탭'으로 분류돼 click 이 억제된다. 닫기·화살표는 click 으로만 동작하므로 첫 탭이 통째로 사라진다. 넘김 분기에서 `e.preventDefault()` 로 수정(미러 SSOT `static/js/runtime/layout-scripts-core.js` + **실서빙 인라인 사본** 양쪽 — 인라인만 빠지면 조용히 무효). 수정 후 같은 창 전 구간 1탭 닫힘, 스와이프·핀치·더블탭 줌 회귀 없음. ② **도면 탭 주문카드: 도면이 여러 장이어도 한 장씩만 조회**. `drawing-handoff.js` 가 `GlobalImageViewer.open([file], 0)` 으로 선택 1장만 넘기고 있었다(목록은 서버 페이저로만 이동). `_build_handoff_viewer_files()` 로 그 주문의 도면 전체를 `mobile_handoff_viewer_files` → `data-foms-drawing-handoff-files` 로 실어 보내고, JS 는 key 매칭으로 시작 인덱스를 잡는다. 순서를 `mobile_handoff_files` 와 같게 둬 뷰어 카운터(i / N)가 페이저 '도면 N / M' 과 일치한다. 실제 페이지에서 1/3→3/3 좌우 이동·딥링크 시작 인덱스(3/3) 확인. 검증: 신규 회귀 가드 3건(뷰어 목록 전량·JS 단일배열 복귀 금지·스와이프 preventDefault 양쪽 사본) — 수정 전 3건 모두 red 확인 · 관련 139 passed · pre_push_smoke 서브셋 739 passed · tests/domains 신규 실패 0(기준선과 동일) · APP_OK | templates/partials/shared/layout_scripts.html, static/js/runtime/layout-scripts-core.js, static/js/foms/drawing-handoff.js, foms/web/drawing/workbench.py, templates/drawing/partials/workbench_mobile_handoff.html, templates/drawing/partials/workbench_detail_body.html, tests/domains/test_tablet_shell_t0_contract.py, tests/domains/test_drawing_workbench_mobile.py, tests/domains/test_drawing_room_push_workbench_ui.py | **운영 반영 완료** — deploy `064151064` · PR #369 · production `2598dadcf` |
| 2026-09-12 | **도면 주문 변경 배너 제거 — 신호는 한 줄, 값은 한 곳** — 사용자 제보: 모바일 도면 화면 상단의 분홍 '주문 내용 변경' 배너가 필요 없다. 아래 타임라인이 이미 같은 변경을 항목별로 전량 보여 주기 때문이다. 멀티 에이전트(CEO 총괄 8명)로 페르소나·3안 설계 후 '차례 카드' 안 채택. ① **모바일**: 배너 블록 삭제 + 상태 리본 안 한 줄(`미확인 · 주문이 바뀌었습니다 · 스펙, 제품명 2줄 · [변경 보기]`). 리본은 목록/상세 분기 **바깥**이라 도면이 여러 장일 때 열리는 목록 뷰(타임라인 없음)의 신호 구멍이 새 블록 없이 메워진다 — 배너만 지웠으면 그 화면이 침묵했다. ② **확인(ack) 버튼은 값 바로 아래 하나**(모바일 타임라인 말풍선 / PC 변경 이력 카드). 옛 JS 는 배너 조상에서 `data-ack-url` 을 읽었기 때문에(`ackBanner` 의 `closest('#dwOrderChangeBanner, .dw-order-change-banner')`) 배너를 지우면 버튼이 조용히 죽는다 — 주소를 버튼 자체에 싣고 탐색을 `closest('[data-ack-url]')` 로 옮겼다. 성공 뒤 동작도 '배너 제거' 에서 '버튼 줄 제거 + 리본을 확인됨 한 줄로 교체' 로 바꿨다. ③ **권한 정합**: ack 는 도면 작업 참여자·관리자만 가능한데(API 게이트) 옛 배너는 영업 시점에도 버튼을 그려 403 을 만들었다. 이제 권한이 없으면 버튼을 렌더하지 않고 `도면팀 확인 대기` 상태만 보인다. ④ **PC 화면도 같은 규칙**: 배너 삭제, 변경 이력 머리글에 이름·줄 수, 확인 버튼은 값 표 아래 카드 안. ⑤ ack 응답에 `acked_by_name`·`acked_at` 추가(커밋 전에 읽는다) — 확인 직후 화면이 '누가 언제'를 말할 수 있게. ⑥ 리본 보조 문구의 하드코딩 상수 `주문 단위 상태 1개`(실제 건수와 무관한 거짓 숫자) 제거. 검증: 신규 계약 13건(값 1회·버튼 1개·목록 뷰 신호·무권한 미렌더·확인 후 누가 언제·API 필드) · 도면 도메인 563 passed · 시각/구조 63 passed · pre_push_smoke exit 0 · APP_OK · `node --check` · 실제 렌더 스크린샷 4컷(모바일 3 + PC 1) 확인 | templates/drawing/partials/workbench_mobile_handoff.html, templates/drawing/partials/workbench_detail_body.html, foms/web/drawing/workbench.py, foms/api/drawing/erp_orders_revision.py, static/css/components/foms-drawing-mobile.css, static/js/drawing/order-change-banner.js, tests/domains/test_drawing_mobile_order_change_line.py | **운영 반영 완료** — deploy `7e8e787a7` · PR #366 · production `0d67f76f3` |
| 2026-09-11 | **모바일 ERP 3건 + perf-gate 정비(운영 PR #346·#354·#355·#358·#360·#361)** — ① **실측 완료 버튼 먹통**: `location.href = pathname+search+'#hash'` 는 해시만 다른(또는 같은) URL 이라 브라우저가 문서를 다시 안 읽는다. 승인은 서버에서 끝났는데 화면만 멈추고 버튼이 disabled 로 굳어 "안 먹는다"로 보였다. `replaceState`+`reload()` 로 교체. 단계별 승인 문구를 서버가 만들게 하고(`quest_approve_cta` — 다음 단계 판정은 전이 서비스 `_STAGE_ADVANCE` 에 위임해 문구가 약속하는 결과와 실제 규칙이 갈라지지 않게), 넘기기 전 확인창(고객명·주문번호). 고객컨펌 카드가 409(COMMAND_REQUIRED)로 거부되는 승인 버튼을 띄우던 막다른 길 제거. ② **아이폰 견적서 사진 저장**: iOS 는 `data:` URL 이미지를 길게 눌러도 사진 앱에 못 넣는다 — 공유 시트를 거쳐야 한다. 모달 '사진에 저장' 버튼 탭이 **새 사용자 제스처**라는 점이 핵심(비동기 캡처 뒤 `share()` 는 활성화를 잃는다). 그 버튼 글자가 아이폰에서 중간 줄바꿈되던 것(`flex-shrink:1`+`white-space:normal`)도 수정. ③ **수집 뱃지가 화면을 밀던 것**: 컨텍스트 프로세서라 모든 렌더가 계산했는데 뱃지를 그리는 템플릿은 셋뿐이고 프래그먼트(=탭 전환)는 레이아웃을 안 싣고도 552ms 를 계산하고 버렸다. 지연 평가(`LazyBadgeCount`) + 숫자를 렌더 뒤 XHR 로. **탭 전환 590→35ms · 첫 화면 423→47ms**(스테이징 콜드). 세는 정의는 안 건드렸다 — SQL COUNT 로 옮기는 길은 `_workbench_group_count` docstring 이 금지(nav 67·탭 45 불일치 전례). ④ **perf-gate 가 실사용자를 재게**: 봇이 `foms_scr`·`foms_ptr` 을 안 실어 서버가 전 표면을 렌더하는 합성 최악값을 재고 있었다(20ms·16% 과대). 페르소나 쿠키 + 그로 생긴 coarse 커버리지 공백을 태블릿 패스로 닫음(coarse 전용 표면 실측: 시공 +64%·완료 +42%·AS +40%) + TTFB 예산 CI 재시드 자리(artifact 만, 봇이 예산을 못 커밋하게). **측정 함정 3종(다음 사람이 같은 데 빠지지 않게)**: ⑴ 쿠키는 jar 가 아니라 `Cookie` 헤더에 합쳐야 전송된다 — requests 는 명시 헤더가 있으면 jar 를 병합 안 한다(jar 로 심었더니 wire 가 1바이트도 안 줄어 들통) ⑵ `(파이프) tail; echo $?` 는 tail 의 종료 코드다 — 게이트 통과를 거짓으로 읽는다 ⑶ `with phase()` 안 호출의 **인자 식도 그 구간에 계상**된다 — 구간 이름이 거짓말을 한다. 그리고 `_work_groups` 구간 계측으로 '고치려던 세 단계가 0~1ms' 임을 확인해 **안 고치기로 결정**했다 — 계측이 없었으면 0ms 를 얻으려고 코드를 뜯고 캡과의 짝 문제까지 떠안았을 것이다 | estimate-preview.js, erp-quest-approve.js, foms-nav-triage-badge.js, context_processors.py, quest_approve_cta.py, staging_perf_gate.py, naver_ingest.py 외 | 3555ccd8e·8ca33225c·4ad0a27b5·11eb6c6e6·6b1bb31ed·635dc99f1 |
| 2026-09-11 | **retention purge 미실행 + 썸네일 열람 기록 제외** — 사용자 질문(네이버 수집 이력이 쌓여도 되나)에서 시작한 운영 DB 실측. 수집 이력 `external_order_links` 는 문제 없음(2,386행·13MB·TOAST 10MB=스냅샷 78%·칩 집계 EXPLAIN 1.97ms·평상시 하루 20~50건, 9/1 백필 1,834건 제외). 삭제 금지 근거도 재확인 — 멱등 원장이라 지우면 백필(MAX_RANGE 90일)이 중복 주문을 만든다. **진짜 결함은 purge 미실행**: `tools/ops/purge_audit_logs.py`·보존기간(security 3년/access 2년/notification 1년/delivery 3년)·`railway-cron-receipt-purge.toml` 이 2026-08 에 다 만들어졌는데 그 config 를 읽을 서비스가 두 프로젝트 어디에도 없었다(FOMS-PRODUCTION 서비스 실측: SIDEFX·Postgres·WORKER·Redis·web·FOMS-cron 뿐, cron 명령은 `cleanup_order_drafts.py --execute` 하나). 증거=`order_mutation_receipts` 보존 7일 초과 6,329행. 운영 DSN 읽기전용 dry-run 으로 확정(감사 4종 scanned=0, 영수증 scanned=6329). 수정 ① 고아 config 삭제 + `railway-cron.toml` startCommand 에 purge 2종 체이닝(양쪽 FOMS-cron 이 같은 toml 을 읽는 것 GraphQL 로 실측), GraphQL 사본·verify 스크립트 needle 동기화, 계약 테스트 6건(명령 존재·`--apply` 존재·세 곳 일치·고아 config 부활 금지). ② `access_logs` 하루 732건의 54%가 썸네일(FILE_VIEW 24,900 중 13,351, 사용자·분 묶음당 5.3행) — dedupe 키가 `(주체, storage_key, action)` 이라 갤러리 한 화면의 서로 다른 thumb key 수십 개가 통과했다. `record_file_access` 진입부에서 FILE_VIEW + basename `thumb_` 제외(중간 경로 `thumb_dir/` 는 제외 대상 아님 — 원본 기록 유실 방지), download/presigned 는 전량 기록 유지. 검증: 대상 테스트 81 passed · pre_push_smoke 795 passed exit 0 · APP_OK. **운영 반영 완료** — PR #365 검사 4종 pass · production `6ceb7795e` · FOMS-cron 재배포 `b3dedb29` SUCCESS(실행 명세 startCommand 에 purge 2종 확인). 첫 실행은 KST 02:00, 잔여는 그 로그 확인뿐 | railway-cron.toml, railway-cron-receipt-purge.toml(삭제), tools/ops/railway_configure_cron_service.py, scripts/ops/verify_foms_cron_prod.ps1, foms/services/audit_writer.py, tests/contracts/runtime/test_cron_purge_wiring.py, tests/domains/test_file_access_log.py, tests/contracts/runtime/test_ptc_physical_exactness.py | deploy `79528bb6d`·`ba571aa47` |
| 2026-09-11 | **관리자 복귀 버튼이 검증 실패 403 을 내던 문제** — 사용자 제보: 운영에서 '원래 관리자로 돌아가기' 를 누르면 `{"error":"요청 검증에 실패했습니다..."}` JSON 이 화면에 떴다. 근본 원인은 write-guard 의 CSRF seed 가 `session['user_id']` 에서 파생된다는 점(`67a93a067`/`641be1497` 의 쿠키 경합 수정): 계정 전환(switch-user)·복귀(switch-back)는 `user_id` 자체를 바꾸므로, 전환 중 렌더된 탭의 토큰 seed 가 전환 계정에 묶여 다른 탭이 먼저 복귀한 순간 전부 `invalid_csrf_token` 403 이 된다(로컬 재현: 탭B 403 blocked). 수정 — seed 기준을 전환에도 변하지 않는 **실제 로그인 주체**(`impersonating_from or user_id`)로 옮겨(`_csrf_principal_id()`/`_derive_seed_for()`) 전환 전·중·후 토큰이 모두 유효하게 했고, 배포 전 렌더된 구 토큰(전환 계정 seed)은 전환 중에 한해 인정하는 호환 경로를 남겨 열려 있는 탭을 깨지 않는다. 부수 효과: 묵은 탭의 복귀는 검증 실패 JSON 대신 '전환된 상태가 아닙니다' 안내로 끝난다. 검증: 회귀 테스트 3건 추가(전환 중 토큰 통과·묵은 탭 403 아님·구 토큰 호환) · tests/domains -k "auth or csrf or guard" 412 passed · pre_push_smoke exit 0 · APP_OK · PR #357 체크 4종 pass | request_write_guard.py, test_auth_impersonation.py | **운영 반영 완료** — deploy `b458becb9` · PR #357 · production `c2b7672e` |
| 2026-09-11 | **네이버 취소·반품 부분 선택(NVCLAIM-PARTIAL-01, 게이트 `FOMS_NAVER_PARTIAL_CLAIM_ENABLED`)** — 운영 #2354: 본품·추가구성 집에서 1건만 취소할 수 없었고 부분 취소 뒤 남은 라인 발송이 집 단위로 막혔다. ① 모달에 상품주문 체크박스 목록 + 서버 `product_order_ids`(None=집 전체 / []=400 / 목록=그 라인만), 본품 선택 시 남은 추가구성 자동 동반 + 서버 `plan_claim_scope` 재검사(빠지면 0건 전송) ② 성공 표식 `cancel_scope ∈ {partial, household}` — partial 행만 발주확인·발송 대상에서 빼고 household·옛 표식·취소 실패 잔존은 집 전체 차단 유지(결정 5) ③ 취소 축에도 FAQ 3880 범위 규격 ④ 취소 버튼은 형제 클레임으로 집을 잠그지 않는다(결정 8) ⑤ 벌크 발송 pre-check `_blocking_reason` 도 `_cancel_guard` 거울(결정 9), 목록 줄·pane 이 어느 형제로 열어도 같은 버튼(M-4). 검증: partial_claim 50 · 관련 360 · integrations+domains 9141 · pre_push_smoke 794 green. 스테이징 §8 ①②③·사용자 #2354 화면 확인 대기 | foms/services/integrations/naver_commerce/{fulfillment,bulk_dispatch,queue}.py, foms/services/jobs/tasks.py, foms/services/feature_flags.py, foms/web/admin/naver_ingest.py, templates/admin/partials/naver_workbench_pane.html, templates/admin/naver_workbench.html, static/js/admin/naver-workbench.js, static/css/admin/naver-workbench.css, tests/services/integrations/test_naver_partial_claim*.py, docs/specs/2026-09-11-naver-partial-claim_SPEC.md, docs/harness/policy/DECISIONS.md | deploy 대기 |
| 2026-09-04 | **정리 계획 카드 정직화 + 취소 처리 정책 동기화** — 사용자 제보: 워크벤치 정리 계획 카드 2번 칸이 `선택 필요` 라고 적어 놓고 취소 처리 갈래는 `disabled` 라 고를 것이 하나뿐이다. CEO 총괄 멀티 에이전트 조사 결과 **근본 원인은 문구가 아니라 정책 드리프트**였다: 2026-09-02 사용자 결정 커밋 `549a801fb` 가 공용 상수 `DISCARDABLE_STATUSES` 의 뜻을 "못 접는 목록"에서 "**사유 없이** 접히는 목록"으로 바꿨는데, 그 커밋이 `ghost_orders.py` 만 고치고 `repay_reconcile.discard_gate` 는 손대지 않아 같은 상수가 두 화면에서 다른 뜻으로 읽혔다 (그 함수 docstring 은 "유령 주문 띠와 같은 상수를 쓴다 — 두 화면이 서로 다른 단계를 열어 주면 담당자가 어느 쪽을 믿어야 할지 알 수 없다"고 선언해 놓고 있었다). 수정 ① 잠금 축을 **단계 → 옛 결제 확정 여부**로 옮겼다(`discard_policy(status, claim_code)`; 붙은 네이버 집이 없거나 `all_done` 일 때만 열린다). 단계는 잠금이 아니라 `needs_reason` 축으로 분리 — 접수 이후 단계는 **관리자 + 사유 한 줄**로 접힌다(유령 띠와 동일). ② `run_gate(claim_code)` 신설 — 확정 전(`all_pending`/`all_mixed`)에는 승계까지 갈래 무관 차단. 화면 i 칸이 예전부터 "확정된 뒤에 정리하세요"라고 적어 놓고도 버튼이 눌리던 자리다. 라우트에서 한 번 더 검사한다(유령 띠 라우트와 같은 규율). ③ 후보 표 버튼 순서·강조를 `recommended_relation`(신규 SSOT `RELATION_BY_CLAIM_CODE`)에 연동 — 표는 "재결제 신호"라 적는데 버튼은 추가결제가 먼저·강조색이라 눈이 가는 쪽이 권고와 반대였고, 관계 오선택은 `deposit_guidance` 의 바꾸기/더하기로 갈려 **고객 청구액**까지 흘러간다. ④ 2번 칸 태그·제목이 실제 갈래 수를 따라간다(`골라 주세요` / `자동 결정`), 확인창 첫 줄이 관계를 다시 말한다, 사유 칸은 갈래 라벨 밖(중첩 label 회피). 핀 `20260904a`→`b` + 핀 계약 4곳 동반. 신규 mutation 라우트가 아니라 기존 `/reconcile` 파라미터 확장이라 manifest·감사 라벨 무변경. 검증: tests/services/integrations 1648 passed(신규 케이스 9건 — 사유 없음 400·사유 있으면 200+감사 원문·MANAGER 403 축·확정 전 차단·버튼 순서) · APP_OK. **미해결**: pre_push_smoke 서브셋 1 failed 는 이전 세션이 남긴 미추적 `tests/visual/test_tmp_as_sort_smoke.py` 가 visual 등재 계약에 걸리는 것(내 변경 밖, 남의 파일이라 안 지움) | order_candidates.py, repay_reconcile.py, naver_ingest.py, naver_workbench_pane.html, naver_workbench.html, naver-workbench.js, naver-workbench.css, 테스트 7개 | **운영 반영 완료** — PR #294 · production `29a6e72bd`(승격 트리 1784 passed·APP_OK, 검사 4개 SUCCESS). 별건으로 문서 계보 157건 해소: PR #295 · production `fc521573e` — 코드만 승격해 온 탓에 AI_STATUS·AI_CHANGELOG 가 deploy 보다 157커밋 뒤처져 매 승격마다 충돌하던 것을 deploy 정본으로 통째 맞췄다(AI_CHANGELOG 고유 줄 0건, AI_STATUS 고유 12줄은 전부 옛 판; 훅 단위 병합은 내용 소실 전례가 있어 안 씀). 함께 승격: 임시폴더 하위 `Remove-Item` 재귀 가드 완화 — 백업 가지에만 남아 있던 것 회수 |
| 2026-09-04 | **네이버 워크벤치·클레임 결함 5건 운영 반영** — 사용자 제보를 CEO 총괄 멀티 에이전트로 나눠 처리했다. ① **상품주문 표 금액 합계 줄**: 워크벤치 상세의 상품주문 표가 건별 금액만 보여 집 전체 금액을 사람이 암산하고 있었다(production `d5d9173a9`). ② **ERP 도크 예약금 안내 바닥값**: 추가결제 안내가 "넣을 금액"을 네이버 결제액만으로 계산해, 이미 받아 둔 예약금이 화면에서 증발했다. 바닥값을 `원래 예약금 + 네이버 결제액` 으로 바로잡음(`19e9ac0a6`). ③ **취소·반품 배지 색**: 클레임이 끝난 건이 회색으로 떨어져 "처리 끝났으니 볼 것 없음"처럼 읽혔다. 끝난 건까지 빨강으로 두고 **거부(rejected)만 회색** — 거부는 주문이 살아 있다는 뜻이다(`bb815f2e0`). ④ **'붙이면 대상이 되는 집' 띠**: `bulk_dispatch.find_unlinked_matches` 가 집 형제 전원이 이미 종결된 집까지 후보로 권했다. `claim_watch.TERMINAL_ORDER_STATUSES` 전원 일치일 때만 제외하고, **모르는 값(키 없음·빈 값·처음 보는 코드)은 종결로 치지 않는다**. 카운터·문구는 취소·반품 축과 분리(`unlinked_excluded_closed`, `b828595d8`). ⑤ **고객 선요청 취소를 '취소 실패'로 부르던 결함 + 승인 뒤 빨간 띠 잔류**: `fulfillment.cancel_order` 가 실패를 적기 전 읽기 1회로 재분류하고(`_buyer_claim_superseded` — 고객 취소 클레임이 열려 있으면 `last_error` 가 아니라 `cancel.superseded_*` 에 적는다. **재조회 실패 = 모름 = 그대로 빨강**), `approve_cancel`/`approve_return` 성공은 **같은 축 실패만** 강등한다(`_clear_settled_failures`, `CLAIM_SETTLED_CLEARS` — 발주확인·반품 등 축 밖은 안 건드림). `naver-workbench.js` 의 `watchFulfillment` 호출 7곳은 전부 `err_at` 를 넘긴다(폴백 없음). 운영에 이미 남은 옛 빨간 띠는 소급해 지우지 않는다 — 화면의 `확인함` 1회로 닫는다(운영 JSONB 백필 금지, CEO 판정, `b130c17df`). 검증: pre_push_smoke exit 0 · CI 전 워크플로 green · 승격 PR #275·#281·#286·#291·#293 머지 | bulk_dispatch.py, fulfillment.py, claim_watch.py, naver-workbench.js, naver_workbench.html, naver_workbench_pane.html, erp-naver-dock.js, 테스트 5개 | production `d5d9173a9`·`19e9ac0a6`·`bb815f2e0`·`b828595d8`·`b130c17df` |
| 2026-09-04 | **ERP 본공정 드롭다운이 AS 접수를 안 보여주던 것 + AS 가 stage 를 오염시키던 구멍 2곳** — 사용자 제보 "AS 접수했는데 ERP 화면이 이전 상태로 계속 저장된다". CEO 총괄 멀티 에이전트(9에이전트) 조사로 현상이 **3겹**임을 확정: ① 드롭다운에서 AS접수를 고르고 저장하면 PUT 이 아예 안 나가고 AS 접수 모달이 뜬다(erpSaveStructured 전이 가드 → `return {success:false}`), 모달을 닫으면 `hidden.bs.modal` 핸들러가 값을 원래 본공정으로 되돌린다 — 이것이 "되돌아감"의 정체다. ② 모달을 완료해도 `register_as_cycle` 은 status·as_axis_status·as_lifecycle 만 바꾸고 workflow.stage 는 안 건드린다(STATE-AS-01). ③ 폼이 stage 를 보내도 `_pin_form_stage_to_server` 가 서버 현재값으로 되돌린다(STATE-FORM-01). 즉 **어떤 경로로도 stage 를 AS 로 만들 수 없었다.** 그런데 그 "두 축 분리"는 한쪽 문만 잠근 미봉이었다 — `LOGISTICS_STATUS_PRESERVE_WORKFLOW_STAGE` 에 AS 코드가 빠져 있어 status 쓰기 경로 2곳(`update_order_status`·`update_order_field` field=status)은 지금도 stage 를 AS_* 로 덮는다. 운영 실측: `STAGE_CHANGED{to:AS_*}` 61건(from 은 RECEIVED 36·MEASURE 10·CONSTRUCTION 8 등 깨끗한 본공정), stage 가 AS_* 인 미완료 주문 477건, 그중 `status=AS_COMPLETED`인데 `stage=AS_RECEIVED`로 굳어 도면·생산·시공 큐에서 빠진 것이 62건. AS 완료 전이는 stage 를 되돌리지 않아 한 번 덮이면 고착한다. **stage 를 실제로 AS 로 쓰는 안은 접었다** — 고착·되돌림 근거 부재(AS 직전 본공정 단계는 workflow·as_lifecycle·이벤트·감사 어디에도 없다)·레거시 477건 소급 불가. 수정 ① 표시: AS 진행 중이면 select 맨 앞에 `value=''`·disabled 표시 옵션(`AS 접수 중`/`AS 완료`) 삽입, 본공정은 title 툴팁(스테이징 실측 select 163px·가용 129px 라 `· 본공정: X`(195px)는 잘린다). 저장 payload 에는 안 실린다 — 폼 조립부가 `formStage || prevStage`. ② 입력 차단: PC·모바일 두 템플릿에서 AS접수/AS완료/AS처리 옵션 제거. ③ 서버 봉합: `AS_OVERLAY_PRESERVE_WORKFLOW_STAGE` 신설(기존 LOGISTICS 상수는 `state_axes_audit._is_normal_overlay_divergence` 감사 분류에도 쓰여 안 건드림), `STAGE_CHANGED` payload 에 `stage_write_skipped`. ④ AS 접수 직후 캐시 stage 위조 줄 삭제(서버가 pin 으로 폐기하는 데드 코드이자 다음 저장의 prevStage 오염원). **곁가지**: 단건 강제 변경이 `order.status = to_code` 로 AS overlay 를 조용히 지우던 것 — 벌크에만 있던 가드를 단건에 복사했다가 되돌렸다. 감리#3 정책상 AS→메인 복귀는 허용이고(`test_override_from_as_to_main_allowed`) 굳은 62건을 사람이 되돌리는 유일한 경로이기도 해서, 막지 않고 응답 `data.as_overlay_cleared`+감사 문구로 드러내는 쪽으로 바꿨다. **미해결**: 레거시 stage 오염 477건(큐 이탈 62건). 복원 근거는 STAGE 이벤트 1건뿐이고(워크플로 추정 38건은 직접 세어보니 틀렸다) 9건은 workflow.history 추측, 52건은 단서 없음 — 사용자 결정으로 확실한 1건(#3110)만 정정하고 나머지는 추측으로 바꾸지 않았다. 검증: 스테이징 실브라우저(#4388 AS접수 → `AS 접수 중`+title, **저장 후 stage=RECEIVED 유지**, PUT 200 / #3925 AS완료 → `AS 완료` / #4380 비AS 대조군 → 표시 옵션 없음·MEASURE 유지 / 모바일 390px 동일 / 콘솔 오류 0), 승격 트리 tests/domains+contracts 6712 passed·5 skipped, pre_push_smoke exit 0, PR 검사 4/4 green. 계약 테스트 17종 신규. 자산 핀 20260901m→20260904c | status_constants.py, api/orders/{status,stage_override}.py, erp_order_tab.html, erp_order_tab_mobile.html, erp_order_js.html, erp-order-shared.js, tests/domains/test_as_overlay_stage_preserve.py, test_erp_order_shared_form_scripts.py, foms_{state,order_mutation}_writer_inventory.json | deploy `308366961`·`f850ba6b6`·`f85665f61` · production `5eb72ce40`(PR #292) |
| 2026-09-03 | **AS 상태가 화면에서 증발하던 결함 2건 — 축 투영 게이트 이탈 + 열린 AS 건 status 봉인** — 사용자 제보 2건을 CEO 총괄 멀티 에이전트(각 9에이전트)로 조사, 운영 실데이터로 원인 확정. **① AS완료인데 미완료 탭**: AS 완료 커맨드(`_reproject_and_sync`)는 `order.status`·`as_completed_date` 를 게이트 밖에서 무조건 쓰는데 `as_axis_status` 투영만 `sync_erp_flat_columns` 의 `is_erp_order` 게이트 안에 있었다 → 비ERP AS 주문은 축이 옛 `RECEIVED` 로 동결. 탭 술어는 축을·뱃지는 status 를 봐서 초록 AS완료 뱃지를 단 채 미완료 탭에 남았다. 운영 정확히 3건(#1315·#1119·#1706), 전부 `is_erp_order=False`. 같은 분·같은 `AS_COMPLETE` 를 탄 ERP 주문 2건은 정상 → 갈림 변수가 `is_erp_order` 하나임을 대조군으로 확정. 자가 회복 경로도 없다(부팅 백필은 `is_erp_order=True` 만, models 훅은 `before_insert` 전용). 수정: `sync_as_axis_column()` 분리해 게이트 앞에서 호출, "값 없으면 안 지움" 규약은 유지(2026-08-14 사고 방어선 무변경), blast radius 운영 실측 0행. **② AS 접수했는데 지방 AS 섹션에서 사라짐**: 지방 보드 체크리스트 자동 승격(`checkAllCompleted`)이 `status=SCHEDULED` 를 쏘면 서버가 열린 AS 건을 안 보고 덮었다(`SCHEDULED` 는 물류 보드 코드라 stage-override·canonical 엔진 둘 다 건너뛰고 `setattr` 직행). 지방 AS 섹션 술어와 ERP `AS: 접수` 뱃지·재접수 버튼이 모두 그 컬럼을 읽어 두 화면에서 동시 증발, 재접수는 409(열린 cycle)라 화면에서 복구 불가. 감사 원장이 인과를 그대로 담았다 — 체크박스 저장 +0.18~0.22초 뒤 `ORDER_STATUS_CHANGED: AS_RECEIVED -> SCHEDULED`. **회귀 커밋은 없다(잠복)** — 덮어쓰기 코드는 `e23fe359a` 이전부터, AS 행이 체크박스 섹션에 들어간 건 `0b4ab4546`(07-13)부터. #4816 은 08-31 접수 후 09-03 06:27 까지 정상이었다. 수정 A(서버): `as_overlay_outranks_status_write()` 신설 — `legacy_status_projection` 의 우선순위(DELETED>ON_HOLD>AS_*>logistics>main)를 쓰기 경로에도 적용, `field_update`·`update_order_status` 대칭. **403 아닌 무시**(실패 배너는 `.alert` 자동닫힘에 지워져 무음 실패가 되고 체크리스트 저장은 정상 업무). 의도적 본공정 전이·보류·삭제·AS 코드는 통과(음성 대조군 4종으로 잠금). 수정 B(클라): `checkAllCompleted` 가 AS 행 표식 3종 제외 — 행 클래스가 섹션마다 달라(`as-order-row` vs `regional-as-shipping-row`) 기존 가드가 사고 난 쪽에 안 걸렸다. **미해결로 남긴 것**: ERP 본공정 드롭다운이 AS 접수 후에도 MEASURE 로 남는 것은 버그가 아니라 STATE-AS-01 설계(`8829079a9`)다 — 정상 동작 중인 수도권 AS 47건도 전부 그렇다. 검증: 승격 트리(`origin/production` 기준) `tests/domains`+`tests/contracts` 6695 passed·5 skipped, pre_push_smoke exit 0, APP_OK, deploy·PR 검사 각 4/4 green, cherry-pick 충돌 0. 계약 테스트 13종 신규. 운영 데이터 5건 복구 완료(백업 `C:/tmp/as_recovery_backup_20260903.json`, 사후 잔여 결함 0) | erp_sync_columns.py, state_axes.py, api/orders/{field_update,status}.py, regional_dashboard.html, tests/domains/{test_as_axis_projection,test_as_overlay_status_write_guard}.py, foms_namespace_surface_tests.py, foms_{state,order_mutation}_writer_inventory.json | deploy `370e2f689`·`fe112d765` · production `ac23a6c16`(PR #288) |
| 2026-09-03 | **회계팀 권한 정리 — CS 동등 업무 권한 + 정산은 회계팀·관리자 전용** — 어제 신설한 회계팀(ACCOUNTING)으로 팀을 옮긴 사용자가 ERP 주문 수정에서 403 을 맞았다. 원인은 `can_edit_erp` 를 비롯한 팀 게이트 8곳이 `team in ("CS","SALES")` 처럼 **문자열 집합**을 직접 보는 것 — 새 팀은 그 표면마다 조용히 막힌다(운영 실측: 강은미 MANAGER+ACCOUNTING, 고애희는 ADMIN 이라 무증상). 수정 ① `order_mutation_policy` 에 capability alias SSOT(`_TEAM_CAPABILITY_ALIASES = {"ACCOUNTING": ("CS",)}` + `team_capabilities`/`team_has_capability`)를 두고, 팀 검사 8곳(정책 엔진·erp_permissions·production steps·packing·quest 승인·assignment 해제·주문 플래그·첨부 정렬)을 전부 그 함수로 통일. **정규화로 CS 치환하지 않는다** — team 값이 ACCOUNTING 으로 남아야 정산 게이트가 회계팀을 구분한다. ② 사용자 결정으로 정산 표면(대시보드 페이지·집계 API·실무 행 API·채널 탭)을 **ADMIN + 회계팀**으로 좁혔다(그 전에는 FINANCE_MUTATION 과 같은 집합이라 CS/영업 매니저 10명이 전사 매출·미수 총액을 봤다). MANAGER 는 엔진에서 team 검사보다 먼저 통과하므로 `teams` tuple 로는 표현이 불가 → `Policy.gate`(`"module:function"` 지연 해석) 신설로 엔진·핸들러·UI 은닉이 같은 함수 `settlement_channel_access.is_accounting_or_admin` 을 쓴다. 주문 상세의 입금확인·현금영수증(`FINANCE_MUTATION`)은 CS/영업 그대로 — 두 집합은 더 이상 같지 않다(계약 테스트로 그 차이를 못박음). 검증: tests/domains 6551 passed·5 skipped, pre_push_smoke, APP_OK | order_mutation_policy.py, settlement_channel_access.py, settlement_dashboard.py, erp_permissions.py, order_flag_permissions.py, order_attachment_permissions.py, assignment.py, api/{production/orders,shipment/packing,quest}.py, settlement_dashboard_body.html, 테스트 7개, foms_state_writer_inventory.json | deploy 대기 |
| 2026-09-03 | **네이버 정산 v1.2 — 예외 큐 미연결 2갈래·지급 보류 일자별 상세·정산 화면 글자 크기 조절** — 운영 실측(읽기 전용)으로 `naver_settle_case` 1,353행이 전부 미연결인데 그중 1,321행은 링크가 있고 주문만 없는 워크벤치 대기(링크 없음은 32)라 한 라벨 "FOMS 주문 미연결" 로는 조치할 사람·화면을 못 갈랐다 → kind `UNMATCHED`(워크벤치 대기, `/admin/naver-ingest/triage?link_id=N`)·`UNLINKED`(수집 전 주문, `/admin/naver-ingest`) 두 갈래(각각 상한, 한 상한이면 적은 쪽이 사라짐), kpi `unmatched_pending_count`/`unmatched_unlinked_count`, 스트립 질의 예산 5→6. 지급 보류 -1.2억 원인 추적용으로 `data.holdback`(payHoldbackAmount·settlementLimitAmount 일자별, 부호 원본, 합계만) + KPI "보류·한도" 타일 role=button 펼침 표(운영엔 한도 0·보류 24행, 보류/해제 짝). 사용자 추가 요청: 정산 화면 글자 크기 −/+ 조절기 — 네이버 워크벤치 `.wb-fs` 패턴 이식(루트 변수 `--s-fs`, 정산 CSS 3파일 font-size 137곳 calc, 단계 1/1.15/1.3/1.5, localStorage `foms.settlement.fontScale`, 기존 위임 클릭·mount 복원, 전역 리스너 0). 이 F6 는 사용자 지시로 CEO 워크플로(설계→워커 3 병렬→통합 검증→2판정 리뷰→판정 ship, 8 에이전트)로 수행, minor 7 중 5 반영·2 후속. 핀 셸 4줄+채널 2줄 → `20260903c`. 검증: 채널 스위트 325·게이트 881·font_scale 16·렌더 3종 399 passed, pre_push_smoke PASSED, APP_OK | foms/services/settlement_channel.py, static/js/settlement/{channel,dashboard}.js, static/css/settlement/settlement-{channel,dashboard,operations}.css, templates/cs/partials/settlement_dashboard_body.html, tests/domains/test_settlement_{channel_api,channel_render,channel_strip,font_scale}.py, docs/plans/2026-09-02-naver-settlement-ledger.md(Phase F), docs/plans/2026-09-03-settlement-followup-brief.md | deploy `e432a3605` |
| 2026-09-02 | **정산 대시보드 폭·높이 개편 — 1440 캡 해제·차트 확대·집중 모드** — 1920 화면에서 본문이 1440px 에 멈춰 양옆 240px 씩 비고(2560 은 560px), 상단 크롬 ≈ 400px 이 본문을 화면 38% 아래로 밀어 실무 탭 표가 60행 중 13행만 보였다(브라우저 축소는 캡 때문에 글자만 작아짐). "모달로 띄우자"는 안은 기각 — 모달은 뷰포트보다 넓어질 수 없고 셸 탭 URL·뒤로가기·실무 탭 확인 모달(모달 위 모달)을 깨뜨린다(페르소나 5종 재검토: `research_personas.md`). 수정: `.s-pane-mount/.s-grid/.s-foot/.s-state` max-width 제거, 기본 13→14px·KPI 23→30px·축 10.5→12px, 추이 차트 268→뷰포트 38%(320~460)·aging 208→300, 요약 그리드 메인(9칸·2줄)+정산현황/채널(3칸)+aging(5)/단계(7) 재배치, `.s-strip` 한 줄을 탭줄 메타로 합침(40px 회수), 집중 모드(공용 크롬 접기 — `body.foms-settle-focus:has(.foms-settlement-root)` 스코프라 셸 스왑 시 자동 해제, Esc·localStorage), 자산 핀 4곳 `20260902a`. 목업 `docs/design/mockups/settlement-dashboard-wide-fix.html`(현재/수정안/집중 비교). 검증: settlement render 261 passed, APP_OK, pre_push_smoke exit 0 | static/css/settlement/settlement-dashboard.css, static/js/settlement/dashboard.js, templates/cs/partials/settlement_dashboard_body.html, tests/domains/test_settlement_dashboard_render.py | deploy `796d26982`·`606a178e3`·`416a3acfc` · production `2768204f7`(PR #277) |
| 2026-09-02 | **네이버 수집 누락 창 폐쇄 + 취소 확정 집 오매칭 차단** — 고객 황인영의 8/10 결제 주문(배송중, 상품주문 5건)이 FOMS 에 0행이었고, 화면은 같은 고객의 8/7 **취소완료** 집을 살아 있는 주문 #4704 에 붙이라고 권했다(붙으면 유령 분류 + 폐기 버튼이 열린다). 원인: 변경 피드는 주문일이 아니라 **마지막 변경 시각**으로 색인된다 — 그 건의 마지막 변경이 소급 수집 종료(08-31) 하루 뒤여서 백필 밖이었고, 정기 스윕은 `PAYED` 만 받아(`mapping.is_collectible`) 버렸다. 버린 이벤트는 카운터도 없어 무음이었다. 실측 근거: 08-10 창 재실행 = 변경 5건 전부 기존 링크(신규 0), 09-01 창 = 그 집 5행 + 다른 7집. 수정 ① 수집 술어를 `ingest._partition_feed` 한 곳으로: PAYED 는 종전대로 처리 큐 안, **처음 보는 productOrderId 는 상태 무관 수집**(확인 완료 표시 = 큐 밖), 아는 번호의 비PAYED 는 상세 미조회(호출량 불변). 상태 열거 방식은 채택하지 않았다 — 관측 못 한 상태에서 같은 누락이 반복된다. 첫 수집 링크로 클레임 알림이 안 나가는 것은 음성·양성 대조군 테스트로 고정. ② `dropped_by_status`·`quiet_collected` 를 워터마크 요약에 남겨 무음 제거. ③ `find_unlinked_matches` 가 클레임 축을 전혀 안 봤다 → 집 전체가 **확정** 취소·반품이면 제외(`all_money_back_settled`), 확정 전과 부분 취소는 남긴다, 뺀 집 수를 띠가 말한다. 운영 복구: 09-01 창 수집으로 대상 5행 + 8집 22행, 06-04~09-01 전체 재긁기 신규 0(잔여 누락 없음), 워터마크 불변·측정 계정 재잠금. 검증: tests/services/integrations 1464 passed, pre_push_smoke exit 0, deploy·승격 PR 검사 각 4/4 green, 승격 트리 직접 1459 passed, 운영 스윕에서 새 코드 동작 확인 | ingest.py, bulk_dispatch.py, naver_workbench.html, naver_dispatch_strip.html + 테스트 3개 | deploy `dc1cde8b6` · production `a0c10c173`(PR #268) |
| 2026-09-02 | **네이버 이력 탭 찾기가 "지금 쪽 50집"만 뒤졌다 — 서버 검색으로 전환** — 이력 표는 서버가 50집씩 잘라 보내는데 찾기 칸은 화면(JS) 필터였다. 운영 800주문·16쪽에서 고객명(김유리)을 쳐도 1쪽에 없으면 `0주문 / 이 페이지 50주문` — note 가 "다른 쪽에 있을 수 있습니다" 라고 말해 주긴 했지만 사람은 "검색 고장" 으로 읽는다. 수정: `_history_find_clause` (컬럼만 보는 SQL — external_order_no·recipient_name·group_key·붙은 주문의 customer_name/phone·전화 숫자·FOMS 주문번호, JSONB 스캔 0)를 `_link_rows` 의 **집 선정 단계**에 걸어 total·쪽수·칩 숫자 4종이 좁힌 결과와 같은 술어에서 나오게 했다(캡 뒤 파이썬 분류 함정 회피). 찾기 칸은 GET 폼(`q`)이고 칩 8개·페이저가 낱말을 들고 간다. 제품명은 `raw_snapshot` 파생값이라 SQL 이 못 봐서 placeholder 에서 뺐다(못 하는 일을 약속하지 않는다). JS 는 이력 탭에서 화면 필터를 빠져나온다 — 서버 결과 위에 한 겹 더 얹히면 note 숫자와 보이는 줄이 갈린다. 픽스처가 수집 파이프라인처럼 매칭 축 컬럼(recipient_name)을 채우도록 고쳤고, 그 과정에서 안 붙은 수집분 매칭 테스트가 원본만 바꿔 실제로 없는 상태를 시험하던 것도 함께 잡았다. 검증: tests/services/integrations 1453 passed, pre_push_smoke exit 0 | foms/web/admin/naver_ingest.py, templates/admin/naver_workbench.html, static/js/admin/naver-workbench.js, static/css/admin/naver-workbench.css + 테스트 5개 | deploy 미푸시(로컬 커밋) |
| 2026-09-02 | **지오코딩 사고 종결 — 최초 트리거 규명 + 조용한 오답 3종 + outbox 소음 제거** — ① 어제 "규명 못 함"으로 남았던 트리거를 찾았다: **`SIDEFX` 서비스에만 `KAKAO_REST_API_KEY` 가 없다.** 조사가 운영 서비스를 3개(web·WORKER·FOMS-cron)만 확인했고 GEOCODE outbox 소비단인 SIDEFX 는 그 뒤 등록돼 목록에서 빠져 있었다. 키가 없으면 `kakao_rest_headers()` 가 RuntimeError → (수정 전) 변환기가 삼켜 "AI 변환 실패" → `failed`. 원장이 관측한 `attempts=1 · last_error=NULL · DONE` 이 이 지문이다. 배포 후에도 새 `failed` 가 생기는 것을 보고 추적했다(신 코드는 `failed` 를 쓰지 않는다 — success/pending/address_error 뿐). 그 서비스가 오늘 찍은 주소 4건을 같은 운영 키로 직접 변환하니 **4/4 첫 전략에서 성공** — 주소는 이번에도 무죄. 조치: 키 설정 + 재배포, 오탐 5건(#5095~#5099) 재변환 5/5 success. ② **동네 중심 좌표를 성공으로 반환하던 폴백**(어제 "범위 밖"으로 남긴 것) — 저장 좌표가 동네 중심 좌표와 같은지 대조해 3,784건 중 **4건** 검출(질의 종류별 1회 호출 817회로 전수). 정체는 동 추출 정규식 `(\w+동)` 이 **구 이름에서 동을 만든 것**(`성동구`→`성동`), 그래서 #2418 이 진짜 위치에서 **2,214m** 떨어진 좌표를 success 로 저장했다. 수정 3갈래: 동 추출에 한글 경계+아파트 동번호 제외, `시+구` 만으로는 폴백 금지(구 중심=수 km), **일시 오류를 겪었으면 폴백 자체 금지**(네트워크가 죽은 자리를 동네 중심으로 덮으면 재시도됐어야 할 건이 굳는다). 운영 #2418 좌표 정정(승인 후, 5개 컬럼만·알림 0). ③ **outbox 소음**: `CHANNEL_PUSH_RECORDED` 가 **1,188행 전량 DEAD**(08-03 이후, `CHANNELTALK_PUSH` 이벤트 1,190건과 일치). 기능 손실은 없다 — 전송은 이 행 이전에 끝나고 이력·이벤트는 같은 tx. 문제는 그 1,188행이 **진짜 배달 실패를 덮는다**는 것(①을 추적할 때 실제로 이 로그를 헤집었다). `record_only_effects.py` 신설로 "배달할 일 없음"을 명시 등록. `STAGE_NOTIFICATION` 118행은 손대지 않았다 — 소비자 미구현이고(운영 notifications 에 단계 전이 유형이 한 번도 없다) 낼지는 제품 결정. 검증: 본 스위트 7755 passed, PG 레인 738 passed, pre_push_smoke exit 0, CI 4/4 green, 승격 트리 직접 검증 | address_converter.py, address_query.py, geocode_retry.py(신규), geocode_helpers.py, geocode_delivery_handler.py, geocode_candidates.py, run_geocode_sweep.py, erp_map.py, measurement/map.py, map_snapshot.py, foms_advanced_address_processor.py, record_only_effects.py(신규), run_domain_side_effect_outbox.py + 테스트 9개 | production `c8492b6b`(PR #243) · `17bc0027`(PR #251) · deploy `e31dd8e5` |
| 2026-09-01 | **지오코딩 일시오류/주소오류 분리 — 근본 수정 4건** — 운영 실측 지도에 `주소오류` 배지가 붙은 11건의 주소가 전부 무죄였다(같은 코드·같은 운영 키로 재변환 11/11 성공). ① **원인 자리**: 변환기가 키 부재·타임아웃·429·HTTP 비200 을 전부 `except Exception` 으로 삼켜 "주소를 찾을 수 없음"과 같은 실패로 강등했고, 저장단이 그걸 `failed` 로 굳혔다. 사유가 DB 에도 로그에도 안 남는다. 변환기가 실패 종류(transient/permanent)를 돌려주게 하고(HTTP 401/403/408/429/5xx=transient, 200+documents 0건·bbox 밖=permanent, 한 전략이라도 transient 면 결과는 transient), 일시 오류는 실패 캐시에도 넣지 않는다(600초 동안 같은 오답 재사용 금지). ② **재시도**: `geocode_status` 를 4상태로(`address_error` 신설). 일시 오류는 `pending` 유지(좌표·해시 불변 — 판정된 적 없는 주소를 처리 완료로 표시하지 않는다), 주소 오류만 `address_error`. SIDEFX handler 는 일시 오류를 예외로 올려 worker 백오프 재시도로 보낸다. 재시도 판정이 세 곳(`erp_map` 영구 제외 / `measurement/map` 24h / 스윕 SQL)에 갈라져 있던 것을 `foms/services/geocode_retry.py` SSOT 로 모았다 — pending 600초·failed 24시간 백오프, `address_error` 는 `--include-failed` 로도 안 잡힌다. 재큐 시 `geocoded_at` 시도 표식을 찍어 중복 enqueue 를 막는다. 화면은 3상태 그대로(읽기 경로 2곳에서 `address_error`→`failed` 정규화). ③ **`번길` 절단**(실패보다 나쁜 조용한 오답): `_ROAD_RE` 가 `판교로 256번길 25` 를 `판교로 256` 으로 잘랐다 — 서로 다른 도로이고, 잘린 값도 좌표가 나오므로 **엉뚱한 좌표로 성공**한다. 운영 키 실호출 대조: `종로 1길 50` 은 옛 규칙이 **426m 떨어진** 지점을 성공으로 반환. 부정 전방탐색만 두면 백트래킹이 숫자를 갉아먹어(123→12) 더 나쁜 절단이 되므로 숫자 경계를 함께 뒀다. ④ **구 이름 부분 치환**: `'강남'→'강남구'` 전역 치환이 `서초구 … 강남빌딩` 을 `강남구빌딩` 으로 만들어 구 추출이 뒤집혔고, simplified 폴백이 강남구 좌표를 성공으로 반환했다 — 실호출 대조 **3,968m** 차이. 낱말 경계에서만 치환(`서울 강남 역삼동`→`강남구` 보완은 유지). 검증: 신규 계약 44건(전부 음성 대조군 동반 — 기존 `test_address_query` 는 문자열 동일성만 봐서 4건 모두 못 잡았다), 본 스위트 7645 passed, pre_push_smoke exit 0. `tests/visual` 로컬 실패 19건은 수정 전 `origin/deploy` 워크트리에서 **동일 목록**임을 확인한 기존 win32 기준선 드리프트. **잔여=운영 승격 승인 대기** | address_converter.py, address_query.py, geocode_retry.py(신규), geocode_helpers.py, geocode_delivery_handler.py, geocode_candidates.py, run_geocode_sweep.py, erp_map.py, measurement/map.py, map_snapshot.py, foms_advanced_address_processor.py + 테스트 7개 | 43c765436, 84fca19d6, f3d3f04f5 (deploy) |
| 2026-08-31 | **네이버 일괄 발송처리 결과 UI(NAVER-BULKDISPATCH-02)** — 운영 1회차에서 버튼을 누르고 새로고침했더니 띠가 통째로 사라져 발송 여부를 화면으로 알 수 없었다(동작은 정상, 6집 26건 전부 성공). 결함은 그 **사라짐이 '다 나갔다'와 '애초에 대상이 없었다'를 동시에 뜻한다**는 것 — 불가역 조작에서 이게 구분 안 되면 사람이 판매자센터를 다시 열게 되고 이 기능이 없앤 일이 되살아난다. 수정: 띠를 지우지 않고 **상태를 바꾼다.** ① `build_day_summary` 신설 — 오늘 실측 네이버 링크를 pending 필터 **없이** 전부 보고 집마다 보냄·남음·실패를 센다. `select_targets` 는 그 위 얇은 필터로 재배치(술어 두 벌 방지). 발송 판정은 신호 두 벌(우리 표식 + 네이버 `delivery.sendDate`)이라 **판매자센터 수동 발송분이 '남음'으로 남지 않는다**(화면 큐가 하고 있는 실수). `build_preview` 기존 키 넷(count/eligible/blocked/rows)의 뜻은 불변, 결과는 새 키로만. ② 두 화면(워크벤치·실측 대시보드) 띠가 4상태로 갈린다 — 완료(초록·발송시각)/일부(보낸 수+남은 수 병기)/실패(빨간 줄+사유)/대기. 오늘 대상이 0인 날은 띠 자체가 없다. ③ `GET /admin/naver-ingest/bulk-dispatch/state`(ADMIN·MANAGER, 읽기 전용, `build_preview` 재사용) 신설 — 버튼 직후 `window.alert` 대신 띠 안 진행 문구 + 3초 폴링으로 'N집 발송 완료'까지. 종료 조건은 `eligible === 0`(남은 집 수로 재면 막힌 집이 있는 날 안 끝난다). 다 나간 집의 옛 실패 기록은 실패로 세지 않는다(수동 발송분이 정확히 그 모양을 만든다). 검증: 신규 계약 29건 포함 bulk_dispatch 63 passed, CI 본 레인 로컬 7223 passed, pre_push_smoke exit 0, CI 4/4 green. **미검증=실브라우저 폴링**(불가역이라 실호출 안 함) | bulk_dispatch.py, naver_ingest.py, naver_workbench.html, naver_dispatch_strip.html, naver_bulk_dispatch_button.html, naver-workbench.css 외 4개 | 18df627a, e204565d, 43f4024d |
| 2026-08-31 | 지도 주소 수정 403 근본 수정 — `map_view.html` 이 `layout_head` 를 안 쓰는 standalone 문서라 CSRF 토큰·fetch 래퍼가 통째로 빠져 ADMIN 도 403(운영 `security_logs` 4건, 2026-07-04 가드 도입 이후 2개월 잠복). 배선을 `csrf_bootstrap.html` 로 분리해 layout_head·map_view 공용 + standalone 계약 테스트 신설. 동반 수정: 실패 안내 유실(`error` vs `message`), 주소 전처리 SSOT 화(붙여쓴 동호수 미제거로 모달·워커 동시 0건 — 주문 5062), `can_edit_erp` 팀 정규화 통일(MEASURE) | csrf_bootstrap.html, map_view.html, address_query.py, address.py, address_converter.py, erp_permissions.py 외 8개 | 3cb6e428, 1ccad147 (PR #214) |
| 2026-08-31 | **고객 공유 링크(카톡 발송) UX 개편 — 계약서 폼 이식 + 도면 일괄 저장** — ① 계약서 링크(`/s/<token>` kind=estimate)가 밋밋한 표로 뜨고 금액칸이 화면 밖으로 잘리던 것을 **ERP 계약서 폼 그대로**(상단 컬러바·로고·계약번호·사업자정보+인감·고객정보·5열 품목표·결제정보·출고가/잔금·법적문구) 서버 렌더로 교체. 렌더 소스는 발급 시점 동결 스냅샷뿐(D6) — 스냅샷 빌더 무변경, 화이트리스트 밖 참조 0건. ② **저장 버튼 무반응의 근본 원인 = `window.print()`** — 카톡 인앱 웹뷰(iOS WKWebView·Android WebView)는 구현 자체가 없다. html2canvas PNG 저장으로 교체하되 `blob:` 은 WKWebView 가 `<a download>` href 로 못 써서(WebKit 216918) **`toDataURL` 1순위**, 저장물은 화면이 아니라 **700px 오프스크린 클론**을 찍는다(ERP `_buildExportClone` 패턴 — 안 그러면 폰 레이아웃이 그림이 된다). 터치 단말은 성공 여부와 무관하게 폴백 이미지를 붙여 '길게 눌러 저장' 길을 남긴다. ③ 계좌번호 복사 버튼(clipboard→execCommand 폴백, 히트 영역 44px). ④ 도면 링크에 `GET /s/<token>/drawings.zip` 신설 — 열람과 같은 검증 체인(404/410/503)·`_collect_drawing_files` 재사용(주문 격리)·200MB 상한·`SpooledTemporaryFile`+`ZIP_STORED` 스트리밍·`FILE_DOWNLOAD` 감사 1건. **한 장이라도 못 읽으면 503**(일부만 담긴 zip 을 200 으로 내보내지 않는다). UI 는 주 버튼(ZIP) + 접힌 개별 목록 + 카톡 UA 서버 판정 안내. ⑤ 곁가지: 할인 줄 숨김(출고가에 이미 흡수 — ERP 규칙), 치수 오독 차단(`word-break: break-word`→`overflow-wrap`), 2공장 로고를 ERP 와 동일 파일로, 고객 화면의 ERP 내부 낱말('승인'·'lightbox'·'16:9') 제거, 공유 자산 `?v=` 핀 드리프트 게이트 신설(저장소에 없던 계약), 사문 CSS 116줄 삭제. 검증: 계약 테스트 신규 30여건 포함 **179 passed**, APP_OK, 로컬 dev 실브라우저 실측(390px 금액칸 right 359<390·PNG 1400×2090·카톡 UA 폴백 노출·복사 동작). 미검증: 실기기 카톡 인앱 다운로드·iOS UA 리터럴 등 5건(원장 기재) | foms/api/share.py, share_estimate_body.html, share_drawing_body.html, foms-share-contract.css(신규), share-contract.js(신규), test_order_share_view.py 외 6개 | (deploy) |
| 2026-08-31 | **네이버 다시 읽기 — 남의 화면 진행 표시 + 소요 시간 예고 + 조회 주기 완화(NVREPAY-05)** — ① 진행 표시가 누른 브라우저 안에만 있어(link_ids 를 들고 있어야 도는 배선) 다른 관리자는 돌고 있는 중에도 `다시 읽기 87주문` 을 보고 또 눌렀다. **새 저장소 없이** 감사 원장의 최근 `NAVER_INGEST_REFRESH_ALL_ENQUEUE` 행(누가·언제·몇 집)과 워커가 찍는 `claim_sync.refreshed_at`(어디까지)를 붙여 `GET .../triage/refresh-running`(ADMIN·인자 없음) 신설 — 행위자가 남으므로 **누가 눌렀는지까지** 말한다. 돌고 있는 동안엔 버튼 자리를 진행이 차지한다(같은 조회 중복 방지). 집 세기는 `claim_watch.refreshed_household_counts` 한 벌로 합쳐 자기 요청·남의 요청이 같은 판정을 쓴다(라우트에 두 번째 계산이 다시 생기면 red). 창 300초 = 화면 폴링 마감과 같은 값, 끝난 집은 넣은 집 수를 못 넘게 자른다. ② 걸리는 시간 침묵이 '멈췄나'의 절반이었다 → 스탬프 분포 실측 3벌(운영 45집 42.3초·스테이징 85집 81.7초·87집 84.5초, 전부 집당 0.94~0.98초)로 **범위** 문구(집당 1.0~2.0초, 워커 1대라 앞선 작업이 있으면 통째로 밀린다 — 실측 첫 스탬프 +40.5·+69.0초). 서버가 한 번 만들고 툴팁·모달·라벨이 같은 값을 쓴다(`refresh_all.eta`·`data-eta`). ③ 자동 조회 주기 300→1800초(코드 무변경, 사용자 확정 — 대가는 취소 발견 최대 30분 지연): **스테이징 worker 적용·재배포**, 부팅 로그 `[naver-sync-loop] started (interval=1800s)` 확인. 운영은 명시 승인 대기. 검증: 계약 15건 신규(총 41), `tests/services/integrations/` 1109 green, pre_push_smoke exit 0, CI 전 워크플로 green, 스테이징 실화면 브라우저 2개 대조(A 가 누르고 B 가 `다시 읽는 중 — 87주문 중 N주문 완료 · Claude 실서버 측정용 시작` 을 보며 버튼 잠김, 87집 84초에 완료·자동 접힘·쿨다운 칩), 코호트 38 원복·닫힘 확인 | claim_watch.py, naver_ingest.py, naver-workbench.js, naver_workbench.html, test_naver_refresh_all.py 외 2개, 2026-08-29-naver-origin-cleanup-strip_SPEC.md | bfab9a11 |
| 2026-08-31 | **마지막 통지 경로가 조용히 죽던 자리** — `fulfillment.record_task_failure` 는 워커가 서비스 바깥에서 죽었을 때(인증 만료·클라이언트 생성 실패) 사유를 남기는 **유일한** 통지 경로인데(docstring 이 스스로 '실패해도 조용하지 않게 한다'고 적어 뒀다), 링크 조회가 `FulfillmentError` 면 아무 말 없이 `return` 했다. 화면은 '요청했습니다'로 멈춰 있고 취소는 재시도 버튼도 없어서, 그 조합이면 실패가 어디에도 안 남는다. 사유를 못 남기더라도 **못 남겼다는 것은 로그로 남긴다**. 이 함수는 테스트가 아예 없었어서 계약 1건 신설(수정을 되돌리면 red 인 것까지 확인). 곁가지 훑기에서 나온 건 — 쓰기 라우트 17개 감사·권한·매니페스트 등재, 감사 action 18종 라벨, 화면·라우트 권한 술어 일치, JSONB ilike 0건, `perf-ok` 주석 11곳 전부 배치/상한 있음은 이상 없었다 | naver_commerce/fulfillment.py, test_naver_fulfillment.py | (deploy 대기) |
| 2026-08-31 | **네이버 감사 로그에 행위자가 없던 것** — `foms/web/admin/naver_ingest.py` 의 `log_access` **19곳 전부**가 `user_id` 를 안 넘겨, 네이버 관리 조작 감사 행이 전량 `user_id IS NULL` 이었다(운영 실측 2026-08-30: `NAVER_INGEST_RUN_NOW`·`CREATE_ORDER`·`REFRESH_ALL_ENQUEUE` 등 전부). 저장소 전체로는 127곳 중 90곳이 넘기고 있어 **관행이 아니라 이 파일의 누락**이었다 — 누가 눌렀는지 없는 감사는 절반짜리다. 19곳에 `session.get("user_id")` 를 넣고, 되돌아가지 못하게 소스 스캔 계약 1건 신설(새 라우트가 빠뜨리면 red). 감사 스텁을 쓰던 반품 계약 1건도 행위자를 검사하도록 조정 | naver_ingest.py, test_naver_refresh_all.py, test_naver_return_wiring.py | (deploy 대기) |
| 2026-08-30 | **`tests/visual` CI 등재(CI-VISUAL-01) + 미결 커밋 `8c1ef69a` 종결** — ① 본 스위트가 `--ignore=tests/visual` 라 등재된 4개만 돌았고, 그래서 `test_erp_order_edit_mobile_form.py` 의 red 가 2주 반 살았다. 21개 파일을 CI 와 같은 조건(`sqlite:///:memory:`·`-p no:playwright`)으로 하나씩 돌려 갈랐다 — 브라우저 없이 도는 14개(135 passed·2 skipped·3.4초) 대 `page`+파일 SQLite 라이브 서버가 필요한 5개(24건 setup error). 앞의 14개를 등재(+10개), 뒤의 5개는 넣으면 CI 가 곧장 빨강이라 제외. **전체 등재는 안 했다.** 혼재 파일도 등재 가능하다 — 브라우저 테스트가 `skipif` 가드를 달고 있으면 CI 에서 skip 된다. ② 등재만 하면 다음 새 파일이 또 샌다 → `test_visual_lane_registry.py` 신설(CI-DOCSCOPE-01 과 같은 모양): 모든 `tests/visual/test_*.py` 는 ci.yml 등재이거나 이유 붙은 제외여야 하고, `ast` 로 분류를 대조한다(제외 파일은 정말 브라우저 픽스처를 요구해야 하고, 등재 파일의 브라우저 테스트는 skip 가드 필수). `pre_push_smoke` 대상에도 추가. 음성 대조군: ci.yml 에서 한 줄 지우고 빈 파일 하나 만든 상태로 둘 다 이름을 대며 빨강 확인 후 원복. ③ 한 달 미결이던 `8c1ef69a`(삭제 라우트 WRITE-GUARD-01 등재)는 **중복 커밋**이었다 — 같은 내용이 `0e98c108` 로 deploy·production 양쪽에 있고 manifest 엔트리 `erp_orders_as.api_as_log_delete` 가 실재한다. 승격 불필요, 미결 줄 종결. | .github/workflows/ci.yml, tests/domains/test_visual_lane_registry.py(신규), scripts/ops/pre_push_smoke.ps1, docs/AI_STATUS.md, docs/plans/2026-08-28-naver-claim-phase-ledger.md | (deploy 대기) |
| 2026-08-30 | **네이버 다시 읽기 — 대상 축소 + 끝 알림(NVREPAY-04)** — 운영 실사용 지적 2건. ① 이력의 모든 주문을 다시 읽으면 안 된다 → 집의 **모든** 상품주문이 종결(`CLAIM_PHASES==done` 또는 `productOrderStatus ∈ {CANCELED, RETURNED, PURCHASE_DECIDED}`)이면 제외 + 10분 쿨다운. 운영 58집→46집. **나이 기준은 쓰지 않는다** — 구매확정은 네이버 쪽 사실이고 나이는 추측이라, 자사 배송·시공으로 몇 달 끄는 `DELIVERING` 23집의 진짜 취소를 놓친다. 하나라도 살아 있으면 읽고(집 단위 AND), 모르는 상태는 종결이 아니다. 뺀 수는 응답·화면이 말한다(조용한 절단 금지) — 버튼도 `전체 다시 읽기`→`다시 읽기 N주문`. ② `다시 읽는 중` 이 안 풀려 멈춘 걸로 읽혔다(운영에서 28초 만에 재클릭·두 번째는 낭비) → `GET .../triage/refresh-progress`(읽기 전용·ADMIN) + `watchRefreshAll` 폴링, 끝나면 자동 새로고침. 기존 벌크 배선(`watchBulk`+`fulfillment-progress`)과 같은 모양이라 새 상태 저장소 없음. `since` 는 큐 적재 **전에** 잡고, 진행은 대표가 아닌 **집 전체**로 센다. 검증: 계약 23건(신규 13) green, `tests/services/integrations/` 1038건 green, 인벤토리 드리프트 게이트 3종 green | claim_watch.py, naver_ingest.py, naver-workbench.js, naver_workbench.html, test_naver_refresh_all.py 외 2개 | 032b43d0, f57710ca, 운영 승격 PR #197 `4be86ab2`(운영 실측 58집→45집·종결 13집 제외, 링크 152/200 만 조회, 진행률·자동 새로고침·쿨다운 전부 확인) |
| 2026-08-30 | **네이버 수집 목록 전체 다시 읽기(NVREPAY-03)** — 정리 대기 띠의 `전부 다시 읽기` 는 그 띠의 모집단(살아 있는 옛 주문)만 대상이라 0건이면 버튼 자체가 없다. 목록 전체의 상태를 갱신할 자리가 없었다. 머리줄에 `전체 다시 읽기 N주문`(ADMIN 전용, policy `ADMIN_OPS`) 신설 + `POST /admin/naver-ingest/refresh-all`. **집당 대표 링크 하나만** 큐에 넣는다(`refresh_household` 가 형제 상품주문 전체를 읽으므로 건별로 넣으면 같은 집을 건수만큼 중복 호출). 확인 모달을 두는 이유는 되돌림이 아니라 규모(집 수십 개 조회 + 새로 발견된 클레임 알림). 캡 200집, 넘으면 최신부터 자르고 잘랐다고 말한다. 네이버 쓰기 0회(상세 조회만). 검증: 계약 10건 신규, 스테이징 실브라우저에서 버튼(131주문)→확인 모달→`queued:131,failed:0`→워커 재조회로 링크 444/444·집 131/131 `updated_at` 갱신(2분 9초)까지 전 구간 확인. 자동 스윕이 놓쳤던 취소 1건이 실제로 알림으로 나왔다. 코호트는 `38` 로 원복·재배포 완료 | claim_watch.py, naver_ingest.py, naver_workbench.html, naver-workbench.{css,js}, audit_message_display.py, ci.yml, 매니페스트 3종, test_naver_refresh_all.py | 12eebe39, 운영 승격 PR #195 `5acef038`(운영 실측 58집/200링크 62초, 자동 경로가 놓친 취소 1건 실제 알림) |
| 2026-08-30 | **재결제 뒤 정리 안 된 옛 네이버 주문 — 목록 띠 + 일괄 다시 읽기(NVREPAY-02)** — 옛 주문의 살아 있음은 그 주문 pane 을 연 사람에게만 보였다. 안 접으면 고객 돈이 두 번 묶이고 옛 주문은 네이버에서 그대로 도는데 목록엔 그 수가 없었다. 처리 탭에 정리 대기 띠(건수·취소/반품 갈래·마지막으로 읽은 때·`옛 주문 열기 →`)와 `전부 다시 읽기` 버튼 신설. 판정은 pane 과 같은 술어(`relations=("NEW",)`)라 ADDON 은 모집단 밖. 일괄 다시 읽기는 상세 조회뿐(네이버 쓰기 0)이라 확인 모달 없음, **화면이 보낸 link_id 를 안 쓰고 서버가 다시 센다**. 불가역은 여전히 옛 집 pane 에서만. 검증: 계약 15건 신규, 스테이징 실화면에서 띠→버튼→워커 재조회→띠 소멸까지 전 구간 확인(시드 후 원상 복구). CI-DOCSCOPE-01 등재 누락으로 첫 푸시 red → 등재 후 4/4 green | order_candidates.py, naver_ingest.py, naver_workbench.html, naver-workbench.{css,js}, audit_message_display.py, ci.yml, 매니페스트 3종, test_naver_origin_cleanup.py, tools/ops/check_naver_repay_origin_alive.py | 9745f4df, 73bd35a3 |
| 2026-08-29 | **모바일 마법사 예약금 증발 핫픽스 4건** — ① 제품 가격보다 예약금을 먼저 넣으면 값이 `0원`으로 눌려 사라졌다(placeholder 가 `0원`이라 빈 칸으로 보임). 원인은 `min(예약금, 출고가)` clamp 가 상한 0(가격 미입력) 에서도 돌았던 것 — 클라 2곳(`buildWizardTotals`·blur)과 서버 1곳을 `상한>0` 조건부로. 전수 확인: JS·Python 통틀어 금액 clamp 는 이 3곳뿐. ② 예약금 칸 음성 입력 마이크 제거(`data-foms-no-voice` opt-out 신설). ③ iOS 초안 유실 — 자동저장이 `beforeunload` 한 곳뿐이라 앱 전환 시 디바운스(1초) 대기분이 날아갔다. `pagehide`·`visibilitychange(hidden)` 동반 + 대기 타이머 정리. ④ 금액칸 커서가 재포맷마다 끝으로 튀던 것 → 끝 오프셋 보존(`restoreAmountCaret`) + iOS 되돌림 rAF 교정. 마법사와 공유 주문 폼(add/edit) 양쪽 적용. 검증: 회귀 테스트 6종 신규, 스테이징 실브라우저(iPhone UA·데스크톱) 재현 확인 — 예약금 300,000원 유지·중간 수정 시 커서 유지·hidden 시 PUT 발생. 운영 승격 PR #189·#191 | static/js/foms/{wizard,draft}.js, static/js/orders/erp-order-shared.js, static/js/foms/voice-input.js, foms/api/erp_order_draft.py, templates/orders/wizard/{step2_products,wizard_shell}.html, templates/orders/partials/erp_order_js.html | 8851e937·e4e86024 (PR #189), 945a54ce (PR #191), c335cd83 |
| 2026-08-28 | 네이버 커머스API 인증 만료일 등록면 신설 — 저장 함수·D-7 경고는 있었는데 값을 넣을 칸이 없어 카드가 늘 `미등록`(경고가 뜰 수 없는 상태). 수집 상태 카드에 달력 칸+저장, `POST /admin/naver-ingest/app-expiry`(ADMIN 전용·형식/5년 밖 연도 400). 신규 mutation 계약 4종 등재 | naver_ingest.py, naver_workbench.html, naver-workbench.js, audit_message_display.py 외 4개 | 5ff71e6a |
| 2026-08-28 | 네이버 이력 탭 모든 행에서 워크벤치 처리 pane 으로 가는 길 — `확인 완료 — 큐에서 빼기` 한 집·주문 생긴 집·취소 집은 처리 목록에 없어 ERP 도크(ADMIN·MANAGER)가 유일 통로였다. 마지막 칸 `원본` → `열기`, 줄마다 `원본 보기`·`워크벤치` 평범한 링크(버튼 0, 이력 절대 규칙 3 유지). 운영 실화면 50줄 확인·pane 개방 확인 | naver_workbench.html, naver-workbench.css, test_naver_workbench_history_open.py 외 6개 | e8e13f48 (PR #182), 8ad31702 (PR #184) |
| 2026-08-27 | 채널톡 401 전송 실패 근본 수정 — issueToken expiresIn 이 공유 세션의 남은 수명(1800→1521)인데 고정 29분 캐시로 죽은 토큰 재사용. expiresIn 기반 TTL + 401 시 재발급 후 1회 재시도. 외부 토큰 클라이언트 규율 계약 테스트 신설 | channel_client.py, test_channel_client.py, test_external_token_client_discipline.py | dde4ac36 (PR #167), 90c452dd |
| 2026-08-26 | 모바일 주문 상세에 ERP 추가 입력(extra_input) 노출 — KV 행 신설·여러 줄 보존(pre_line), v3 셸 CSS 규칙 이식·자산 핀 범프 | erp_mobile_order_display.py, order_detail_mobile_v2.html, foms_kv.html, foms-kv-row.css, foms-mobile-v3.css 외 6개 | fdec107d·1886992a (PR #155) |
| 2026-08-26 | **네이버 알림 2건 손질 — 만료 알림 ROLE 1건 + 클레임 문안 한국어화** — ① `app_expiry.check_and_notify` 가 아직 `for admin in admins:` 로 관리자 수만큼 Notification 을 복제하고 있었다(주석의 '팀 타깃으로 ADMIN 을 못 고른다'는 ROLE 타깃 생기기 전 이야기). `target_type='ROLE'`+`target_role='ADMIN'` 1건으로 전환. ② 클레임 알림 사유가 영문 코드 그대로 나갔고(운영 실물 `사유 MISTAKE_ORDER`) 아직 주문이 아닌 수집분은 본문에 번호밖에 없어 누구 건인지 못 찾았다 → `CLAIM_REASON_LABELS`/`claim_reason_text`(모르는 코드는 원문 유지) + 원본 스냅샷에서 수취인·상품명(24자 컷)을 실어 `… · 반재혁 · 라홈 무몰딩 붙박이장 로라 시… · 상품주문번호 X 외 N건`, 제목도 `네이버 취소 완료 — 반재혁`. **점검 부산물(미조치)**: `DRAWING_TRANSFER_CANCELLED`·`DRAWING_REVISION_CANCELLED`·`URGENT_MENTION`·`ACCOUNT_SIGNUP`·`ACCOUNT_RESET_REQUEST` 는 `push_sender._DEFAULT_P1_TYPES` 미등재라 폰 push 가 조용히 안 간다(사용자 판단으로 현행 유지). 검증: 만료 계약 11 passed(수정 전 2 red)·문안 계약 2건 신규·네이버 통합+팬아웃 655 passed·smoke 324 exit 0 | foms/services/integrations/naver_commerce/{app_expiry,claim_watch,mapping}.py, tests/services/integrations/{test_naver_app_expiry,test_naver_claim_group_notification}.py | 909dbfb1 |
| 2026-08-25 | **버튼 2개 통합 템플릿 승인 대비 배선 + T15 칩 운영 반영 확인** — ① **발송 경로를 env 하나로 가르게 했다**: `SOLAPI_TEMPLATE_SHARE_BOTH_ID_{LAHOM|HAUD}` 가 있고 `kind='bundle'` 이면 그 자리에서 도면·계약서 공유를 각각 발급해 토큰 두 개로 **버튼 2개 템플릿**을 보내고, env 가 없으면 지금처럼 통합 열람 링크 1개를 보낸다. 라우트·UI·화면 무변경이라 **심사 통과 시 코드 배포 없이 전환**된다. 계약서 쪽은 발급 시점 동결(스냅샷 상한 초과 시 발송 전 400 — 조용히 옛 값이 나가지 않는다), 멱등 앵커는 bundle 공유 1건 그대로라 중복 클릭이 링크 4개를 만들지 않는다. 페어 share id 2개를 `OrderEvent` payload·감사 detail(`template: 'share_both'`)에 남긴다. ② **T15 발송 흔적 칩은 이미 운영에 있었다** — 타 세션의 전량 승격 PR #145(production `39fa919d`)에 T15 커밋 8개가 실려 갔다. 운영 실서버에서 `erp-alimtalk-trace.js`·`.css` 200 + `erpAlimtalkTraceRender` 존재 확인, 운영↔deploy T15 파일 6개 바이트 동일. 그래서 **승격 PR #144 는 중복**이고 지금은 CONFLICTING(같은 내용이 다른 SHA 로 먼저 들어감 — 리베이스 시 ledger add/add 충돌로 드러남). ③ 심사 중 템플릿 2종: 라홈 `KA01TP260825021747177Iu2C2ykuJfS` · 하우드 `KA01TP260825021755111MLTAvg2dLLn`. **승인 후 사용자 작업 2건**: env 3곳 등록 + 문자 대체발송 문구(replacements) 콘솔 등록(MCP 도구에 해당 파라미터 없음). 검증: share·gate 92 passed, `APP_OK`, pre_push_smoke exit 0, CI 전 워크플로 green | foms/api/share.py, tests/domains/test_order_share_alimtalk.py, docs/plans/2026-08-11-customer-share-phase-a-ledger.md | db17611e, 8f1e8cf1 |
| 2026-08-25 | **네이버 쿠폰 사용 여부를 화면이 말한다 + 필드 인벤토리** — 쿠폰 할인이 `할인` 합계에 녹아 있어 담당자가 "이거 쿠폰 썼나"를 네이버에서 다시 확인해야 했다(워크벤치 v3 상세엔 할인 표시 자체가 없었다). 스테이징 실데이터 281건 중 50건에 쿠폰이 붙고 **부담 주체가 다른 두 종류가 섞여 온다**(`NMP_PRD_DCNT` 네이버 100% 부담 · `NMP_PRD_DUP_DCNT` 판매자 부담) — 같은 '쿠폰 1만원'이라도 정산액이 깎이는 건과 아닌 건이 갈리므로 **판매자 부담분**을 따로 낸다. 규율 둘: 안 쓴 집도 '쿠폰 사용 안 함'이라 말한다(침묵=없음/모름 구분 불가), 부담 비율이 없으면 부담액은 모름(None)이다(0=우리 부담 없음으로 읽힌다). 워크벤치 상세는 집 단위 한 줄+행별 할인, 도크는 '쿠폰 N장 −M원 (판매자 부담/전액 네이버 부담)'. 카드사 프로모션은 다른 축이라 따로 싣는다. 함께: **필드 인벤토리 문서 신설**(`docs/guides/NAVER_FIELD_INVENTORY.md`) — 281건 전수 census 로 오는 것·쓰는 것·안 쓰는 것을 적었다. 안 쓰는 축 넷: 정산(수수료 분해)·부분취소 잔여(remain*)·배송(delivery.sendDate)·클레임 상세 사유 원문(cancelDetailedReason). 검증: 신규 계약 8건 포함 integrations 597 passed, smoke 324 exit 0, CI 4/4 green, 스테이징 실화면(쿠폰 2장 −21,000원·판매자 부담 11,000원) | foms/services/integrations/naver_commerce/{mapping,promotion,dock}.py, foms/web/admin/naver_ingest.py, templates/admin/partials/naver_workbench_pane.html, static/css/admin/naver-workbench.css, static/js/orders/erp-naver-dock.js, docs/guides/NAVER_FIELD_INVENTORY.md | ca4f9d45·87316f93 |
| 2026-08-25 | **네이버 수집 운영 개방 뒤 안정화 — 막혀 있던 `주문 만들기` + 화면 결함 3건** — ① **운영 `주문 만들기` 가 처음부터 막혀 있었다**: `수집 actor 계정이 없다: naver_ingest_bot (T0 선행 작업)`. 승격·개방은 코드와 플래그만 다뤘고 설치 체크리스트 ①(시스템 계정 2개 생성)이 한 번도 안 돌았다 — 운영 `users` 에 두 행 모두 없었다(스테이징엔 id 62·63). 수집·목록은 멀쩡해서 **사람이 첫 버튼을 누르기 전까지 어떤 로그도 내지 않는** 결함이다. `create_naver_ingest_accounts.py` 를 운영 DB 로 dry-run → 승인 → 1회 적용(id 61·62, 기존 행 변경 0), 검증은 에러를 내던 그 함수로 했다(`resolve_ingest_account_ids -> (61, 62)`). 설치 문서 체크리스트에 이 함정을 명문화. ② **워크벤치 두 줄 머리가 안 붙어 보이던 결함** — 코드는 이미 `position: sticky` 였고, 전역 nav 가 `sticky·top:0·z-index:1000` 이라 `z-index:3` 인 머리줄이 그 **밑에 깔린** 것이다. nav 높이가 폭에 따라 67→97→121→169px(1920/992/900/768 실측)로 변해 고정 오프셋이 불가능 → JS 실측을 `--wb-nav-h` 로 흘리고 CSS 네 자리가 문다(`resize` + `ResizeObserver`). ③ **도크 머리말 ↔ 링크가 여는 집 불일치** — 집이 둘인 주문에서 머리말은 첫 집, `워크벤치에서 열기` 는 나중 집이었다(스테이징 주문 4485 실사례, U-1 실조회로 `relation=REPAY` 확정). 머리말이 집 번호를 전부 말하고 링크가 여는 집을 지목하며, 두 값은 **같은 행**에서 끌어온다. ④ **붙이기 중복 이력 정책 확정** — 두 번 눌러도 상태가 안 바뀌면 `OrderEvent` 를 안 쌓는다(되돌린 뒤 재붙이기는 다시 남는다). 누가 몇 번 눌렀는가는 `log_access` 가 전량 보관. 검증: integrations 600 passed(신규 5), smoke 324 exit 0, 운영 수집 실측(20 links·7집·실패 0) | foms/services/integrations/naver_commerce/{dock,promotion}.py, foms/web/admin/naver_ingest.py, static/css/admin/naver-workbench.css, static/js/admin/naver-workbench.js, static/js/orders/erp-naver-dock.js, static/css/orders/erp-naver-dock.css, templates/admin/naver_workbench.html, templates/orders/partials/erp_order_js.html, docs/guides/NAVER_INGEST_SETUP.md | 6ed045c9 |
| 2026-08-25 | **네이버 취소 알림을 집 단위로(세부옵션 4건 = 알림 4건 결함)** — 운영 알림함에 같은 취소 문장이 4건 쌓였다(상품주문번호 …111391/401/411/421, `group_key` 동일). 네이버는 주문 1건을 세부옵션마다 다른 상품주문번호로 쪼개 주는데 화면(확인 큐·이력)만 `group_key` 로 집을 세고 클레임 알림은 링크 단위로 남아 있었다. `refresh_claims` 가 링크마다 보내던 것을 대기 목록에 모았다가 **집 + 상태 + 수신자**로 묶어 1건씩 보낸다(같은 집이라도 담당자가 다르면 분리 — 재결제로 집이 주문 2건이 되는 경로). 문안은 대표 번호 + '외 N건', `notified_status` 는 집의 모든 링크에 남긴다(한 링크만 남기면 다음 스윕이 나머지로 같은 알림을 다시 만든다). 검증: 신규 계약 4건(수정 전 2 red 확인)·네이버 통합+팬아웃 589 passed·smoke 324 exit 0 | foms/services/integrations/naver_commerce/claim_watch.py, tests/services/integrations/test_naver_claim_group_notification.py(신규) | 41810b27 |
| 2026-08-25 | **도면+계약서 한 링크(kind=bundle) + 지방 주문 안내 연락처 본사 CS** — ① 링크 두 개를 보내면 고객이 뭘 눌러야 할지 헷갈린다 → `bundle` 종류 신설. 계약서 쪽은 estimate 와 같은 발급 시점 동결이고, 스냅샷 없는 bundle 링크는 열람에서 503(조용히 도면만 보여주지 않는다). 열람 페이지는 도면·계약서 본문 **파셜을 공유**해 단독 링크와 문구가 갈리지 않게 했다. 인쇄는 계약서만(도면은 presign 이미지). 알림톡 드롭다운·모바일 시트에 '도면 + 계약서 (한 링크로)' 항목, 발송 흐름은 kind 만 넘긴다. ② 지방 주문은 도면 컨펌을 본사 CS 가 받는데 안내에는 현장 담당자 번호가 나가고 있었다 → 담당자 표기 '고객센터' + 연락처 본사 대표번호(라홈 1566-0792 / 라홈 외 발주사 1566-0703), 카톡 실패 시 문자 발신번호도 같은 번호(`regional_cs` 단계가 담당자 앞에). env `FOMS_REGIONAL_CONTACT_PHONE_{LAHOM,HAUD}`. ③ 버튼 2개짜리 새 알림톡 템플릿 2종 심사 제출(라홈 `KA01TP260825021747177Iu2C2ykuJfS` · 하우드 `KA01TP260825021755111MLTAvg2dLLn`) — 승인되면 도면·계약서 토큰 2개를 한 통에 싣는 경로로 전환. 검증: 공유 도메인 76 passed, smoke 324 exit 0 | foms/services/order_share.py, foms/api/share.py, templates/orders/{share_bundle_view,partials/share_drawing_body,partials/share_estimate_body}.html(신규), templates/orders/partials/{erp_order_tab,erp_alimtalk_picker_modal,erp_order_js}.html, static/js/orders/erp-share.js, static/css/orders/foms-share-view.css | 932eb242·bc6bf0ad·70b94b14·703e61e3 |
| 2026-08-24 | **알림톡 발송 흔적 칩(T15) — 보낸 주문에 '보냈다'가 화면에 남는다** — 지금까지는 드롭다운 → 항목 → 모달을 거쳐야 마지막 발송이 보였다. 확인 비용이 커서 아무도 확인하지 않고 눌렀고, 같은 고객에게 예약 안내가 두 번 나가는 걸 막을 방법이 화면에 없었다. 발송 실패도 조용히 지나갔다. ① 알림톡 버튼 아래 상시 칩 4상태(`✓ 예약 안내 보냄 · MM-DD HH:MM · 보낸사람` / `✓ 문자로 보냄`(주황) / `✗ 발송 실패 · 사유` / `아직 안 보냄`(점선)). 이력은 이미 `structured_data` 로 실려 오므로 **렌더에 서버 왕복 0**, 발송 직후 갱신도 send-manual 응답의 `last` 로 한다. 실패 사유 문구는 발송 모듈 맵 재사용(사본 금지). ② 칩 클릭 = 발송 이력 패널 — `GET /api/orders/<id>/events` 에 `event_type` 필터·`created_by_name`·한글 라벨 추가(이름은 id 집합 1회 조회, N+1 금지). ③ 채널 확정 — 접수 시점 type 은 항상 `ATA` 이고 카톡이 실패해야 벤더가 `SMS`/`LMS` 로 바꾸므로, 발송 1분 뒤 `POST /api/kakao/alimtalk/confirm-channel/<id>` 가 벤더에 **한 번** 묻는다(멱등: 확정된 건은 벤더 미호출, 조회가 성공하면 결과가 비어도 `channel_checked_at` 을 남겨 무한 재조회 차단). 실행은 web 프로세스 — `SOLAPI_*` env 가 web 에만 있고 sidefx worker 엔 handler 가 `STORAGE_DELETE` 뿐이다. ④ 이력 레코드에 `sent_by_name` 을 발송 시점에 굳힌다(읽기 시점엔 id 를 이름으로 바꿀 기회가 없다). **착수 중 잡은 결함 2건**: 채널 확정이 벤더 왕복 사이에 들어온 저장을 덮던 자리(쓰기 직전 재조회, 재현 테스트 red→green), 칩 스크립트를 페이지 스코프로 실어 태블릿(셸 v3 경로)에서 통째로 빠지던 자리(전역 1곳으로 이동). 검증: 신규/확장 테스트 91+, smoke 324 exit 0, CI 4/4 green, 스테이징 실주문 렌더·이력 패널·라우트·모바일·태블릿 확인. **잔여**: 실발송 1건 E2E(실제 발송이라 사용자 승인 필요)·태블릿 실기기 육안 | foms/services/kakao_alimtalk.py, foms/api/kakao/__init__.py, foms/api/events.py, static/js/orders/erp-alimtalk-trace.js(신규), static/css/orders/erp-alimtalk-trace.css(신규), templates/orders/partials/erp_alimtalk_trace_modal.html(신규), static/js/foms/tablet-measure-form.js, templates/partials/shared/layout_scripts.html 외 | 7ec9a0fb·57100869·f31ccc5e·d91a993c·16184465·e689382c·431e1fc0 |
| 2026-08-23 | 대시보드 캡 결함 2건 production 승격 — 캡 뒤 파이썬 분류 구조로 화면이 조용히 비던 문제. legacy 보드 3종(수도권 실측 알림 444·449건 잘림 실측) 캡 2000+발동 로그, 실측 300건 상한 고지 | measurement/dashboard.py, measurement_read_model.py, erp_dashboard_search.py, dashboard_main.html 외 2개 | d5b44d87 (PR #139) |
| 2026-08-23 | 도면 작업실 모집단 SQL 선스코프 — 운영 28건 중 27건 실종 해소(22.5ms→0.92ms), 부분 인덱스 3종+병합 리비전 merge_prod_drawq | drawing_workbench_read_model.py, web/drawing/workbench.py, drawqueue_00 외 2개 | 89b9ca7f (PR #136) |
| 2026-08-23 | **모바일 주문 마법사: 제품 카드 번호·삭제 + 주문 구분(지방주문·라홈시스템·긴급)·상차일** — ① `+ 제품 추가`가 1번 카드를 통째 복제하면서 헤더 `#1 제품 1`까지 물려받아 카드가 몇 개든 전부 "제품 1"로 보였다. 카드마다 라벨을 손대는 대신 **DOM 순서를 정본**으로 삼는 `renumberProductCards()` 한 곳에서 `data-product-index`·`#N`·기본 타이틀·첨부 input id 를 재매김한다(복제·삭제·초안 복원 공통 경로). 제품명 입력 시 기본 타이틀도 closure 인덱스가 아니라 DOM 속성을 읽어 삭제 후 순번이 어긋나지 않는다. ② 카드 삭제 수단이 아예 없었다 — 헤더에 ✕(카드 2개 이상일 때만 노출), 확인창 → 삭제 → 재번호 → 금액 재계산 → 초안 저장. 헤더 클릭 위임과 겹치므로 접기/펴기 토글에서 삭제 클릭을 제외. ③ 3단계(일정·담당)에 주문 구분 칩 3종. 지방주문은 `Order.is_regional`+`construction_type` **컬럼**(구분 미선택은 ERP 저장 경로와 동일하게 400), 라홈시스템·긴급은 `structured_data.flags`(긴급은 `sync_erp_flat_columns` 가 `erp_urgent` 로 동기화). 지방주문 상차일은 지방 대시보드 "상차 예정 알림"이 읽는 `shipping_scheduled_date` 컬럼에 넣고, 비지방이면 남은 값을 잘라낸다. ④ 추가 버튼으로 만든 카드는 접힘이 기본이라 곧바로 입력할 수 없었다 → 즉시 펼치고 제품명 포커스(초안 복원 경로는 접힘 유지). 검증: 마법사 계약·API 테스트 22건, smoke 324, CI 4종 green, dev·운영 실브라우저(360x772) 확인 | templates/orders/wizard/{step2_products,step3_schedule,wizard_shell}.html, static/js/foms/{wizard,product-item}.js, static/css/components/{foms-wizard,foms-product-item}.css, foms/api/erp_order_draft.py, tests/domains/{test_erp_order_draft_wizard,test_foms_p1_remaining}.py | 2b4c4518·3925fca6·PR #135 |
| 2026-08-21 | **실측 PUSH 신설(채널톡 229923) + 모바일 알림톡 선택 시트** — ① 영발 PUSH 와 같은 본문·같은 실측 첨부를 실측방으로 한 번 더 보내는 `push_kind='measure_room'` 추가(그룹 `CHANNEL_GROUP_MEASURE_ROOM`, 미설정 시 229923 폴백). **이력 키는 분리**(`channeltalk_push_measure_room`) — 영발을 보냈다고 실측 PUSH 가 재전송으로 취급돼 변경 사유를 요구하면 안 된다. PC 카드 헤더 버튼(영발 옆) + 모바일 PUSH 선택 시트 4번째 항목. 폼 PUT 이 서버 소유 이력을 덮지 않도록 보존 키 3곳(서버 2·클라 1) 등재. ② 모바일 알림톡 dropup 제거 — 좁은 폭에서 메뉴가 액션바를 덮어 터치가 막혔다. PUSH 와 같은 선택 시트(`erp-channel-push.css` 재사용)로 통일하고, 시트 선택지는 PC 드롭다운과 같은 클래스를 그대로 달아 기존 위임 핸들러가 처리한다(시트가 완전히 닫힌 뒤 replay 클릭 — 모달 2개가 겹쳐 backdrop 이 남는 것을 막는다). 검증: 신규 계약 4건 + 채널/ERP 도메인 635 + smoke 323 green, state writer 인벤토리 라인시프트 재생성 | foms/api/channel/channel_integration.py, foms/services/channel_policy.py, foms/api/erp_orders_structured.py, foms/services/order_copy.py, templates/orders/partials/{erp_order_tab,erp_order_tab_mobile,erp_channel_push_picker_modal,erp_alimtalk_picker_modal(신규),erp_order_js}.html, static/js/orders/{erp-order-shared,erp-channel-push-confirm,erp-alimtalk-send}.js, static/css/orders/erp-channel-push.css | 99a634ab2 |
| 2026-08-20 | **NOTIF-ROLE-01: 역할 대상 알림(`target_role`) — 관리자 팬아웃 SSOT 복원** — 관리자 전원에게 가는 알림이 수신자 수만큼 별개 `Notification` row 로 복제돼 왔다(알림 SSOT = 공유 row 1건 + 수신자별 state). 에스컬레이션이 그 복제본을 다시 관리자 수만큼 물면서 N² 로 불어났다(스테이징 실측: 클레임 148건 → 에스컬레이션 1073건). 수정: `notifications.target_role` 컬럼 + `target_type='ROLE'` 수신 경로 신설, `resolve_recipients_for_notification` 에 역할 해석 추가(넓은→좁은 우선순위 ALL<ROLE<TEAM<MANAGER<USER), `account_requests`(운영 반영 코드)·`claim_watch`(네이버 클레임) 를 사건 1건 = row 1건으로 전환. **읽기 경로 무변경** — 목록·배지가 전부 `notification_user_states` 조인이라 새 대상 종류를 몰라도 된다. claim_watch 반환값은 state 수가 아니라 **수신자 수** 유지(`refresh_claims` 의 `if sent:` 가 중복 억제 게이트 — 0이면 5분마다 재알림). 실측 대조: ROLE 원본 1건 → 에스컬레이션 3건(관리자 1인당 1건), 전환 전 형태는 같은 조건 9건. 마이그레이션 `notifrole_00`(down=`naver_relation_00`, 단일 head, create_all 왕복 검증). 멀티에이전트 4팀 병렬 실행 + 총감독 전건 재검증(diff 직접 확인·테스트 직접 실행) | models.py, migrations/versions/notifrole_00_notification_target_role.py, foms/services/notifications/recipients.py, foms/services/security/account_requests.py, foms/services/integrations/naver_commerce/claim_watch.py, tests/domains/*(신규 3) | 99a634ab2 |
| 2026-08-20 | **에스컬레이션 알림 본문 채움 + 원본당 1건 중복 억제 (운영 승격 완료 `67ecaff3`)** — 미확인 긴급 알림 스윕이 ADMIN 알림함에 제목만 같은 빈 알림을 무더기로 쌓던 문제. 원인 2개: ① `_create_escalation_notification` 이 `message=None` 이라 목록 렌더가 `<p>` 자체를 안 그림(무슨 알림이 방치됐는지 알 수 없음) ② 생성 단위가 원본 알림이 아니라 `NotificationUserState` 라 (state 수 × 대상자 수) 만큼 중복. 수정: `_build_escalation_message`(담당자·미확인 경과·원본 제목·원본 본문 160자 요약·주문번호) + `_already_notified_user_ids`/`_fresh_targets`(통보 대상 id 를 escalated/operator_escalated 이벤트 `metadata_json.escalation_target_user_ids` 에 남겨 이후 스윕·단계에서 제외). push/realtime 은 Spec D2 대로 generic 유지(`_build_payload` 는 `message` 미참조 — 잠금화면 노출 없음). 계약 테스트 5건 신규(수정 전 4 red 확인). **남은 상류 문제**: 스테이징 에스컬레이션 1073건의 원천은 `naver_commerce/claim_watch.py` 가 클레임 1건마다 ADMIN 수만큼 `is_urgent=True` 알림을 **개별 Notification 으로** 만들고 아무도 ack 하지 않는 구조 (claim 1건 → 알림 4 → 에스컬레이션 16, 본 수정 전 32). 원본 1건+fan_out 다중 state 로 바꾸거나 수집 알림의 긴급 해제가 필요 | foms/services/notifications/escalation.py, tests/domains/test_notification_escalation.py | 8a76330c·PR #120 |
| 2026-08-20 | 백업 상황판(F7) + 복원 리허설 절차서(F4). **상황판이 백업 보관소를 직접 읽지 않는다** — 그러려면 웹 앱이 백업 버킷 열쇠나 Railway 계정 토큰을 들고 있어야 하고, 앱이 털렸을 때 백업까지 열리면 백업의 목적이 지워진다. 백업 쪽이 성공했을 때만 HMAC 서명 심박을 보내고 앱은 그 기록만 읽는다. `/admin/backup-status`: ok/stale(30시간 초과)/**missing(심박 없음도 빨강 — '한 번도 성공한 적 없음'은 정상이 아니다)**. `POST /api/ops/backup-heartbeat`: 서명이 인증, 비밀 미설정 시 fail-closed(열어 두면 누구나 '백업 정상'을 써넣어 상황판을 속인다). 심박도 감사에 남긴다(`OPS_BACKUP_HEARTBEAT`) — 인벤토리 재생성으로 UNAUDITED 를 늘려 게이트를 무력화하지 않기 위해 audit coverage 100% 유지. manifest 2종 exempt 등재. 종단검증: 오서명 401·정서명 200 확인 후 **프로브 심박 삭제**(가짜 초록 방지). Railway 비밀은 dev=재배포 동반, prod=`skipDeploys`로 재기동 없이 심어만 둠(승격 배포 때 적용). F4 는 `RESTORE.md` §8 을 실행 가능한 분기 리허설로 교체 — 자동화하지 않는 이유(age 개인키를 CI 에 두면 백업 전량 복호화 권한이 상시 온라인)와 `--clean --if-exists` 를 운영 DSN 에 붙이면 운영이 날아간다는 경고 포함 | foms/services/backup_status.py, foms/web/admin/backup_status.py, templates/admin/backup_status.html, foms/platform/blueprints.py, (별도 저장소) backup.py·backup-daily.yml·RESTORE.md | 5468b910·4788044·a0dcce5 |
| 2026-08-20 | RESTORE-GUI-01 T1: 변경 이력에서 **필드 한 건 되돌리기**. 2026-08-14 사고 복구를 사람 손+CLI 로 했는데 근거 원장(`order_field_changes`)은 이미 운영에 있고 그걸 화면에서 집어 되돌릴 수단만 없었다. 요청은 **원장 행 id 하나만** 받는다 — 경로도 값도 서버가 기록에서 읽으므로 임의 `structured_data` 경로 쓰기가 성립하지 않는다(그 라우트는 보안 위험으로 이미 제거됐고 되살리지 않는다). 거부 4개: 화이트리스트 밖 400 / 원장 값 절단(`…`) 400(잘린 값 되쓰기 = 복원이 아니라 훼손) / 현재값이 문자열 축이 아니면 400(원장은 정규화 문자열만 담아 bool·dict 원형 복원 불가) / 현재값≠원장 after 면 409(lock 획득 후 재판정으로 한 번 더). 쓰기 본체는 `field_restore.apply_restore` — mutation writer 인벤토리가 **파일 단위** 분류라 라우트 파일에 두면 기존 EXTERNAL 과 묶여 EXTERNAL 이 는다(allowlist CANONICAL 등재). 화면은 기존 변경 이력 탭(ORDER-DIFF-02)에 얹었다 — 서버가 `restorable`/`reason` 을 실어 줘 못 되돌리는 행은 버튼을 켜지 않는다. **첫 푸시가 CI red**: 신규 mutation 은 write guard·policy manifest **2종** 등재가 규칙인데 1종만 했고 `ORDER_FIELD_RESTORED` 업무 라벨도 빠졌다 (pre_push_smoke 서브셋에 그 두 게이트가 없다). 검증: 신규 10건 + 관련 35건, smoke 323, CI 4종 green | foms/services/orders/{field_restore,structured_diff,order_field_change_writer}.py, foms/api/events.py, static/js/orders/order-change-history.js, templates/orders/partials/edit_order_body.html, docs/harness/{write_guard,order_mutation_policy}_manifest.json | 3d3c2f61·d887ef86 |
| 2026-08-19 | 오프사이트 백업 전면 실패 복구(`lahomsystem/foms-ops-backup`): 2026-08-13 저장소 개설 후 스케줄 6회가 **전부 실패, 성공 이력 0** — 매일 실패 메일만 오고 아무도 몰랐다. 원인 2개. ① pgdg `postgresql-client-17` 은 `/usr/lib/postgresql/17/bin` 에만 설치되고 `/usr/bin/pg_dump` 심볼릭 링크는 러너 기본 16 그대로라 `shutil.which("pg_dump")` 가 16 을 집어 버전 게이트에서 죽었다 → `GITHUB_PATH` 로 17 bin 고정(`pg_restore` 도 함께). ② R2 시크릿 4종(`BACKUP_R2_BUCKET`/`ENDPOINT`/`ACCESS_KEY_ID`/`SECRET_ACCESS_KEY`) 미등록 → 버킷 `foms-backup` 전용 Object R&W 토큰으로 등록(앱 첨부 버킷과 분리). **첫 성공**: dump 5.3MB·TOC 929건·age 암호화·`db/2026/08/*.dump.age` 업로드. 재발 차단 3종: `preflight` 서브커맨드(환경변수·도구·pg_dump 버전을 덤프 **전에** 확인 — 운영 수백 MB 읽고 나서 죽는 낭비 제거), `db/LATEST.json` 심박(업로드 성공 후에만 시각·크기·sha256·TOC 기록 → 워크플로가 **아예 안 돌아도** 밖에서 감지), 실패 시 `backup-failure` 이슈 자동 등록·연속 실패 누적. 검증용 `workflow_dispatch inputs.mode=smoke`(`--schema-only --local-out`, 운영 부하 없이 배선 점검). Railway 층 실측: 운영 postgres Daily 12:00 UTC·보존 6일 정상 동작, **redis·FOMS-DEV postgres 는 스케줄 없음(백업 0건)**. 8/14 AS 증발 사고 조사용 스냅샷 2건(8/13 12:00 직전·8/14 12:00 직후) `volumeInstanceBackupLock` 으로 만료 잠금 — 8/13 건은 당일 12:00 UTC 만료 예정이었다 | (별도 저장소) .github/workflows/backup-daily.yml, backup.py | 9220019·59b377f |
| 2026-08-18 | AS-AXIS-01: AS 대시보드 술어를 status에서 AS 축 투영으로 교체 — 8/14 사고의 **구조적 원인** 제거. 정본은 `as_lifecycle`인데 목록 술어가 파생값 `orders.status` 단독이라 아무 write나 덮으면 목록이 증발했다. 플랫 투영 `as_axis_status`(부분 인덱스, 마이그레이션 `asaxis_00`) + 유도 SSOT `derive_as_axis_status` + `sync_erp_flat_columns` 동기화 + 백필/드리프트 감사 도구. 술어 교체 4곳(탭 2·모집단 3·지도 1). **실측으로 바뀐 설계 2건**: ① 날짜 폴백 제거 — 백필하니 미완료 54→67로 늘었고 늘어난 건 전부 as_received_date만 남은 옛 주문(운영 18건, 3~5월, AS 이벤트 0)이라 화면 무변동을 택함(재백필 후 구/신 53/53·426/426·집합차 0) ② **투영 암묵삭제 금지** — 2단 배포 후 스테이징 사고 재현에서 레거시 AS 주문이 여전히 증발: 일괄 상태변경이 sync를 지나는데 lifecycle 없는 행은 근거가 status뿐이라 재유도하면 None→투영 삭제. 유도값 있을 때만 갱신하도록 수정. 곁가지 근본수정: `blueprint_projection._legacy_orders`가 전체 컬럼 SELECT라 새 컬럼 추가만으로 alembic 체인 왕복이 깨져 `load_only`로 축소. 검증: 스테이징 실브라우저 사고 재현(강제 완료 후에도 AS 완료 탭 유지·`as_axis_status` 보존), 드리프트 507건 0, PG 레인 737 green(체인 왕복+인덱스 가드), AS 도메인 845, smoke 323 | models.py, migrations/versions/asaxis_00_as_axis_status.py, foms/services/orders/state_axes.py, foms/services/erp_sync_columns.py, foms/services/as_dashboard_helpers.py, foms/web/cs/as_dashboard.py, foms/services/map_snapshot.py, foms/services/orders/blueprint_projection.py, tools/ops/{backfill_as_axis_status,audit_as_axis_drift}.py, tests/domains/test_as_axis_projection.py, tests/postgres/test_scale_as.py | 29fb31b5·0a533944·e061beb7 |
| 2026-08-15 | AS 대시보드 증발 사고 복구 + 재발 방지 + 복구 도구화: 운영 일괄 완료처리(06:29~06:34 UTC, 주문 일괄 상태변경 239건 + 단계 강제 변경 314건)가 AS 접수/완료 주문의 `status`를 COMPLETED로 덮어 **AS 대시보드에서 55건 증발**(기록은 무손상, 목록 술어가 `Order.status` 단독). 운영 DB 직접 복구 55/55(근거: 감사행 before 35건 + AS 이벤트 유도 20건, 스냅샷·감사행·롤백 스크립트 보존). 가드: `as_overlay_status()` SSOT(**status 기준 — AS 주문의 workflow.stage는 MEASURE로 남아 stage로는 못 잡는다**), 일괄 경로 2종 AS 기본 제외 + 제외 목록·경고 반환(`include_as: true` opt-in만 포함, 단건은 종전대로), `STAGE_OVERRIDE` payload에 `from_status`·`as_overlay_cleared` + `ORDER_STATUS_CHANGED` 감사행 신설(복구 근거 공백 해소), 프런트 제외 고지 alert·?v 20260814d. 스테이징 실브라우저 실측(API 2종 차단 + UI 실조작 시 AS완료 유지) 후 운영 승격 PR #112(`63737e91` 배포 SUCCESS). 이어 복구 절차 도구화: `data_doctor.py`(inspect→plan→apply→rollback, confidence 4층 exact>logged>event>inferred, 기본 dry-run·현재값 불일치 행 skip·상한 500)와 `railway_pitr.py`(백업 조회·시점 fork·drop, 운영 볼륨 덮어쓰기 미구현). 운영 백업 실태 확인: Daily 12:00 UTC·**보존 6일** → 통짜 시점복원은 사고 후 정상 업무를 지우므로 금지, fork 후 선별 반영이 정본 | foms/services/orders/stage_override.py, foms/api/orders/{stage_override,status}.py, static/js/orders/{erp-stage-override.js,dashboard/erp-dashboard-detail-dom.js}, tools/ops/{data_doctor,railway_pitr}.py, docs/guides/DATA_INCIDENT_RECOVERY.md, tests/domains/{test_as_bulk_status_guard,test_data_doctor_plan}.py | b7c85bb8·fbeb07b8 |
| 2026-08-14 | 폰에 광폭(≥992) 전용 데스크톱 큐 미전송 — 화면 힌트 쿠키 `foms_scr`: 폰은 물리 화면 긴 변이 992 미만이라 **세로든 가로든 `min-width:992px` 미디어쿼리에 구조적으로 도달 불가**인데 그 조건에서만 픽셀을 갖는 데스크톱 작업 큐를 매 요청 받고 있었다. pre-paint 인라인 부트가 `max(screen.width, screen.height)` 를 쿠키로 남기고(SSOT 사본 static/js/runtime/foms-screen-hint-boot.js) 서버 `wants_wide_only_surfaces()` 가 판정 — **screen 은 기기 고정 특성**(창 조절 무관·회전해도 최댓값 불변)이라 pointer 힌트와 같은 계열이고 리사이즈 재요청 배선이 불필요하다(뷰포트 innerWidth 판정과의 결정적 차이). 실측 computed style 3점(390/844/1600)으로 대상 선별: 주문 `.foms-shell-desktop-only` h=0/0/1146 · 시공 `.foms-construction-legacy-queue` h=0/0/815 · 생산 `.foms-production-legacy-queue` h=0/0/815. **AS 데스크톱 표는 제외 — `d-md-block`(768 기준)이라 폰 가로 844 에서 실제로 표시된다(390 none / 844 block h=240 실측).** 같은 패턴 복사 사고를 막으려 부재를 계약으로 고정. 스테이징 결과 주문 398.7→119.2KB(-279.5, -70.1%)·시공 534.7→406.9(-127.8)·생산 534.0→414.4(-119.6) = **폰 526.9KB**, AS 725.1 무변화(의도대로). 안전 폴백: 쿠키 미설정·빈값·비숫자·0·음수·소수 전부 렌더(첫 요청은 항상 폴백). 경계 992 는 렌더(>=). 검증: 폰390 4탭 desktopQ gone·모바일 h=1000~24729·콘솔 0·4xx/5xx 0, PC1600 3탭 desktopQ h=773~815·50행·콘솔 0. 계약 t0 5종+feature_flags 15종(경계·폴백 전수)+PTC runtime JS 인벤토리 등재. smoke 322 green | static/js/runtime/foms-screen-hint-boot.js(신규), templates/partials/shared/layout_head.html, templates/{construction,production}/partials/dashboard_body.html, templates/orders/partials/dashboard_main.html, foms/services/{feature_flags,context_processors}.py, tests/domains/{test_tablet_shell_t0_contract,test_feature_flags}.py, tests/contracts/runtime/test_ptc_physical_exactness.py | 61af5bd8 |
| 2026-08-14 | 주문 대시보드 태블릿 표면 미전송(운영 승격 PR #95 `9f9b1ade`): PR #84·#85 가 시공·AS·생산·도면·완료의 `pointer: coarse` 전용 표면을 `coarse_pointer_surfaces` 로 막았는데 **주문 2곳(`tablet_dashboard_topbar.html`·`tablet_workqueue_grid.html`)이 누락**돼 있었다. 두 표면 모두 foms-tablet-landscape.css 단독 소유·base `display:none`·표시 규칙은 `(min-width:992px) and (orientation:landscape) and (pointer:coarse)` 한 곳뿐(`data-foms-shell` 강제 해치 파일 전체 0건 확인) → 마우스 기기엔 죽은 마크업. PC 그리드 은닉도 같은 coarse MQ 소속이라 서버 스킵이 blank 를 만들 수 없다. **셸 variant 무관이라 v2(운영 전원)·v3 양쪽 실효** — 이번 다이어트에서 운영에 실제 효과가 나는 첫 절감(v3 죽은 표면 건은 운영 `FOMS_SHELL_V3_ENABLED` 미설정이라 no-op). 실측 주문 fragment 398.7→351.5KB(-47.2KB, -11.8%). **측정 방법 정정: `X-FOMS-ERP-SHELL` 헤더만으로는 전체 페이지가 온다 — `get_erp_shell_view_mode` 가 헤더+`view=fragment` 를 둘 다 요구**(그간 보고된 fragment 크기는 전부 전체 페이지 값이었고 감소량만 유효). 검증: dev 실로그인 QA — 8탭 실제 탭스왑(전부 실데이터·콘솔 0)·상세 lazy fetch 200(detail-payload 128ms+attachments)·모바일 390x844 v3 6탭(fallback none·bottomnav fixed·4xx/5xx 0)·검색 243→56건·쿠키 fine 미전송 vs coarse 렌더 폴백 확인. 계약 test_tablet_t2_contract.py 2건 게이트 문자열 갱신(pre_push_smoke 서브셋 밖이라 직접 실행). CI 4/4 green. 승격 PR perf-gate 1차 red 는 도면 dTTFB 135>124 였으나 wire 17.5KB 불변·재실행 60 으로 타이밍 드리프트 확정 | templates/orders/partials/dashboard_main.html, tests/domains/test_tablet_t2_contract.py | 47bb01de |
| 2026-08-13 | v3 코호트의 죽은 모바일 v2 표면 미전송(6개 대시보드): 시공 인라인 style 가설을 실측으로 먼저 검증해 **기각**(5탭 합쳐 21.0KB=0.9%, 시공 552회/31.3KB는 그 표만의 특이 케이스) → 표면별 바이트 재측정에서 진범 확인. v3 셸은 `.fos-v3-mobile-scope`+`.fos-v3-desktop-fallback` 을 둘 다 렌더하는데, fallback 이 **보이는** 조건(`foms-mobile-v3.css` 결함G 게이트의 여집합) = 광폭(≥992 landscape\|fine\|none) = `foms-mobile-v2-surfaces-hide.css` 가 `.foms-mobile-v2-dashboard` 를 `display:none !important` 로 끄는 조건과 **정확히 일치** → v3 사용자에겐 어떤 뷰포트·방향·포인터에서도 불가시. **폭이 식에서 소거되므로 태블릿 표면 건과 같은 성질**(AS 의 `max-width` 게이트와 달리 리사이즈 재요청 배선 불요). 도면은 소유 CSS(`foms-drawing-mobile-card.css`)가 v2 전용 번들이라 v3 엔 미로드 — 광폭에서 자식 전부 숨어 섹션 h=0 실측. 검증: 배포 전 스테이징 computed style 베이스라인(1600=`none`, 900=조상 none 이라 norect) → 배포 후 6대시보드×2뷰포트 재측정(1600 fallback h=1045~2913·v3 h=0 / 900 fallback h=0·v3 h=1200~7862, 죽은 표면 gone, console 에러 0). 실측 감소 생산 178.0KB(-29.1%)·시공 127.8KB(-26.6%)·도면 66.9KB(-20.6%)·출고 29.7·실측 19.1·주문 14.3 = **435.8KB**. 계약 `test_shell_v3_contract.py` Contract 11(v3 6라우트 마커 부재 + v2 코호트 유지 대칭, 게이트 되돌리면 red 확인). 기존 계약 266 green·pre_push_smoke 322 green. AI_STATUS 상단 예산 초과(4073>4000, 타 세션 상속 red)도 동반 해소 | templates/{construction,production}/partials/dashboard_body.html, templates/{orders,measurement,shipment}/partials/dashboard_main.html, templates/drawing/partials/workbench_dashboard_body.html, tests/domains/test_shell_v3_contract.py | 4819c280 |
| 2026-08-13 | AS 주문 식별자 행 컨테이너 일원화: AS fragment 한 장에 data-order-id 가 1,271개(행당 22개 × 57행 × 2표면) 실려 있었고 값은 전부 행 컨테이너와 동일했다. 위험 지점=표면간 상태 동기화가 `.as-pending-btn[data-order-id=x]` 같은 **전역 속성 선택자** 10곳이라 자손 속성을 그냥 지우면 다른 표면이 조용히 안 따라 바뀐다. 순서: ① 읽기 전환(orderIdOf=자기 dataset→closest 폴백, findInOrderScopes=컨테이너 스코프 조회로 같은 집합 확보) ② 방출 제거(자손 전부, 단 쓰기 API 가 통째로 돌려주고 detached 파싱되는 루트 .as-timeline/.as-tl-cell/.as-rchart/.as-construction-worker-list 와 quick-add form 은 유지). 결과 1271→228, 마우스 사용자 wire 36,612→31,636B(-13.6%)·raw -21.9KB, CI perf-gate AS wire 51,051→46,315B dTTFB 99 PASS. 검증: 계약 테스트 신설(행 안 허용 루트 외 방출 0 + 컨트롤 7종 잔존) + 배선 테스트 3건 새 계약 갱신 + 스테이징 실브라우저 (미결 토글 양 표면 동기·서버 반영·원복, 도면 체크 confirm·영속, 방문일 저장 양 표면·원복, 타임라인 확장·확장 안 역참조, 가능시간 팝오버 forOrder, 사진 모달, 모바일 카드 지연로드, 모바일 카드발 토글) console 에러 0. ?v 20260810a→20260813a | static/js/cs/as-dashboard.js, templates/cs/partials/{as_dashboard_body,as_mobile_order_card,as_card_macros,as_round_chart}.html, tests/domains/{test_as_row_order_id_scope,test_as_timeline_wiring}.py | dd507a46 |
| 2026-08-13 | AS fragment dTTFB 예산 초과(181 > 168) 근본 해소: 페이로드는 wire 51K 로 고정이라 코드 회귀가 안 보였음 → 스테이징 실측 분해로 render 17ms vs 헤더 106ms 확인(압축 무죄 — identity 99ms), EPT-B7 `phase()` 계측 신설로 지출처 확정(tab_counts=27·list_query=21·row_display=49 중 rd_sanitize 18/rd_timeline 17). 수정 2건: ① 같은 모집단을 세던 `_count_cases` 2회를 SUM(CASE) 단일 스캔으로 병합('total'은 'incomplete' 재사용) ② 순수 함수 `sanitize_as_content_html` LRU 메모이즈(2048칸·입력 8KB 상한, 초과는 캐시 우회) — 행 100 × 2필드 BeautifulSoup 재파싱 제거. 검증: 스테이징 웜 실측 rd_sanitize 18→0·tab_counts 27→15·dHDR 129→96, CI perf-gate 심판석 AS dTTFB 96 PASS(전 경로 green), AS 도메인 663 + pre_push_smoke 322 green. 예산 168은 불변 유지(공식값 176보다 이미 엄격, 완화 금지) | foms/services/as_content_safety.py, foms/services/as_dashboard_read_model.py, foms/services/as_dashboard_display.py, foms/web/cs/as_dashboard.py, tests/domains/test_as_content_safety.py | a3bdcf9c·cb1834f5·a4f15f97 |
| 2026-08-11 | 대시보드 캐시 무효화 엔진화(MUT-CACHE-01): 라우트별 수동 무효화 구조에서 누락 6경로 확인(단계 강제 변경 310초 지연 스테이징 재현). execute_order_mutation 이 변경 전/후 stage·삭제 표식을 session.info intent 로 기록하고 after_commit 리스너가 소비 — 삭제·복원=broad, 단계 이동=orders+양쪽 탭, 단계 유지=orders+해당 탭, 탭 없는 단계 이동=broad. 롤백 시 intent 폐기·실패는 fail-open 로그. 엔진 미경유 2곳(/edit 폼·order_date_sync 훅 전 family 확대) 보강. 테스트 12종. 검증: 스테이징 즉시 반영 PASS·smoke 322·domains 4277 green, production 승격 PR #79(`24675249`) | foms/services/orders/revision.py, foms/services/common/dashboard_cache.py, foms/services/{app_init,order_date_sync}.py, foms/web/orders/edit.py, tests/domains/test_mutation_cache_invalidation.py 외 | 4702715d |
| 2026-08-10 | 주문 삭제·복원 대시보드 즉시 반영(운영 #4717): 삭제 경로 3곳(단건 delete·벌크 삭제 API·legacy bulk_action)에 대시보드 read-slice 캐시 무효화가 없어 TTL 300초 동안 실측 날짜별 집계에 삭제된 주문이 잔존(패널/리스트 캐시 키가 달라 좌우 숫자 불일치). helper `invalidate_dashboard_caches_after_delete_transition`(broad 7 family + AS 추천 캐시)를 commit 직후 호출, 계약 테스트 6종 추가. 스테이징 E2E 삭제 즉시 집계 2→1·복원 복귀 PASS, CI 4/4 green, production 승격 PR #72 머지(`76b9086c`) | foms/services/common/dashboard_cache.py, foms/web/orders/{trash,listing}.py, foms/api/orders/status.py, tests/domains/{test_delete_trash,test_delete_bulk,test_dashboard_cache_invalidation_scope}.py | aa0b788e |
| 2026-08-10 | 채널톡 AS PUSH 신설(AS 접수 내용+AS 첨부 → AS방 230351): ERP 탭·모바일 PUSH 선택 시트·AS 대시보드 3경로, 본문 조립 서버 SSOT(channel_as_message.py)로 통일 + 모바일 선택 시트 풀스크린 눌림 근본 수정(erp-pro 전역 !important 특이도) + 시공 대시보드 캐시 키 정규화·숫자판 모집단 일치(‘긴급 발주’=시공 기준) + 진단 헤더 2종 신설(EPT-B7-PHASES·DASH-SLICES). 실측: 요약 재계산 추정 88ms vs 실측 27ms — 추정 기반 SQL 이관 중단 판단. production 승격 PR #64·#65·#67·#68 | foms/services/channel_as_message.py, foms/api/channel/channel_integration.py, foms/services/common/{dashboard_cache,ept_b7_profile}.py, foms/web/construction/dashboard.py, static/js/cs/as-dashboard.js, templates/orders/partials/erp_channel_push_picker_modal.html 외 | 340b0064 |
| 2026-08-10 | 세션 자동 기록 | dashboard_cache.py, layout_scripts.html, photo-capture.js 외 2개 | f94f534d |
| 2026-08-10 | 세션 자동 기록 | dashboard.py, test_construction_dashboard_cache_key_sharing.py, test_as_dashboard_attachment_modal.py 외 2개 | 2dc54772 |
| 2026-08-09 | 세션 자동 기록 | tablet-measure-form.js, erp-order-shared.js, test_erp_order_shared_form_scripts.py 외 2개 | 45c53c90 |
| 2026-08-13 | AS fragment dTTFB 예산 초과(181 > 168) 근본 해소: 페이로드는 wire 51K 고정이라 코드 회귀가 안 보였음 → 스테이징 실측 분해로 render 17ms vs 헤더 106ms 확인(압축 무죄 — identity 99ms), EPT-B7 `phase()` 계측으로 지출처 확정(tab_counts=27·list_query=21·row_display=49 중 rd_sanitize 18/rd_timeline 17). 수정 2건: ① 같은 모집단을 세던 `_count_cases` 2회를 SUM(CASE) 단일 스캔으로 병합('total'은 'incomplete' 재사용) ② 순수 함수 `sanitize_as_content_html` LRU 메모이즈(2048칸·입력 8KB 상한, 초과는 캐시 우회) — 행 100 × 2필드 BeautifulSoup 재파싱 제거. 검증: 스테이징 웜 실측 rd_sanitize 18→0·tab_counts 27→15·dHDR 129→96, CI perf-gate 2런 연속 AS dTTFB 96 PASS, AS 도메인 663 + pre_push_smoke 322 green. 예산 168 불변 유지 | foms/services/as_content_safety.py, foms/services/as_dashboard_read_model.py, foms/services/as_dashboard_display.py, foms/web/cs/as_dashboard.py, tests/domains/test_as_content_safety.py | a3bdcf9c·cb1834f5·a4f15f97·0fc3ac24 |
| 2026-07-27 | AS 대시보드 타임라인 개편(T1~T16): as_log append-only 타임라인 + 무상/유상 2단계 판정, 구 contenteditable 2탭 에디터 퇴역, PC/모바일/태블릿 3표면 매크로 SSOT, 시스템 이벤트 실흐름 배선, 검색 as_log 확장, sanitize 미종결 태그 XSS 봉합·2단 상한, lost-update/clobber 서버 보존 가드 — 태스크별 리뷰 루프 + 최종 whole-branch 리뷰 + pre_push_smoke exit 0(253 passed) | foms/services/orders/as_log.py, foms/api/cs/as_orders.py, templates/cs/partials/as_card_macros.html, static/js/cs/as-dashboard.js, foms/services/as_dashboard_display.py 외 | 6473d3b3~876836e1 |
| 2026-07-27 | 세션 자동 기록 | regional_dashboard.html, test_tablet_rail_contract.py, test_tablet_as_compare_contract.py 외 2개 | abddac61 |
| 2026-07-24 | 생산 보드 5차: 필터 바 단순화 — [필터] 토글·필터 접기·상태 select 제거, 공장 select를 검색 앞, 변경 버튼 상시 노출(검색=전체 조건) — pytest 212 passed + 필터 바 렌더 동작검증 | tablet_kanban_body.html, tablet-domain-sheets.js, foms-tablet-production-kanban.css 외 | (미커밋) |
| 2026-07-24 | 생산 보드 4차: 제작 취소 깨끗한 되돌림(rework/hold 정리·제작이력 승인간주) + 라벨 인쇄 제거(통합바 1줄) + 전체화면 토글(크롬 접기·복원버튼·실측 캡90) — pytest 212 passed + Playwright 전체화면·F-1 실측검증, 리뷰 Critical/Major 0 | production/orders.py, production_dashboard_display.py, tablet_kanban_body.html, foms-tablet-production-kanban.css, tablet-domain-sheets.js 외 | (미커밋) |
| 2026-07-24 | 생산 보드 3차: 제작취소 버튼 nowrap + 고정 바 재배치(KPI 5열·pcbar통합·필터접기·열캡 실측265) + 완료 이력(hold_history 보존·무채 배지) — pytest 200 passed + Playwright 재배치·이력 실측검증, 리뷰 Major 1+Minor 3 반영 | production/orders.py, production_dashboard_display.py, tablet_kanban_body.html, tablet_sheet.html, filters_grid.html, foms-tablet-production-kanban.css, tablet-domain-sheets.js 외 | (미커밋) |
| 2026-07-24 | 생산 보드 2차 P4~P8(보류 해제 confirm·사유 가시성 스트립·제작/완료 취소 rollback·보류 KPI/D+n/임박경고·PC 배지) — pytest 187 passed + Playwright 태블릿·PC 21/21 PASS | production/orders.py, production_dashboard_display.py, dashboard.py, tablet_kanban_body.html, tablet_sheet.html, filters_grid.html, tablet-domain-sheets.js 외 | (미커밋) |
| 2026-07-24 | 생산 보드 프로세스 가드 3종(전이 전제조건·보류 게이트·수정 제작 rework) — 스펙 docs/plans/2026-07-24-production-process-guards-plan.md, pytest 159 passed + Playwright 태블릿 28/28 PASS | production/orders.py, dashboard.py, tablet_sheet.html, tablet_kanban_body.html, tablet-production-kanban.js, tablet-domain-sheets.js 외 | (미커밋) |
| 2026-07-23 | 세션 자동 기록 | erp-order-shared.js, test_erp_order_edit_mobile_form.py, test_p1_mockup_structure.py 외 2개 | a8e3b168 |
| 2026-07-23 | 세션 자동 기록 | test_p1_mockup_structure.py, test_tablet_t2_contract.py, test_erp_spec_calc_followup.py 외 2개 | 3c328837 |
| 2026-07-23 | 세션 자동 기록 | test_tablet_t2_contract.py, test_p1_mockup_structure.py, test_erp_spec_calc_followup.py 외 2개 | 31f05379 |
| 2026-07-23 | 세션 자동 기록 | erp_order_tab_mobile.html, test_erp_order_edit_mobile_form.py, foms-form-field.css 외 2개 | 31f05379 |
| 2026-07-23 | 세션 자동 기록 | test_erp_order_edit_mobile_form.py, test_erp_order_shared_form_scripts.py, test_erp_spec_calc_followup.py 외 2개 | 79c2d9a2 |
| 2026-07-23 | 세션 자동 기록 | test_erp_order_edit_mobile_form.py, wizard_shell.html, foms-mobile-surfaces.css 외 2개 | 79c2d9a2 |
| 2026-07-23 | 세션 자동 기록 | wizard_shell.html, layout_head.html, foms-mobile-surfaces.css 외 2개 | 9aad209a |
| 2026-07-23 | 세션 자동 기록 | mobile.css, final-review.md, erp_order_js.html 외 2개 | 78872888 |
| 2026-07-23 | 세션 자동 기록 | erp_order_js.html, wizard_shell.html, layout_head.html 외 2개 | 78872888 |
| 2026-07-23 | 세션 자동 기록 | test_p1_mockup_structure.py, test_tablet_t2_contract.py, layout_head.html 외 2개 | bae12980 |
