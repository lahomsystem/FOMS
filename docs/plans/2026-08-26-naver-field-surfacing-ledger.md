# 네이버 필드 3건을 화면에 올린다 — 진행 원장 (2026-08-26)

> 근거: `docs/guides/NAVER_FIELD_INVENTORY.md` §3 우선순위 1~3
> 앞선 작업: `docs/plans/2026-08-26-naver-deposit-and-refresh-ledger.md`
> 작업 트리: `c:\tmp\nvfix` (브랜치 `tmp/naver-fix-20260825`)

셋 다 **원본 스냅샷에 이미 수집돼 있다**(281/281). 화면이 안 읽었을 뿐이라
**네이버로 나가는 호출은 0**이다.

| # | 필드 | 왜 |
|---|---|---|
| F-1 | `cancelDetailedReason` · `returnDetailedReason` | 고객이 직접 쓴 사유 원문. 실데이터가 `"일시불 재결제 예정"` — 재결제 판정의 결정적 근거인데 담당자가 이 한 줄을 보려고 판매자센터를 따로 열었다 |
| F-2 | `delivery.sendDate` · `deliveryStatus` | 발송처리를 우리가 눌러 놓고 그 결과 시각을 화면이 안 읽는다 |
| F-3 | `remain*` / `initial*` | 한 집에서 일부만 취소되면 "원래 몇 개였는지"를 화면이 말하지 못한다 |

## 진행

| # | 몫 | 상태 |
|---|---|---|
| A | 추출 3종 (`mapping.py`) + 테스트 17건 | **DONE** |
| B | 화면 표시 (pane · CSS · `naver_ingest.py`) + 테스트 12건 | **DONE** |
| C | 검증 → 커밋 → deploy → CI | **DONE** — `535d4244`, CI 4/4 green |
| D | 운영 승격 | **DONE** — PR #158 머지, production `c81d01ed` |
| E | 스테이징 실화면 확인 | **DONE** — 링크 100건 전수. **오탐 2건 발견 → 수정** |
| F | 사유 원문을 R-1 후보 표까지 | **DONE** — `8116a95f` |

## E. 스테이징 실화면 확인이 뒤집은 것 (커밋 `73ca5798`)

승격 전에 `claude_master`(코호트를 잠시 `38,58` 로 열고 **원복 완료**)로 워크벤치 링크
**100건을 전수**로 열어 세 필드가 실제로 무엇을 말하는지 셌다. F-1 은 값어치가 그대로였고
**F-2·F-3 는 표시된 것이 전부 오탐**이었다.

| | 실측 | 무엇이 틀렸나 |
|---|---|---|
| F-3 `원래 N` | 18건 표시 / **진짜 부분취소 0건** | 분포가 값 그대로 82 · `remain 0`(전부취소·전부반품) 18 · 진짜 부분취소 0. 판정이 `initial != remain` 이라 **전부취소가 전부 걸렸다** |
| F-3 표시값 | `지금 14 / 원래 14` | 표의 수량·금액은 네이버 `quantity`·`totalPaymentAmount` 라 클레임이 걸려도 **안 줄어든다**(전부반품된 집도 `quantity 14 · remainQuantity 0`). 즉 **이미 원래 값**인데 그 밑에 같은 숫자를 또 놨고, 그래서 위의 숫자가 잔여인 것처럼 읽혔다 |
| F-2 `어긋남` | 44건 중 41건 | 방향을 세니 네이버 단독 41 · 양쪽 3 · **우리 쪽 단독 0**. 경고의 **100% 가 판매자센터 직접 발송**(정상)이었다 |

**고친 것**

- `_axis_partial`: 부분취소는 `0 < remain < initial` 일 때만. 축별 플래그
  (`quantity_partial`·`amount_partial`)를 함께 내보내 화면이 판정을 다시 만들지 않는다.
- 덧붙이는 값은 `원래 {initial}` → **`남은 {remain}`**. 표의 숫자가 이미 원래 값이다.
- `_dispatch_view.mismatch`: `bool(ours) != bool(naver)` → **`bool(ours) and not bool(naver)`**.
  원 docstring 이 적어 둔 한 방향(유실)으로 좁혔다. 두 열은 그대로 나란히 남아 **사실은
  사라지지 않고 93% 짜리 상시 경고만 사라진다**.
- `.wb-cmp__was` → `.wb-cmp__remain`.

**재배포 뒤 실화면 재확인**: 어긋남 칩 41 → **0**, 잔여 줄 18 → **0**(진짜 부분취소가
0건이므로 0 이 맞다), 발송 행 44건은 그대로, 자산 핀 `20260826d` 반영.

> 교훈: **분포를 세기 전에는 신호인지 잡음인지 알 수 없다.** 두 판정 다 코드로는
> 맞아 보였고 합성 데이터 테스트도 전부 green 이었다. 100건을 세고 나서야 하나는
> 100% 오탐, 다른 하나는 축이 뒤집혀 있었다.

## F. 사유 원문을 R-1 후보 표까지 (커밋 `8116a95f`)

F-1 은 pane 위쪽, 즉 **지금 수집분**에만 있었다. 재결제냐 추가결제냐를 실제로 고르는 자리는
그 아래 **후보 표**다. 라벨(`전부 취소`)은 무엇이 일어났는지만 말하고, 왜인지는 사유 원문에
있다 — 스테이징 실데이터 5건이 그 답을 적고 있었다: `일시불 재결제 예정` · `취소 재결제` ·
`재주문예정` · `재결제` 는 재결제고, `사이즈 재측정후 주문할께요` 는 아니다.

- `_naver_facts` 가 후보의 취소된 링크마다 사유 원문을 모은다. 추출은 `mapping.extract_claim`
  한 곳만 쓴다(두 벌로 읽으면 pane 과 후보 표가 다른 문장을 말한다).
- 본품·옵션이 같은 문장을 각각 들고 오므로 중복은 뺀다. 서로 다른 문장은 하나도 안 버린다.
- 없으면 아무것도 안 낸다(빈 따옴표 금지).
- **매크로 이름 함정**: 이 템플릿은 이미 `cancel_reasons`(취소 사유 코드표, 모달 picker)를
  쓴다. 같은 이름으로 매크로를 만들면 `'Macro object' has no attribute 'items'` 로 그
  화면이 통째로 500 이 된다 — `customer_cancel_sentences` 로 지었다.

**스테이징 확인**: 후보 표에 사유 원문 3건 렌더(표본 `“재결제”`).

## 검증 기록

| 항목 | 결과 |
|---|---|
| import | `APP_OK` |
| `tests/services/integrations` | `721 passed` |
| smoke | `=== PRE-PUSH SMOKE PASSED ===` |
| deploy CI | 4/4 green (`535d4244`) |
| 승격 트리 | `APP_OK` · `711 passed` · smoke exit 0 · PR CI(perf-gate·pg-lane) 통과 |
| 스테이징 실화면 | 링크 100건 전수 × 2회(수정 전·후) |

## 설계에서 못박은 것

**부분취소 판정** — `remain*`·`initial*` 는 **281/281 전 건에 다 온다**. 존재 여부로 보면
모든 집이 부분취소고, `initial != remain` 으로 봐도 **전부취소가 걸린다**(실데이터가 그랬다).
부분취소는 **일부가 남았을 때**(`0 < remain < initial`)만이다.

**`reason` 과 `detailed_reason` 을 합치지 않는다** — 앞은 집계·판정에 쓰는 코드값이고
뒤는 사람이 쓴 문장이다. 기존 6키는 뜻도 값도 불변(회귀 테스트로 잠금).

**없는 값은 줄 자체를 안 낸다** — 빈 칸이나 `-` 로 채우면 화면이 거짓말을 한다.

**어긋남은 한 방향만** — 상시 켜진 경고는 안 읽힌다. 사실을 지우는 것이 아니라 **경고를
아끼는 것**이다(두 열은 그대로 나란히 있다).

## 알려진 사각

`_snapshot_projection`(`naver_ingest.py`)이 만드는 **얇은 문서**는 `cancel`·`currentClaim` 을
통째로 실어 `detailed_reason` 은 살아남지만, **`delivery` 와 `initial*`/`remain*` 은 없다**.
지금은 두 호출 다 통째 스냅샷을 읽으므로 무해하다. 나중에 **목록(얇은) 경로에서 F-2·F-3 를
부르면 조용히 빈 값이 된다** — 그때 투영에 경로를 더해야 한다.

배송 상태 라벨은 실데이터 확인분(`NOT_TRACKING`·`DIRECT_DELIVERY`)에 `DELIVERING`·
`DELIVERED` 만 더했다. 그 밖의 코드는 규율대로 **원문 그대로** 나온다.

**진짜 부분취소는 스테이징에 0건이다.** F-3 의 `남은 N` 은 아직 실화면에서 한 번도 뜬 적이
없다 — 단언은 합성 데이터로만 잠겨 있다. 실물이 처음 들어오면 그때 눈으로 확인한다.

## 다음 세션이 이어받을 것

1. **운영 실화면 확인** — production `c81d01ed` 배포 뒤 워크벤치에서 사유 원문 한 줄과
   발송 행이 뜨는지. 운영 코호트는 `all` 이라 이미 전 담당자에게 보인다.
   (`claude_master` production 계정은 **기본 잠금** — 사용자 요청 1건당 해제→측정→재잠금.)
2. **네이버 단독 발송에 표식이 필요한가** — 지금은 아무 표식이 없다(경고에서 뺐다).
   "FOMS 를 안 거치고 나간 발송"이 41/44 라 흔한 일이지만, 그 집에 발송처리 버튼이
   그대로 열려 있으면 중복 호출이 될 수 있다. 실사용에서 문제가 되는지부터 본다.
