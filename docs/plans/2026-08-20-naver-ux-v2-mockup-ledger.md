# 네이버 수집 화면 UX 개편 v2 목업 — 플랜 + 진행 원장

- 등급: `**C` / 세션 범위: **v2 목업까지** (구현 스펙·코드 수정은 승인 후 별도 세션)
- 설계 정본: `docs/research/2026-08-20-naver-ux/04_설계_결정.md`
- 브리프(실데이터·도메인 규칙·금지): `docs/research/2026-08-20-naver-ux/00_브리프.md`
- 입력 목업 v1: `docs/design/mockups/naver-ingest-workbench.html` (**수정하지 않는다**)
- 산출물: `docs/design/mockups/naver-ingest-workbench-v2.html` (신규 1개 파일, 단독 실행)
- 작업 위치: `c:/tmp/foms-s-naver-ingest` (브랜치 `session/naver-ingest`)

## 사용자 확정 사항 (2026-08-20 질의 2회)

| 항목 | 결정 |
|---|---|
| 파일 | 새 파일 v2 — v1 보존, 나란히 비교 |
| 결정 5 인라인 확장 | **우측 상세에 상품주문 표** (좌측 큐는 한 집 한 줄 유지) |
| 결함 #7 대조표 | **2단 구성** — 위=집 단위(수취인·연락처·주소·결제), 아래=상품주문 행 단위(옵션 원문·금액) |
| 결정 6 불가역 확인 | **실제로 뜨는 모달** (JS 동작) |
| 결정 7 실패 4단계 | **둘 다** — 상단 접이식 결과 띠 + 전체 이력 탭 실패 기록 |
| 핫키 J/K/Enter | **제거** (04 문서 "핫키 최적화 반영 안 함"과 정합) |
| 결정 4 3층째 표식 | **왼쪽 세로 색띠 + 글자 라벨** (색맹 대비, 회색=손대면 안 됨) |
| 이중 표기 위치 | **헤더에만** — 탭별 상품주문 건수는 실측에 없어 지어내면 브리프 금지 위반. 헤더는 실측 `43집 · 상품주문 140건` |

## v1 격차 (이번에 닫을 것)

| 결정 | v1 | v2 목표 |
|---|---|---|
| 1 탭 4개 통합 | 있음 | 승계 |
| 8 좌우 분할 | 있음 | 승계 |
| 3 숫자 이중 표기 | 없음 | 탭·헤더·필터 전부 `43집 (상품주문 140건)` |
| 4 상태 3층 | 반쪽 | 진행축 배지 + 클레임축 배지 + 좌측 색띠/글자 |
| 5 행 인라인 확장 | 없음 | 우측 상세 상품주문 n행 표(옵션 원문 전문) |
| 6 불가역 4종 세트 | 없음 | 모달 + 건수 재진술 + 되돌릴 수 없음 + 사후 경로 |
| 7 실패 4단계 | 없음 | 카운터 → 실패목록 → 건별 사유 → 실패건만 재시도 |
| 2 취소·반품 전체이력 잔존 | 반쪽 | 회색 + 액션 비활성으로 같은 자리 유지 |
| 결함 #4 | "이 집 숨기기" | **확인 완료**(큐에서 빼기) |
| 결함 #8 | — | 페이지네이션에 필터 유지 표시 |

## Task

### T1 — 뼈대 + 공통 (DONE)
v2 파일 생성. v1 CSS 토큰 승계, 상태 스트립, 탭 4개(이중 표기 배지), 좌우 그리드, 색띠 3층 상태 CSS.
핫키 힌트 전부 제거.
**완료 기준**: 파일 단독 실행으로 4개 탭 전환 동작 · 탭 배지는 집 수만(실측) · 헤더에 `43집 · 상품주문 140건` · 문서 내 `J`/`K`/`Enter` 힌트 0건(grep).

### T2 — 처리 대기 탭 (DONE)
좌측 큐(색띠 + 진행/클레임 배지 + 글자 라벨) · 우측 2단 대조표(집 단위 / 상품주문 6건 행) · 옵션 원문 크게.
**완료 기준**: 이재두 집 선택 시 상품주문 6행이 옵션 원문과 함께 보임 · 대조표 FOMS 열이 `–` 아닌 실제 값·(미입력) 상태 구분 · 브리프 실데이터 외 숫자·이름 0건.

### T3 — 발주확인 전 탭 + 불가역 모달 (DONE)
발주확인 보내기 → 모달 실동작. 문장에 건수 재진술 + "되돌릴 수 없습니다" + 보낸 뒤 무엇이 열리는지.
**완료 기준**: 버튼 클릭 시 모달 표시·취소로 닫힘 · 모달 본문에 대상 건수 숫자가 문장으로 재등장 · 선택 없으면 버튼 비활성(2026-08-14 AS 사고 패턴 차단).

### T4 — 실패 4단계 (DONE)
상단 접이식 결과 띠(성공/실패 카운터 → 펼치면 실패 건별 사유 → 실패건만 재시도) + 전체 이력 탭 실패 기록 행.
**완료 기준**: 띠 펼침 동작 · 실패 3건 각각 사유 문자열 보유 · `data-foms-no-autodismiss` 의도를 주석으로 명시(5초 자동닫힘 함정 대응).

### T5 — 취소·반품 탭 (DONE)
액션 잠금 유지 + **확인 완료** 버튼(결함 #4). 주문 없는 건도 큐에서 뺄 수 있음.
**완료 기준**: 주문 만들기·발주확인 비활성 · 확인 완료만 활성 · "숨기기" 문구 0건.

### T6 — 전체 이력 탭 + 검수 (DONE)
취소·반품 회색 잔존(결정 2) · 이중 표기 · 페이지네이션 필터 유지 표시(결함 #8).
**완료 기준**: 8개 결정 체크리스트 전부 ✅ · 1440 폭 브라우저 실측 스크린샷 1장 · 가로 스크롤 없음 · 외부 CDN/폰트/이미지 참조 0건(grep).

## 검증 명령
```
grep -c -E 'cdn\.|https?://fonts|<img src="http' docs/design/mockups/naver-ingest-workbench-v2.html   # 0 이어야 함
grep -n -E '\bJ\b/|Enter</span>' docs/design/mockups/naver-ingest-workbench-v2.html                     # 0 건
```
브라우저 1440 실측은 gstack browse.

## 진행 기록
- 2026-08-20 세션 시작. 설계 질의 2회 완료, 플랜 작성.
- 2026-08-20 사용자 승인 — T1부터 순차 진행. 이중 표기는 헤더 한정으로 확정.
- 2026-08-20 **T1~T6 완료.** `docs/design/mockups/naver-ingest-workbench-v2.html` (55KB, 단독 실행).
  검증: Playwright 1440x980 실측 **21항목 전부 PASS**(JS 에러 0 · 4탭 전환 · 가로 스크롤 0 · 결과 띠 펼침 ·
  모달 3종 건수 재진술 · 선택 0집 버튼 비활성 → 2집 선택 시 라벨 동기 · 취소탭 확인완료만 활성).
  정적: 외부 CDN/폰트/이미지 0 · 핫키 잔존 0 · 브리프 밖 인명 0.
  스크립트 `scratchpad/verify_v2.py`, 스크린샷 `scratchpad/v2_*.png`(커밋 안 함).
  **판단 기록**: 실측에 없는 값(탭별 상품주문 건수·상품주문 2~6행·임재용/전태곤 금액)은 지어내지 않고
  회색 이탤릭 자리표시로 남겼다 — 브리프 금지 1번(가짜 숫자) 준수. 취소·반품은 실측 확인된 2집만 썼다
  (v1 의 `박선미` 4집은 실측에 없는 인명이라 v2 에서 제외).
- **잔여**: 사용자 검토 → 승인 시 구현 스펙 + task 분해(선행 결함 6개 먼저, 계약 테스트 60개 동반 수정).
