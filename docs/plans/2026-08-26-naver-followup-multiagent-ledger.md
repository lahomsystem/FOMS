# 네이버 후속 4건 — 멀티에이전트 진행 원장 (2026-08-26)

> 사용자 지시: "테스크 멀티에이전트로 나눠 처리 하고 CEO 에이전트가 총괄 해"
> 작업 트리: `c:\tmp\nvfix` (브랜치 `tmp/naver-fix-20260825`)
> 앞선 원장: `2026-08-26-naver-field-surfacing-ledger.md` · `2026-08-26-naver-deposit-and-refresh-ledger.md`

## 역할

| 역할 | 몫 | 규율 |
|---|---|---|
| **주 세션** | 디스패치 · **diff 직접 확인** · 테스트 직접 실행 · 커밋 · 푸시 · 승격 | 서브에이전트 "완료" 보고는 주장일 뿐이다. 검증 없이 승인하지 않는다 |
| **CEO** | 설계 판정 · 범위 감독 · 최종 2판정 리뷰(스펙 준수 / 코드 품질 분리) | pre-judge 금지. 근거는 파일·라인으로 |
| **W1~W3** | 각자 1건, 서로 다른 파일 | 커밋·푸시 금지(주 세션 몫) · 범위 밖 파일 편집 금지 |

## Task

| # | 몫 | 파일 경계 | 상태 |
|---|---|---|---|
| T0 | 이력 원본 보기 운영 승격 | 승격 워크트리 | **주 세션** |
| T1 | push 알림 미보장 — Redis 오진과 같은 뿌리 | `foms/services/notifications/push_sender.py` + 그 테스트 | PENDING |
| T2 | 문자열 legacy 잔금이 클램프를 빠져나간다(CEO L-2) | `foms/services/erp_mobile_order_display.py` + 그 테스트 | PENDING |
| T3 | 네이버 단독 발송 집의 발송처리 **중복호출 위험** 점검 | **읽기 전용 조사** — 편집 0 | PENDING |
| T4 | 네이버 상태 동기화 버튼(주문확인·발송·취소/반품 최신화) | 설계 승인 후 결정 | 설계 대기 |

## 공통 완료 기준 (전 task)

1. `python -c "import app; print('APP_OK')"` → `APP_OK`
2. 담당 파일의 테스트 스위트 전량 통과
3. **새 사실마다 단언 1개 이상.** 테스트 없이 "고쳤다" 는 주장일 뿐이다
4. 프로젝트 규율: 함수 50줄 이하 · docstring 필수 · 타입 힌트 필수(신규) · bare except 금지 ·
   인라인 스타일 금지 · 없는 값은 지어내지 않는다(빈 칸·`-` 로 채우지 않는다)
5. 커밋·푸시 금지. 변경 파일 목록과 검증 결과를 보고한다

## 진행

| # | 몫 | 상태 | 커밋 / 결과 |
|---|---|---|---|
| T0 | 이력 원본 보기 + 후보 표 사유 원문 운영 승격 | **DONE** | PR #161 머지 → production `a796e200` |
| T1 | push 알림 미보장 | **DONE** | `a171a550` — 확실히 0대일 때만 막는다. 신규 단언 2건 |
| T2 | 문자열 legacy 잔금 | **DONE** | `f90d84fe` — 부호 보존 파싱 후 클램프. 신규 단언 3건 |
| T3 | 발송처리 중복호출 위험 점검 | **DONE(조사)** | 위험 **실재** — 아래 참조 |
| T4 | 네이버 상태 동기화(읽기) | **PENDING — 다음 세션** | 설계 확정. T5 가 같은 파일을 먼저 고쳐야 해서 미뤘고, 이제 길이 열렸다 |
| T5 | 중복호출 차단(버튼 잠금 + 서버 방어선) | **DONE** | `6feccc85` — 신규 단언 9건(빨강 먼저 확인) |
| T6 | 채널톡 readiness 같은 뿌리 | **DONE** | `c68f4015` — 못 셌으면 degraded(200), 확실히 0대만 fail |
| T8 | 반품 전송 설계 | **DONE(판정)** | **착수 게이트 걸림** — 아래 참조 |

## T3 조사 결과 — 위험은 실재한다 (주 세션이 인용 라인을 직접 확인)

발송처리 판정이 **양쪽 다 우리 표식만** 본다. 네이버가 이미 발송을 말하고 있어도 아무도 안 본다.

- `naver_workbench_pane.html:75`
  `can_dispatch = not locked and not dispatched and (not place_pending or close_now)`
- `fulfillment.py:544`
  `todo = [row for row in links if not _state(row).get("dispatched_at")]`

`todo` 가 비지 않으면 `client.dispatch_product_orders(payload)` 가 **실제로 나간다**. 서버가 막는
지점이 없다. `fulfillment.py:10-11` docstring 이 "네이버의 400(이미 처리됨)을 정상 흐름으로 삼지
않는다" 고 적어 둔 것은, 설계자가 **네이버 거절을 안전망으로 기대하지 않았다**는 명시적 진술이다.

구조적 비대칭도 있다: 발주확인(`place_pending`)은 우리 표식이 없어도 컬럼으로 보완하는데
(`fulfillment.py:126-137`), 발송에는 그런 폴백이 없다. 그래서 시간이 지나면 발주확인은 자동으로
풀리지만 **발송은 절대 자동으로 True 가 되지 않아 버튼이 열리는 방향으로 수렴**한다.

**운영 실측은 이 방향 0건**(운영 발송은 FOMS 를 거친다). 스테이징 41건은 판매자센터 직접 발송
시절의 것으로 **보인다**(확정 아님). 지금 이 조건을 만족하는 살아있는 집이 몇 건인지는 확인 못 함.

## T4 CEO 설계 판정 — 절반은 이미 있다

| 갈래 | 판정 | 근거 |
|---|---|---|
| 쓰기(발주확인·발송처리·취소) | **이미 있다** | pane 버튼 4종 + 벌크 + 결과 폴링 + 실패 재시도 |
| 읽기 — 전역 자동 | **이미 있다** | 5분 스윕이 `refresh_claims` 로 취소·반품·판매자센터 발주확인을 다시 읽는다(`ingest.py:278-284`) |
| 읽기 — **집 1건 지목** | **없다** | `get_product_orders` 호출자 2곳 전부 목록 기반 |
| 반품 전송 | 새 불가역 호출 | 사용자가 "둘 다" 로 결정 → T8 에서 따로 설계 |

권고안: `refresh_claims` 를 **한 줄도 안 고치고** id 목록만 넘겨 재사용한다 — 알림 묶기·자기취소
억제·중복 억제·스냅샷/발주상태/묶음키 갱신이 전부 따라온다. 새 불가역 호출 **0개**.

**CEO 가 확인 못 한 것 — 주 세션이 확인했다(2026-08-26)**: 코드 기본값은 꺼짐이지만
(`start.sh:33`), **운영 WORKER 에는 켜져 있다**:

```
FOMS_NAVER_SYNC_ENABLED = 1
FOMS_NAVER_SYNC_INTERVAL_SECONDS = 300
```

즉 자동 클레임 감시가 **운영에서 5분 주기로 실제로 돌고 있다**. 그러니 수동 버튼의 값어치는
"안 되던 게 된다" 가 아니라 **① 5분을 안 기다린다 ② 네이버 변경 이벤트가 안 오는 건을 잡는다**
둘이다. ②가 실질적인 이유다 — `claim_watch.py:8-9` 가 스스로 "취소가 그 목록에 어떤 이름으로
실리는지 실물로 확인되지 않았다" 고 적어 뒀다. 이벤트가 안 오면 자동 경로로는 **영영** 못 잡는다.


## T8 반품 전송 설계 판정 (2026-08-26)

### 착수 게이트가 하나 있다 — **네이버에 반품 API 가 있는지 저장소로는 알 수 없다**

우리 클라이언트가 부르는 네이버 경로는 6개고 반품은 0개다(`client.py:304·333·361·389·420·458`).
저장소 전수 검색에서 **반품 경로 문자열 0건**. 앱 권한 그룹 설명은 "주문조회, 발주/발송처리,
취소, 교환 등" 인데 **"반품" 이라는 낱말이 없다**(`docs/specs/2026-08-19-naver-order-relation-and-fulfillment_SPEC.md:180-188`).

즉 정확한 상태는 "기능이 없다" 가 아니라 **"API 통로가 있는지 모른다"** 다. 판매자센터에
[판매자 직접 반품] 업무 기능이 있는 것은 매뉴얼로 확인됐지만, 그건 UI 문서지 API 명세가 아니다.
**공식 문서 확인 전에는 구현 착수 자체가 불가능하다.**

### FOMS 버튼 하나로 끝나는 갈래는 없다

접수(어쩌면 가능) → 수거정보 입력 → **실제 회수(우리 차량 — 자사배송이라 택배 수거가 아니다)**
→ 검수(사람) → 승인·환불(판매자센터). 중간 셋은 버튼으로 못 만든다.

### 지금 바로 할 수 있는 것 — **S0: 반품 축 읽기 구멍 3개** (불가역 0 · 네이버 호출 0)

주 세션이 `mapping.py:190-239` 를 직접 확인했다:

| 구멍 | 사실 |
|---|---|
| `COLLECTING` · `COLLECT_DONE` | 차단 집합(`BLOCKING_CLAIM_STATUSES`)에는 있는데 **라벨이 없다** → 반품 수거중 집이 화면에 **영문 원문**으로 뜬다 |
| `CANCELING` | 같은 문제(차단에만 있다) |
| `RETURN_REJECT` | **어느 집합에도 없다** — 차단도 안 되고 라벨도 없다. 취소는 `CANCEL_REJECT` 라벨이 있는데 반품만 비대칭 |

수거·환불 축(`collectCompletedDate` · `refundExpectedDate` · `refundStandbyStatus/Reason`)도
**원본에 이미 온다**(회수지 15/281). 화면이 안 읽을 뿐이다 — 오늘 F-1~F-3 와 같은 성질이다.

**보내기보다 읽기가 먼저인 이유**: 반품을 *보내려면* 먼저 반품을 *정확히 읽어야* 한다.
지금은 수거중이 영문으로 뜨고 반품 거부는 잠금조차 안 된다. 보내기부터 만들면 **우리가 만든
클레임의 진행 상황을 우리 화면이 못 읽는다.**

### 관측 사실

우리가 본 반품은 **전부 이미 끝난 것**(`RETURN_DONE`)이다. `RETURN_REQUEST`·`COLLECTING`·
`COLLECT_DONE` 을 실제로 관측한 기록이 저장소에 없다 — 중간 상태를 화면이 어떻게 보여주는지
아무도 본 적이 없다.

### 사람이 답해야 할 것 (요약 — 전문은 CEO 보고)

- **API**: 엔드포인트 존재·경로·body / 사유 코드 목록 / **자사 회수(택배사 없음)를 받는가** / 접수 철회 가능한가
- **우리 조건**: `DIRECT_DELIVERY`(`NOT_TRACKING`)가 반품접수 조건을 만족하는가 / v1.0 연동몰인가 v2.1인가
- **돈·고객**: 환불 시점(접수 즉시/수거 후/승인 후) / 접수 즉시 고객에게 가는 통보 문구
- **업무**: **가구 반품을 우리가 먼저 접수하는 경우가 연 몇 건인가** — 0 에 가까우면 값어치가 판매자센터 한 번 여는 비용보다 작다
- **검증**: 스테이징 실호출은 **가상주문으로 못 한다**(발송된 실주문 필요) — 대상은 사용자가 고른다


## 운영 승격 마감 (2026-08-26)

**PR #162 머지 → production `60a88ba3` 배포 SUCCESS · `/login` 200.**

| 커밋 | 무엇 |
|---|---|
| `9acd5a62` | push 알림 미보장 |
| `e70ca2f4` | 문자열 옛 잔금 |
| `692f2c77` | 채널톡 readiness |
| `1d16c686` | 발송처리 중복호출 차단 |

앞의 셋은 **같은 뿌리 하나**였다 — `Worker.count` 2단 fallback 이 둘 다 실패해도 0 을 돌려줘
"진짜 0대"와 "못 셌다"가 같은 값이 됐다. 오늘 `queue.py` 에 `worker_count_known` 을 만들고
(PR #159), 이 PR 이 **남은 호출부 3곳을 전부 닫았다**. 호출부 총 4곳 — 수집·push·채널톡·진단.

**승격 위생**: `promote_completeness` complete: True. 코드 4건 충돌 0. 문서에서 `AI_STATUS.md`
충돌 1회 — **타 세션의 '최신' 줄**이라 기능 의존이 아니다. 운영 쪽 문장을 지켰고(incoming 통째
수용 안 함), AI_STATUS 만 고치는 커밋 1건은 skip. 충돌 마커 0 확인.

**검증**: 승격 트리 `APP_OK` · **840 passed** · `pre_push_smoke` exit 0 · PR CI 2/2 pass ·
deploy CI 4/4 green.

## 다음 세션이 이어받을 것

1. **T4 다시 읽기 버튼 구현** — 설계 확정(위 §T4). `refresh_claims` 를 한 줄도 안 고치고
   id 목록만 넘겨 재사용. 새 불가역 호출 **0개**. 조각 S1~S7 과 계약 4종 등재 목록은 CEO
   설계안에 그대로 있다. **신규 mutation 라우트 계약 4종을 빠뜨리면 로컬 green → CI red** 다.
2. **T8-S0 반품 축 읽기 구멍 3종** — `COLLECTING`·`COLLECT_DONE` 라벨 부재, `RETURN_REJECT`
   가 차단·라벨 어디에도 없음, `CANCELING` 도 같은 문제(`mapping.py:190-239`). 수거·환불 축
   (`collectCompletedDate`·`refundExpectedDate`·`refundStandbyStatus/Reason`)도 원본에 이미
   온다(회수지 15/281). **불가역 0 · 네이버 호출 0.** 보내기보다 읽기가 먼저다.
3. **T8 반품 전송** — 사람이 답해야 할 것 15개(§T8). 특히 **API 존재 확인**이 물리적 게이트고,
   **"가구 반품을 우리가 먼저 접수하는 경우가 연 몇 건인가"** 가 값어치 판정의 핵심이다.
4. **T5 실화면 확인 미실시** — 버튼 잠금이 스테이징 실화면에서 어떻게 보이는지 눈으로 안 봤다.
   스테이징에 이 조건(네이버 `sendDate` 있음 + 우리 표식 없음)인 집이 41건 있어 바로 보인다.
   코호트를 `38,58` 로 잠시 열고 확인 후 **원복**한다.

---

# 2차 세션 (2026-08-26 저녁) — 진행 원장

> 작업 트리 동일(`c:\tmp\nvfix` · `tmp/naver-fix-20260825`). 시작 지점 `78bc6e72`.
> 사용자 추가 지시 2건이 중간에 들어와 범위가 넓어졌다.

## Task 판

| # | 몫 | 파일 경계 | 담당 | 상태 |
|---|---|---|---|---|
| N1 | T8-S0 반품 축 **추출**(라벨 구멍 + 수거·환불 축) | `mapping.py` + 새 테스트 | 주 세션 | **DONE** |
| N2 | 도크 재결제 — 이전 주문 시각 구분 (persona 검토 포함) | `dock.py` · `erp-naver-dock.js/.css` · 도크 파셜 · `erp_order_js.html`(핀만) | 위임 | 진행 |
| N3 | 네이버 탭 사용자 가시 낱말 `집` → `주문` | `templates/admin/naver_*` · `naver-workbench.js` · `naver_ingest.py`(문자열만) | 위임 | 진행 |
| N4 | T8-S0 반품 축 **화면 표시** | pane · `naver-workbench.css` · `naver_ingest.py` | 주 세션 | N3 뒤 (같은 파일) |
| T5 | 발송처리 잠금 스테이징 실화면 확인 | 배포 후 | 주 세션 | 대기 |
| T4 | 다시 읽기 버튼 | 아래 §T4 재설계 | 주 세션 | 설계 |

## N1 — T8-S0 추출 (DONE)

`mapping.py` 한 파일. 신규 단언 **16건**(`tests/services/integrations/test_naver_return_axis.py`).
기존 `tests/services/integrations` **764 passed**.

**라벨 구멍**은 3종이 아니라 **7종**이었다. 차단 집합에 있는데 라벨이 없던 것이
`COLLECTING`·`COLLECT_DONE`·`CANCELING` 말고 `CANCEL_REQUESTED`·`RETURN_REQUESTED` 도
있었고, 거부 비대칭은 `RETURN_REJECT` 말고 `EXCHANGE_REJECT` 도 같았다.
구멍이 다시 생기지 않게 **집합 대 집합** 단언을 걸었다
(`BLOCKING_CLAIM_STATUSES - set(CLAIM_STATUS_LABELS) == []`).

- `COLLECTING`/`COLLECT_DONE` 라벨은 `반품 수거중` 이 아니라 **`수거중`·`수거 완료`** 다.
  수거는 반품·교환 **양쪽**에서 온다 — 반품이라고 적으면 교환 건에서 화면이 틀린 이름을 말한다.
- `RETURN_REJECT`·`EXCHANGE_REJECT` 는 **차단하지 않는다**. `CANCEL_REJECT` 와 같은 규칙이다
  (거부 = 클레임 무산 = 주문 정상 진행). 앞 세션 메모의 "반품 거부는 잠금조차 안 된다" 는
  **잠금이 빠진 게 아니라 라벨이 빠진 것**이었다.

**조용한 유실 하나를 같이 닫았다**: `extract_claim` 이 사유 원문을 `cancel` 블록 **안에서만**
찾았다. 코드가 `returnDetailedReason` 을 읽으려 시도하고 있었는데, 반품이 별도 블록으로 오면
그 키가 `cancel` 안에 있을 리 없어 **영영 빈 문자열**이었다(배송메모 사건과 같은 모양).
`_claim_blocks()` 로 블록 후보를 우선순위 순으로 모아 키별로 처음 값을 쓴다.
`cancel` 이 첫 번째다.

**정정(CEO 리뷰 A1)**: 여기 처음 적었던 "기존 동작 불변"은 **틀렸다**. 블록 목록
마지막에 `currentClaim` **자신**이 들어가서, 클레임 상세가 거기 평평하게 실려 오면
옛 코드가 떨어지던 `order.claimStatus` 대신 그 값을 쓴다 — `blocking` 이 뒤집힐 수
있는 경로다. 값이 더 구체적이라 개선 방향이지만 **불변이 아니다**. 정확한 진술은
"더 구체적인 소스를 먼저 본다"이고, 우선순위를 단언 2건으로 못박았다.

**관측되지 않은 것(정직하게 남긴다)**: 반품 상세가 어느 부모 키로 오는지 실물 확인은
`cancel` 뿐이다. `returnInfo`·`return`·`exchange` 는 **폴백 가정**이고 테스트 픽스처도 가정이다.
실물 반품이 처음 들어오면 그때 눈으로 확인한다.

## T4 재설계 — 앞 세션 인수인계가 놓친 게이트 하나

> 인수인계: "`refresh_claims` 를 한 줄도 안 고치고 id 목록만 넘겨 재사용. 새 불가역 호출 0개."
> **그 부분은 맞다. 그런데 그것만으로는 안 된다.**

**네이버 HTTP 는 WORKER 에서만 나간다**(커머스API 호출 IP 3슬롯 계약). 웹 요청에서 직접
`client.get_product_orders` 를 부르면 등록되지 않은 IP 라 차단된다 —
`naver_ingest.py:2557-2561` 의 발송처리 라우트가 그 이유를 그대로 적어 두고 **큐에 넣는다**.

즉 T4 는 "라우트에서 `refresh_claims` 호출" 이 아니라 **큐 작업 1종 신설**이다:

| 조각 | 자리 |
|---|---|
| S1 | `queue.py` — `enqueue_naver_refresh(link_id, actor_user_id)` (`enqueue_naver_fulfillment:208` 본뜬다) |
| S2 | `tasks.py` — `run_naver_refresh_task` : 그 집의 `external_id` 를 모아 `changed=[{"productOrderId": …}]` 로 만들어 `refresh_claims` 에 넘긴다. **`refresh_claims` 는 불변** |
| S3 | `naver_ingest.py` — `POST /admin/naver-ingest/<link_id>/refresh` (읽기 전용 조작이지만 **큐 상태를 쓰므로** mutation 계약 대상) |
| S4 | 진행 표시 — 발송처리처럼 `rev` 폴링이 필요하다. 다시 읽기는 `fulfillment` state 를 안 건드리므로 **자기 신호가 따로 있어야 한다** |
| S5 | pane 버튼 + 잠금 사유 문구 |
| S6 | 큐 불가용(REDIS 없음) 503 경로 — 발송처리와 같은 문구 규율 |
| S7 | 테스트: 큐 enqueue 단언 · 태스크가 `refresh_claims` 에 넘기는 id 목록 단언 · 라우트 권한/계약 |

**계약 4종 등재처(실물 확인)**

1. `docs/harness/foms_order_mutation_policy_manifest.json`
2. `docs/harness/foms_write_guard_manifest.json`
3. `docs/harness/foms_audit_coverage_inventory.json` (lineno 는 안 본다 — 게이트가 lineno-free)
4. 감사 action 라벨 등재 (`NAVER_INGEST_*` 계열 — 미등재 시 FOMS CI red)

**앞 세션 CEO 설계안(S1~S7 원문)은 저장소에 없다.** 원장이 "CEO 설계안에 그대로 있다" 고
적었지만 그 보고는 세션과 함께 사라졌다 — 위 표는 **재도출**이다. 같은 사고가 반복됐다
(리뷰·설계 결과는 받는 즉시 원장에).

## T8 착수 게이트 — 절반 열렸다 (2026-08-26 저녁, 공개 문서 조사)

앞 세션 판정은 "**API 통로가 있는지 모른다**"였다. 공개 문서로 **통로 존재는 확인**했다.

네이버 커머스API 릴리즈 노트(v2.34.0)가 **반품 API 군을 이름으로 열거**한다 —
`반품 요청` · `반품 승인` · `반품 보류` · `반품 보류 해제` · `반품 거부(철회)`.
클레임 경로 모양도 확인된다: `POST /external/v1/pay-order/seller/product-orders/
{productOrderId}/claim/cancel/approve` 형태이고, 클레임 타입은 `cancel`·`return`·
`exchange` 세 축으로 갈린다.

**아직 못 연 것 — 여기부터가 진짜 게이트다**

| 못 연 것 | 왜 중요한가 |
|---|---|
| `반품 요청` API 를 **판매자가** 쓸 수 있는가, 아니면 구매자 요청분에 대한 승인/거부만인가 | 우리가 먼저 접수하는 갈래가 아예 없을 수 있다 |
| 우리 앱 권한 그룹에 반품이 **실제로 포함**되는가 | 권한 설명에 "반품" 낱말이 없다(스펙 문서 :180-188). 통로가 있어도 우리 열쇠가 없을 수 있다 |
| **자사 회수**(택배사 없음)를 받는가 | 가구는 우리 차가 간다. 택배 송장 필수면 이 길은 막힌다 |
| `DIRECT_DELIVERY`/`NOT_TRACKING` 이 반품 접수 조건을 만족하는가 | 우리 배송 형태가 전부 이것이다 |

정확한 body 명세는 apicenter(로그인·JS 필요)라 공개 검색으로는 못 읽었다.

**그리고 값어치 판정은 API 와 무관하게 사람이 답해야 한다** — "가구 반품을 우리가 먼저
접수하는 경우가 연 몇 건인가". 0 에 가까우면 통로가 열려 있어도 만들 값어치가
판매자센터 한 번 여는 비용보다 작다.

출처: [릴리즈 노트 v2.34.0](https://github.com/commerce-api-naver/commerce-api/discussions/1841) ·
[취소 승인 경로 인용 스레드](https://github.com/commerce-api-naver/commerce-api/discussions/1618)

## N2 — 도크 재결제 구분 (위임 → 주 세션 검증)

**persona 판정(에이전트, 주 세션이 근거 라인 확인)**: 흐리게 + 머리말 표식. **접지 않는다** —
재결제는 "같은 주문을 다시 결제"라 옛 옵션 원문이 여전히 유효한 규격일 수 있고, 담당자가
실제로 하는 일은 새 것과 **비교**다. 접으면 비교가 불가능하다.

**REPAY 와 ADDON 을 같게 다루지 않는다** — `repay_reconcile.deposit_guidance` 가 근거다.
재결제는 예약금을 **바꾸고**(옛 결제는 환불됨), 추가결제는 **더한다**(옛 결제는 살아 있다).
그래서 ADDON 주문의 원 주문은 흐리게 하지 않는다 — 흐리면 살아 있는 것을 죽은 것으로 그린다.

**추정 금지 적용**: 관계가 전부 `NEW` 인 옛 데이터는 어느 쪽도 '이전 주문'이 아니다.
주문이 하나면 라벨을 아예 안 만든다 — 보통 주문 화면은 오늘과 같다.

같이 고친 것: 귀속 드롭다운의 **동명이인 본품 2개**(글자까지 같았다). 이름은 원문 그대로
두고 꼬리표를 **이름 앞에** 붙인다(뒤에 붙이면 좁은 select 에서 잘려 다시 같아진다).

주 세션 검증: 클래스명·`data-*` 무변경 확인 · 신규 함수 5개 전부 50줄 이하 · CSS 는 이 파일
관례대로 raw hex(파일에 CSS 변수가 0개다) · `?v` 핀 2줄 범프 확인 · 신규 단언 12건(에이전트가
빨강 11건 먼저 확인).

**남은 사각(에이전트 자진 보고 — 그대로 남긴다)**: ① `확인 완료` 게이트가 지금도 **이전 주문
행까지 전부 체크해야** 열린다(죽은 주문에 "반영함"을 찍어야 하는 구조). ② REPAY 와 ADDON 이
한 주문에 섞이면 ADDON 은 대체로 안 본다(근거가 코드에 없다). ③ 실브라우저 미검증.

## N3 — `집` → `주문` (위임 → 주 세션 검증·마감)

86자리. sed 전량 치환이 아니라 주석·문자열 밖을 마스킹한 뒤 **렌더되는 자리만** 골랐다
(`수집`·`편집`·`집계`·`모집단`·`집합` 오염 0 — 주 세션이 diff 로 확인).
클래스명·`data-*`·id 무변경. JS 가 바뀌어 `?v` 핀 범프.

**뜻이 겹치는 6곳은 사용자가 정했다 — `건` 으로.** 다만 `건` 은 이미 세는 단위라
두 자리(`상품주문 3건이 주문 1건으로`, `나머지 N건은`)에서 삼중이 된다. 그 둘은 지시어를
빼서 해결했다(주 세션 판단).

| 자리 | 결과 |
|---|---|
| `naver_triage.html:381` | `상품주문 N건이 주문 1건으로 묶입니다` (지시어 제거) |
| `naver_triage.html:423` | `주문을 만들 수 없는 **건**입니다` |
| `pane:177` · `pane:283` | `이 **건**의 상품주문은 모두 이미 주문에 들어가 있습니다` |
| `pane:731` | `나머지 N건은 이미 주문이 있어…` (지시어 제거) |
| `naver_ingest.py:2895` | `이 **건**의 기존 주문 후보가 아닙니다` |

## N4 — 반품 축 화면 표시 (주 세션)

pane 의 클레임 사유 원문 바로 아래. `known` 이 False 면 **줄 자체를 안 낸다**.
회수지(`collectAddress`)만 싣고 반품 수취지(`returnReceiveAddress`)는 안 싣는다 —
앞은 "우리 차가 가지러 갈 곳", 뒤는 "우리 물류가 받는 곳"이라 축이 다르다.
신규 단언 4건. 자산 핀 `20260826f → 20260826g`.

## T4 — 다시 읽기 (주 세션, 완료)

| 조각 | 결과 |
|---|---|
| S1 큐 | `enqueue_naver_refresh` |
| S2 워커 | `run_naver_refresh_task` → `claim_watch.refresh_household` → `refresh_claims`(불변) |
| S3 라우트 | `POST /admin/naver-ingest/<link_id>/refresh` |
| S4 진행 표시 | **새 엔드포인트를 만들지 않았다.** `_fulfillment_state.rev` 지문에 `claim_sync.refreshed_at` 을 넣어 기존 폴링이 그대로 뒤집히게 했다 |
| S5 버튼 | pane `#wb-refresh` — **잠긴 주문에도 낸다**(반품이 어디까지 갔는지 보려고 누른다). 확인 모달 없음(되돌릴 게 없다) |
| S6 큐 불가용 | 503 + 사유 |
| S7 테스트 | 서비스 7건 + 라우트 5건 |

**계약 4종 등재 완료**: mutation policy manifest · write guard manifest ·
audit coverage inventory(스캐너 재생성) · 감사 라벨 `NAVER_INGEST_REFRESH_ENQUEUE`.

## CEO 2판정 리뷰 (2026-08-26 저녁) — 지적 전량 + 처리

> 판정 A(스펙 준수) **조건부 통과** · 판정 B(코드 품질) **조건부 통과(CI red 1건 포함)**.
> 주 세션이 **6건 전부 실물 재검증**했다. 재검증 중 함정 하나: 턴 경계에서 셸 작업
> 디렉토리가 `c:\DEV\FOMS` 로 리셋돼 첫 확인 3건이 엉뚱한 트리에서 돌았다(무효 판정 →
> `cd /c/tmp/nvfix` 후 재확인). **`pwd` 확인 없이 검증하지 않는다.**

### 막아야 할 것 — 전부 고쳤다

| # | 지적 | 재검증 | 처리 |
|---|---|---|---|
| 1 | `foms_failopen_inventory.json` 미재생 → CI red | **사실**(`1 failed`) | `failopen_scan.py` 재생성 → 13 passed |
| 2 | 회수지만 온 건에서 `반품 진행` 이 **라벨만 있고 안이 빈 줄**로 뜬다 | **사실**(`known` 이 주소로도 True) | 진행 줄은 **스칼라 4종 중 하나라도** 있을 때만. 단언 추가 |
| 3 | "되돌릴 게 없다" 가 거짓 — 다시 읽기가 **알림을 팬아웃**한다 | **사실**(`refresh_claims` 가 `_notify` 호출) | 문구 3곳 정정. 알림은 **부작용이 아니라 목적**이라고 명시하고, 버튼 title 이 그 사실을 말한다 |
| 4 | 쓰기 금지 가드가 **없는 메서드 이름**을 막는다(2/3 공허) | **사실** — 실물은 `confirm_place_orders`·`request_cancel_product_order` | 이름 교정 |
| 5 | 옛 발주확인 실패가 남은 주문에서 "다시 읽기 실패"로 오보 | **사실**(`last_error` 는 지울 때까지 남는다) | 라우트가 `err_at`(누르기 직전 실패 시각)을 주고 화면이 비교. **기존 호출은 인자 미전달이라 동작 불변** |
| 6 | `dock.py` 가 이번 배치가 없앤 낱말(`옛 집`)을 새로 만들었다 | **사실** | `이전 주문` 으로 |

추가로 A1 정정(위 §N1)과 `run_naver_refresh_task` 타입 힌트(B8)를 처리했다.

### 남긴 것 (알고 넘어간다)

- **5분 스윕이 25초 폴링 창에서 rev 를 흔들 수 있다** — "완료" 한 번 오표시. 확률 낮고
  `softRefresh` 가 곧 사실로 덮는다.
- **`NEW` 집 2개 + `REPAY`** 면 둘 다 "이전 주문"이 된다(실제 대체는 하나). 실데이터 사례 없음.
- **`확인 완료` 게이트가 이전 주문 행까지 체크를 요구**한다(N2 자진 보고). 게이트 변경은
  회귀 위험이 있어 별도 판단.
- `collect_address.zip_code` 는 뽑지만 화면이 안 쓴다. `주문` 이 두 뜻으로 인접한 자리
  4곳(`수집됨(주문 전) 12주문` 등) — 틀린 건 아니고 읽기 부담.
- 관계 라벨 문자열이 4벌(선재 부채, 이번에 한 벌 늘었다).

### 확인 못 한 것

- **실브라우저 미검증**(도크 흐림 톤·반품 진행 줄·집 표식 칩).
- **실물 반품 스냅샷 부재** — `returnInfo`/`return`/`exchange` 블록 이름은 여전히 가정.
- 다시 읽기가 실제로 push 까지 나가는지(`push_sender._DEFAULT_P1_TYPES` 등재 여부) 미실행.

## 2차 세션 마감 (2026-08-26)

**deploy `aecd25f6` · CI 4/4 green**(FOMS CI · Harness CI · PG Lane · perf-gate — `gh run list` 로
커밋별 전 워크플로 확인). 커밋 6건, `push_own_session_commits.py --shas` 로 자기 몫만 올렸다
(origin/deploy 가 타 세션 CI 수정 `01fd8e97` 로 앞서 있었다).

검증: `APP_OK` · `tests -k naver` **811 passed** · `tests/contracts tests/domains` **5135 passed** ·
`pre_push_smoke` **exit 0**.

### 다음 세션이 이어받을 것 (순서대로)

1. **스테이징 실화면 전수 확인 — 첫 일이다.** 코호트를 `38,58` 로 **한 번만** 열고
   ① T5 발송처리 잠금(해당 조건 41건) ② 반품 진행 줄·회수지 ③ 도크 이전 주문 구분
   ④ `주문` 문구를 한 번에 보고 **`38` 로 원복**한다. 지난번 교훈: **분포를 세기 전에는
   신호인지 잡음인지 알 수 없다**(100건 세고서야 오탐 2종이 드러났다).
2. **운영 승격** — 실화면 확인 뒤. 세션 커밋 6건 cherry-pick(전체 merge 금지).
3. **T8 반품 전송 설계** — 사용자 답: **연 10건 이상**이라 값어치는 있다. 남은 물리적
   게이트는 "**판매자가 먼저 접수**하는 API 가 있는가 / 우리 앱 권한에 반품이 포함되는가 /
   **자사 회수**(택배 송장 없음)를 받는가" 셋이고, apicenter 문서(로그인 필요)를 봐야 한다.
4. **남긴 사각 3건** — 실물 반품 스냅샷이 오면 블록 이름 확인 · `확인 완료` 게이트가
   이전 주문 행까지 체크를 요구하는 문제 · 다시 읽기 push 실발송 미확인.
