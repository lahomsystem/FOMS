# 진행 원장 — 워크벤치 UX 감사 반영 (2026-08-24 후속)

상위 계약: `docs/specs/2026-08-23-naver-workbench-v3_CONTRACT.md` (§2.4 이번에 개정)
선행 원장: `2026-08-24-naver-workbench-thin-badge-ledger.md` ·
`2026-08-24-naver-attach-invisible-result-ledger.md`
작업 위치: `c:\tmp\foms-s-naver-ingest` (브랜치 `session/naver-ingest`)

멀티 에이전트 감사(조사 3 + 적대 검증 1)에서 나온 목록·알림 결함을 사용자 승인 순서대로
반영한 기록. **조사 보고를 그대로 쓰지 않았다** — 실재 여부를 코드로 다시 확인하고,
과장된 심각도는 낮췄으며(아래 '기각·강등'), 실화면 검증에서 새로 드러난 결함도 함께 고쳤다.

## 반영한 것

| # | 결함 | 고친 방식 | 커밋 |
|---|---|---|---|
| 1 | 취소·반품 집이 업무량으로 계산됨(72집 중 13집) | 목록은 두고 **숫자만 쪼갬**(`_actionable_count`) · 계약 §2.4 개정 | `e3be4652` |
| 2 | 탭 줄이 스크롤에 사라짐 | `.wb-tabs` sticky(z-index 3) · pane top 배율 연동 | `e3be4652` |
| 3 | 목록 중간에서 접수시각이 되감김 | 병합 뒤 전역 정렬(`created_sort` 원본 시각) | `f8f4ecbb` |
| 4 | 발송기한 임박 집을 찾을 수 없음 | 정렬 2종 · **캡보다 먼저** 정렬 | `f8f4ecbb` |
| 5 | 검색창 0개 | 목록 안 즉시 찾기(범위를 라벨에 명시) | `f8f4ecbb` |
| 6 | 임박순 상단을 잠긴 집이 차지 · 기한이 안 보임 | 잠긴 집 맨 뒤 · 기한 배지 조건 완화 | `a83e8044` |
| 7 | 'N건 묶음'이 모달 건수와 다름 | 배지 = 집 전체(`household_count`) · 모달이 차이를 설명 | `27b15987` |
| 8 | 발주확인 완료 무표시 | 체크박스가 회색인 행에만 완료 배지 | `27b15987` |
| 9 | 우리가 낸 취소가 "멈춰라" 긴급 알림으로 역류 | `_is_our_cancel` 로 **알림만** 차단 · 억제 건수 집계 | `346e05fa` |

부수: 목록 헤더를 "보이는 N줄"로 라벨 분리, 칩 힌트 문구 제거(사용자 요청),
정렬·찾기 줄도 sticky(사용자 요청).

## 기각·강등 (조사 보고를 그대로 쓰지 않은 자리)

- **"규격 입력할 차례가 발주확인 미완료를 덮어쓴다"(심각도 상) → 중.** 노란 '발주확인 전'
  배지는 spec 행에도 **뜬다**(`naver_workbench.html`). 사실이 숨겨지지는 않는다. 틀린 것은
  오른쪽 한 줄 안내뿐이다.
- **"터치에서 잠금 사유가 안 보인다"(심각도 중) → 이미 해결됨.** 앞선 작업의
  `.wb-pick:disabled { pointer-events: none }` 로 탭이 행에 닿아 상세가 열리고 사유가 거기
  상시 문구로 있다. 재작업하지 않았다.
- **붙이기 무동작 진단 2종 → 둘 다 틀림.** 별도 원장 참조
  (`2026-08-24-naver-attach-invisible-result-ledger.md`).

## 실화면 검증에서 새로 나온 것 (테스트로는 못 잡았다)

임박순을 실제로 눌러 보니 상단 6줄 중 4줄이 '손대지 않음'이었고, 상단 15줄이 기한을 하나도
안 보여줬다. 내 회귀 테스트는 "기한 없는 집이 맨 뒤"는 검사했지만 **"잠긴 집이 맨 뒤"는
생각하지 못했다**. 정렬한 값이 화면에 없다는 것도 코드만 봐서는 안 보였다.
→ `a83e8044` 로 고치고 그 케이스를 테스트에 박았다.

## 자기점검에서 잡은 가짜 테스트

알림 역류 회귀에서 억제를 꺼도 `notified == 0` 이 통과했다 — **알림 수신자가 없어서**
우연히 맞은 것이었다. ADMIN 을 만들어 진짜로 걸리게 강화한 뒤 다시 red 를 확인했다.
"red 를 봤다"만으로는 부족하고 **무엇 때문에 red 인지**까지 봐야 한다.

## 검증

- `tests/services/integrations/` **559 passed**(신규 회귀 18건 추가) · `APP_OK` ·
  인벤토리 게이트 44 passed · 커밋마다 `pre_push_smoke` exit 0.
- CI: `f1dcc098`·`be1a7e45`·`e3be4652`·`f8f4ecbb`·`a83e8044`·`27b15987` 4/4 green 확인.
- 스테이징 실화면(claude_master, 코호트 임시 38,58 기간): 스트립 61 + 손대지 않음 14 =
  칩 '전체' 75 · 탭 sticky 스크롤 1200px 후 y=0 · 찾기 '신중섭' 2집 · 주문번호로 link 58
  단건 · 임박순 09-02→09-04 오름차순에 잠긴 집 전부 하단 · 주문 4485 도크에 추가결제
  6건 1,610,780원.

## 남은 것

- **`346e05fa`(정렬·찾기 줄 sticky)는 육안 미확인.** 코호트가 `38` 로 원복돼 claude_master
  로는 화면이 안 열린다. 오프셋이 틀리면 두 줄이 겹치거나 상세 머리가 가려진다 —
  `upperkill` 로 스크롤해 확인 필요.
- `docs/AI_STATUS.md` 미갱신(병렬 세션 푸시 중 충돌 회피). 기록은 원장 4개가 갖고 있다.
- 얇게 읽기 존치 판단(스테이징 이득 0 실측 — 선행 원장 참조).
- 케이스 1 재결제 취소 프로세스는 **보류**(사용자 지시). 위험 1건 미해결:
  재결제가 `CLOSE_NOW_RELATIONS` 에 있어 붙이면 발송처리가 '지금 닫기'로 열린다.
