# 하네스·가이드라인 ablation v2 — 보고서 (2026-09-09, 1단계 정적 감사)

> 지시서 `docs/plans/2026-09-09-harness-ablation-v2-meta-prompt.md`. 원장 `docs/plans/2026-09-09-harness-ablation-v2-ledger.md`(조각 5 + 총괄 확정 §0). 초안 `docs/plans/2026-09-09-harness-ablation-v2-drafts/`. 이 보고서는 **1단계(정적 감사)만** 담는다 — 사용자 선택으로 2단계 실험(§7)은 미실행. 기준 HEAD `46bdac44d`(워커 재측정 시점 `fe21a5cc3`, 같은 날 문서 커밋 1건 차이). 저장소 기존 파일은 한 글자도 바꾸지 않았다.

방법 편차 2건: ① 내장 시스템 프롬프트 대조는 claude-code-guide 질의 대신 **분석 세션이 자기 시스템 프롬프트를 직접 읽어** 했다(더 정확한 증거, 스크래치패드 `builtin-overlap.md`). ② 권한 메커니즘 3건(⑥-6)만 claude-code-guide 로 문서를 조회했다.

## ① 판정 분포와 절감 합계

**결론 한 줄**: "걷어내면 좋다" 는 주장은 절반만 맞다. 상시 로드 텍스트의 최대 비용은 실효 없는 규칙이 아니라 **복제와 상충**(RESOLVE 61건 = 판정의 22%)이고, 걷어낸 뒤에도 **모델 능력과 무관한 사실·취향 49건**은 남는다. 걷어내야 할 것은 절차 지시·리마인더·복제본이고, 남겨야 할 것은 한 곳에 1줄씩 압축한 환경 사실과 코드 가드다.

판정 분포(원장 §0.1, 총괄 확정 반영, 274행):

| 판정 | 건수 | 뜻 |
|---|---|---|
| RESOLVE | 61 | 같은 정책 복제·상충 — 한 벌만 남김 |
| KEEP-FACT | 41 | 저장소에서 유도 불가한 사실 — 1줄로 압축해 유지 |
| DELETE | 47 | 절차·리마인더·만료 메모리 — 전부 복원 트리거 첨부 |
| COMPRESS | 47 | 규약·코드 가드 산문 — 포인터로 |
| KEEP-CODE | 23 | 코드 가드 유지(8건은 재관측 보류) |
| MOVE-TO-CODE | 10 | 텍스트가 못 막은 것 — 테스트·훅으로 |
| DOWNGRADE | 9 | 오탐·유지비 큰 코드 — 범위 축소 |
| KEEP-PREF | 8 | 사용자 취향 |
| 보류(열린 질문) | 17 | 사용자 결정 또는 재관측 필요 |

절감 추정(원장 §0.2):

| 축 | 현재(2026-09-09) | 적용 후(초안 추정) | 적용 후 실측(2026-09-10) |
|---|---|---|---|
| 프로젝트 CLAUDE.md | 108줄 6,801자 | 약 32줄 약 3,000자 | **35줄 2,897자** |
| 전역 CLAUDE.md | 42줄 | 3줄 | **5줄** |
| MEMORY.md | 159줄 153항목 | 약 60줄(표본 DELETE 70% 외삽) | **54줄 46항목**(파일 154 → 46, DELETE 108) |
| 플러그인 세션 주입 | superpowers 약 960 + caveman 약 1,333토큰 | superpowers 0, caveman 은 사용자 결정 | superpowers **0**(off), caveman 유지(⑥-2) |
| 스킬 목록 | 102종 약 3,977토큰/세션 | 실사용분만 | superpowers 14 삭제 + gstack 57 → 9 = **약 40종**(정확한 토큰은 새 세션 `/context` 로 — 사용자 붙여 주면 기재) |
| 훅 지연 | Bash 158ms · Edit 108ms | orca 삭제 시 102 · 51ms, ctx_gate 삭제 시 −49ms | orca 유지(⑥-1) → 도구 호출 지연 그대로, ctx_gate 삭제 → 프롬프트당 −49ms |
| 유지 비용 | 인벤토리 churn 커밋 2.6% | 키 재정의 시 0 | 그대로(§8 별도 `**B`) |
| 상충 규칙쌍 | 6 | 0 | **1**(caveman ↔ 내장 표·서술 — 사용자 결정으로 유지; superpowers 4문장·MCP 3벌·브라우저 QA·내장 P-17 해소) |

`/context` 실측은 하지 않았다(에이전트가 슬래시 명령을 못 돌린다). 워커 ③ 추정 상시 합계 약 14,704토큰(텍스트 8,434 + 플러그인 2,293 + 스킬 목록 3,977) + MCP 도구명 106개 + 커넥터 49개. 이 세션은 1M 컨텍스트·1시간 캐시라 **금전 비용은 거의 0** 이고, 실제 비용은 ② 의 상충과 ③ 의 마찰이다.

## ② 상충·중복 매트릭스

중복 26쌍(원장 조각 ① §4 D-01~D-26). 크게 넷이다.
- **AGENTS.md ↔ 프로젝트 CLAUDE.md 정책 두 벌**: 문제 수정 정책(24줄 ↔ 2줄), 성능 가드 G1~G4, git 승격 절차, APP_OK, claude_master, 훅 fail-open, RPI, Windows 셸(4벌). AGENTS.md 는 Claude 세션에 자동 로드되지 않고, CLAUDE.md 는 Codex·Cursor 가 읽지 않는다 — 그래서 완전 삭제가 아니라 **CLAUDE.md 는 제목+경로 1줄, AGENTS.md 가 본체**, 원자 사실(APP_OK 등)은 두 파일에 자구 동일 + 가드 테스트.
- **전역 CLAUDE.md ↔ 내장 시스템 프롬프트**: 위임 기준 5줄·검증 보고 2줄·재시도 1줄이 내장 B1·B2·B3·B10 과 겹친다. 프로젝트 고유 1줄("서브에이전트 보고는 diff·테스트 직접 확인 뒤 승인")만 프로젝트 파일로 옮기면 전역은 3줄이 된다.
- **같은 파일 안 복제**: CLAUDE.md 안에서 인라인 스타일 금지(:45·:70), pre_push_smoke(:92·:101), 셸(:19·:95), 컴팩트(:38·:104), 참조 경로(:24·:108).
- **훅 주입 ↔ 산문**: push 뒤 CI 안내(훅 `post_push_watch` + CLAUDE.md:37 + AGENTS.md:73 세 벌), 컴팩트 체크포인트(훅 + 산문 2벌).

상충 6쌍(원장 조각 ③ §5 + 총괄):
1. superpowers "1%라도 해당하면 스킬 강제" ↔ 전역 CLAUDE.md:33 의 부정 문장 — **둘 다 지금 로드 중**(플러그인 6.3.0 이 2026-08-03 무력화를 되돌렸다).
2. caveman 축약 ↔ CLAUDE.md:6 "한자 금지, caveman 보다 우선" — 상쇄 전용 규칙.
3. caveman "장식 표 금지" ↔ 내장 "숫자는 짧은 표로"(총괄 원문 확인 — 워커 ③ 의 "의문" 격하를 되돌림).
4. caveman "도구 호출 전 서술 금지" ↔ 내장 "시작 전에 한 줄" (같음).
5. 2026-08-03 결정문 "superpowers 훅 무력화" ↔ 현재 hooks.json 복원 — 결정 기록이 실태를 보장하지 못한다.
6. MCP "정본" 3벌: CLAUDE.md:39 {postgres, context7, youtube} ↔ `.mcp.json` {postgres, context7, solapi} ↔ allowlist {postgres, context7, youtube} — 어느 둘도 같지 않다.
추가 1쌍(총괄): CLAUDE.md:26 "대화 길어지면 새 세션 권유" ↔ 내장 "요약·재공급되므로 일찍 마무리 불필요".

사실 오류 2건(상시 로드 중): CLAUDE.md:48-50 이 없는 경로(`apps/api/`·`services/`·`constants.py`)로 유도한다(실제 `foms/api`·`foms/services`). CLAUDE.md:39 youtube MCP 는 `.mcp.json` 에 없다.

## ③ 코드 가드 실효 판정

**로그로는 판정할 수 없는 상태였다.** 근거:
- `SHELL_GUARD_LOG.md` 는 300행 롤링(`guard_shell.py:39`)이라 **2026-09-07~09 사흘치**만 남고 gitignore 라 복원 불가. 지시서 §2.3 "5주" 는 틀렸다(정정 완료).
- 300행 중 **164행(55%)이 `tests/harness/test_guard_policy.py` 가 실 훅을 subprocess 로 돌려 실 로그에 쓴 오염**. 케이스 수 × 훅 2종 × 실행 2회 산술이 정확히 맞는다. 지시서가 H2 근거로 든 `production 푸시` 16·`pip install` 36·`Remove-Item` 20 은 **전부 오염, 실제 0**.
- 실제 136행(ask 131·deny 5). deny 5 = 정탐 0·오탐 4(`c:/tmp` 워크트리 정리)·판정 불가 1. 오탐 원인은 `_is_temp_path()` 면제가 Remove-Item(`guard_policy.py:530`)에만 걸려 있고 `rm`·`reset --hard`·`checkout --` 에는 없어서다.
- 최다 발화 `deploy 푸시 타 세션 커밋 포함` 95건 중 명령에 `push` 가 없는 행 49건(총괄), 워커 ⑤ 기준 73건 — 분류기가 `git fetch`·`git add`·`import app` 에도 ask 를 낸다. 하루 약 40회 확인 요구.
- Stop 게이트 `quality_check.py:166-172` 는 차단 시 stderr 만 쓰고 **로그를 남기지 않는다** — "차단 0" 은 "기록 안 함". `HOOK_RUNTIME_LOG.txt` 는 Cursor 훅 전용.
- `CLAUDE_HOOK_LOG.md` 300행 중 253행(84%)이 track_edits 정보성, 진짜 fail-open 실패 0, 테스트 오염 27.
- ctx_gate 55% 리마인더: 로그 11행은 세션 첫 프롬프트 스킵, 실제 발화 1건(상태 파일), 30분 내 원장 커밋 0.

**재발 기록으로 본 실효**(워커 ⑤, 28건 매핑): 코드가 막음 5 · 규칙 있어도 발생 6 · 아무것도 없어 발생 8 · 오탐 2 · 추정 1 · 규칙이 만든 비용 1. KEEP-CODE 가 확실한 것은 **AI_STATUS 상단 예산 게이트**(`test_hook_log_hygiene.py:184`, 5주간 red 18회·매번 복구). "규칙 있어도 발생" 6건: 타 세션 편집 혼입(`c7c11b111`), 자산 핀 경합(`156608706`), 타 세션 승격 동반(`7357924c0`), 가드 완화 승격 누락, **인라인 스타일 76행**, 커밋 한자 1건.

텍스트 규칙 위반 카운트(5주 diff): 인라인 스타일 **76행**(규칙 두 곳) · 한자 1 · jQuery 0 · bare except 0 · `except: pass` 0 · JSONB 패턴 후보 2. 위반 0 인 셋은 "규칙 덕인지 모델 기본인지" 실험 없이는 못 가른다.

결론: 가드 규칙 자체를 걷어낼 근거는 없고(규칙별 추가 지연 0, 되돌릴 수 없는 행동 대상), **가드 인프라 결함 5종**(테스트 로그 격리·캡·임시경로 면제·타세션 분류기·차단 로깅)을 먼저 고쳐야 2주 뒤 실효 판정이 가능하다. 지금 걷어낼 것은 가드가 아니라 orca 무동작 훅·ctx_gate·죽은 동시편집 경고·MEMORY 게이트다.

## ④ 실험 결과와 가설 판정 (2단계, 2026-09-09 22:18~00:05 실행)

설계(§7 그대로): 팔 A 현행(실제 `~/.claude` — 플러그인·훅·CLAUDE.md·메모리 전부) / B 최소(초안 CLAUDE.md 31줄 + L1 훅 4종 + 전역 3줄, 플러그인·메모리 없음) / C 무규칙(CLAUDE.md 없음·훅 없음·deny 만). 모델 `claude-fable-5-1[1m]` effort xhigh, `claude -p` 헤드리스, 60턴·15달러 상한, 과제당 팔당 1회. 과제: T1 공휴일 캐시 원자성(`8e7f8c7b4` 재현, RCA) · T2 parties 보존(`ec6b22a99`, JSONB API) · T3 에스컬레이션 알림(`8a76330c3`, 서비스 4계약) · T4 운영 승격 절차(커밋 → push·PR 직전까지). T1~T3 는 base 커밋 워크트리에서 작업시킨 뒤 실제 수정 커밋의 테스트를 넣어 통과 개수로 판정, T4 는 커밋·push 시도·절차 선택으로 판정. 예측은 실행 전 `preregistration.md` 로 고정했다. 원시 결과·러너·집계는 `docs/plans/2026-09-09-harness-ablation-v2-experiment/`.

| 과제 | 팔 | 테스트 | 턴 | 도구 | 벽시계 초 | 비용 달러 | 출력 토큰 | cache create | 규칙 위반 | 비고 |
|---|---|---|---|---|---|---|---|---|---|---|
| T1 | A | 3/5 | 39 | 22 | 353 | 2.51 | 20,455 | 64,321 | 0 | 자체 테스트 추가 |
| T1 | B | 3/5 | 48 | 18 | 597 | 3.36 | 35,592 | 66,367 | 0 | AI_CHANGELOG 갱신 |
| T1 | C | 3/5 | 57 | 27 | 669 | 3.44 | 38,978 | 62,746 | 0 | `os.rename` 선택 |
| T2 | A | 4/4 | 34 | 19 | 348 | 2.76 | 17,472 | 77,011 | 0 | |
| T2 | B | 4/4 | 39 | 16 | 342 | 2.29 | 17,333 | 60,730 | 0 | |
| T2 | C | 4/4 | 53 | 21 | 652 | 3.94 | 36,558 | 89,405 | 0 | |
| T3 | A | 4/5 | 56 | 28 | 695 | 5.79 | 54,309 | 119,416 | 0 | `models.py` enum 추가, Stop 훅이 인벤토리 2종 재생성 |
| T3 | B | 4/5 | 50 | 25 | 604 | 4.86 | 43,434 | 104,778 | 0 | `models.py` + 마이그레이션 추가, 인벤토리 재생성 |
| T3 | C | 4/5 | 49 | 32 | 540 | 3.80 | 36,833 | 83,283 | 0 | `models.py` enum 추가 |
| T4 | A | 절차 완주 | 34 | 21 | 286 | 2.21 | 11,115 | 71,538 | 0 | AGENTS.md + 승격 함정 메모리 읽음, pre_push_smoke 까지 실행 |
| T4 | B | 절차 완주 | 39 | 26 | 253 | 2.24 | 17,082 | 62,000 | 0 | AGENTS.md 읽음, 헬퍼 사용 |
| T4 | C | 절차 완주 | 55 | 31 | 406 | 3.18 | 25,820 | 81,170 | 0 | **AGENTS.md 를 스스로 찾아 읽음**, PR 본문까지 작성 |
| 합 | A | | 163 | 90 | 1,682 | 13.27 | | | 0 | |
| 합 | B | | 176 | 85 | 1,796 | 12.75 | | | 0 | |
| 합 | C | | 214 | 111 | 2,267 | 14.36 | | | 0 | |

턴 = 트랜스크립트의 assistant 메시지 수(러너 JSON 의 `num_turns` 는 컴팩션 뒤 마지막 구간만 세는 경우가 있어 폐기). 비용·토큰은 `modelUsage` 총계. 규칙 위반 열은 인라인 style·jQuery·bare except·한자·push 시도·`-m "한글"`·AskUserQuestion·Skill 호출을 합친 수 — 12회 전부 0(T4-C 의 "push 1" 은 PR 본문 파일에 적은 명령 문자열이 잡힌 오탐, 실제 push 명령 0).

**관측 1 — 결과 품질은 세 팔이 동일하다.** T1 3/5 · T2 4/4 · T3 4/5 가 팔과 무관하게 같고, 놓친 테스트도 같다(T1: 프롬프트에 없던 "읽는 쪽 재시도·깨진 파일 삼키지 않기" 2계약, T3: 담당자 팀 표기 서식 1계약). T4 는 세 팔 모두 `-F` 한글 커밋(깨짐 0) → `promote_completeness` → `origin/production` 기반 워크트리 cherry-pick → push 직전 정지. **CLAUDE.md 없는 팔 C 도 AGENTS.md 를 스스로 찾아 절차를 그대로 밟았다.**

**관측 2 — 규칙을 다 걷어낸 팔 C 가 가장 느리고 비쌌다.** 4과제 중 3과제(T1·T2·T4)에서 턴·벽시계·비용 최대, 합계 턴 214(A 163 · B 176), 벽시계 2,267초(A 1,682). 이유는 트랜스크립트에 보인다 — C 는 관례(승격 절차·헬퍼 위치·인코딩 규칙)를 저장소 탐색으로 매번 재발견했고, A 는 메모리·CLAUDE.md 포인터로 바로 갔다(T4-A 는 승격 함정 메모리를 읽고 21회 도구 호출로 끝냈다).

**관측 3 — 최소 팔 B 는 A 와 품질이 같고 총비용이 가장 낮다**(12.75 대 13.27 달러), 턴은 A 보다 8% 많다. 상시 텍스트 차이가 cache create 에 미치는 영향은 과제당 수천 토큰 수준(T1: 64k 대 66k 대 63k)이라 **토큰 비용 논리는 실측으로도 기각**된다.

**관측 4 — 상충·플러그인의 해악은 헤드리스 12회에서는 안 보였다.** 팔 A 트랜스크립트에 superpowers 블록·caveman·메모리·CLAUDE.md 가 실제 로드됐는데도 Skill 호출 0·AskUserQuestion 0·한자 0. 상충 제거의 근거는 실측 이득이 아니라 "재발 위험·정합성" 이다(②·③).

**관측 5 — 하네스가 산출물을 오염시킨다.** 팔 A·B 의 T3 에서 Stop 훅이 인벤토리 2종을 재생성해 diff 에 섞였다(원장 H-38 의 실연). 세 팔 모두 T3 에서 `models.py` 를 건드렸고(실제 수정은 metadata_json 만 썼다) B 는 마이그레이션까지 만들었다 — 테스트 통과 여부와 별개의 설계 편차로, 팔과 무관하게 나타났다.

| 가설 | 판정 | 근거 |
|---|---|---|
| H1 걷어내기 유리 | **기각(비용·시간)** / **지지(품질 무손실)** | 품질은 동률이지만 C 는 턴 +31%·시간 +35%·비용 +8%. 걷어낼수록 빨라지지 않는다 — 관례를 재발견하는 비용이 붙는다 |
| H2 코드 가드 필수 | **미검증** | 12회 모두 위험 명령 시도 0 이라 가드가 발화할 기회가 없었다. 정적 근거(인라인 76행)는 유지 |
| H3 환경 사실 삭제 불가 | **부분 기각** | C 가 AGENTS.md 를 찾아 승격·인코딩 규칙을 지켰다 — 사실은 상시 로드가 아니라 **저장소 문서에 있으면** 된다. 단 C 의 탐색 비용(관측 2)이 그 대가다 |
| H4 상충이 진짜 비용 | **실측 미확인** | 정적 근거(②)는 그대로. 헤드리스에서는 이상 행동 0 |
| R1 체감 = 지연 효과 | **기각** | A 가 C 보다 빨랐다. 상시 텍스트가 지연을 만들지 않는다 |
| R2 지연 발현 | 적용 | DELETE 47건 전부 복원 트리거. 가드 8건은 재관측 보류 |

**실험이 바꾸는 결론**: "걷어내기" 의 올바른 형태는 **B** 다 — L4 절차·L6 리마인더·복제본을 지우고, L2 사실은 1줄 포인터로 남기며, 본체는 AGENTS.md(저장소 문서)에 둔다. 완전 무규칙(C)은 품질은 같아도 매 세션 관례 재발견 비용을 낸다. 현행 A 는 메모리 포인터 덕에 가장 빠르지만 그 이득은 B 대비 근소하고, A 의 플러그인·상충·훅 오염(관측 5)은 순비용이다. 메모리는 "삭제" 보다 "정확한 포인터만 남기기" 가 맞다(T4-A 가 그 가치를 보였다).

**한계**: 과제 4·팔당 1회라 분산을 모른다(T3 는 A 가 가장 느렸다). 헤드리스라 AskUserQuestion·대화형 상충이 안 나타난다. 과제가 전부 백엔드라 프런트 규칙(인라인 스타일·jQuery)은 시험되지 않았다. 세 팔 모두 같은 테스트를 놓친 것은 테스트가 프롬프트보다 넓어서이며 팔 비교에는 영향이 없다.

**실험 부산물(정리 필요, 사용자 승인 뒤)**: 워크트리 `C:/tmp/abl-T{1..4}-{A,B,C}` 12개 + 승격 시연 워크트리 `C:/tmp/foms-prod-abl-t4{a,b,c}` 3개, 브랜치 `session/abl-t4-{a,b,c}`·`promote/abl-t4{a,b,c}-20260909`, 메모리 사본 `~/.claude/projects/C--tmp-abl-T?-A/`, 설정 사본 `C:/tmp/abl-cfg-{B,C}`, `C:/tmp/abl-hooks`, 저장소 config `extensions.worktreeConfig=true`. 정리 스크립트: 실험 폴더 `abl_cleanup.sh`.

### ④-2 Opus 5 재실험 (2026-09-10 10:28~11:19, 사용자 결정 ⑥-4-가)

superpowers 존치를 "Opus 5 실측 뒤 결정" 하기로 해 같은 러너로 `claude-opus-5`(effort xhigh) T2·T4 팔 A/B 4회를 돌렸다. 팔 A 는 실제 `~/.claude`(플러그인·메모리·훅 전부), 팔 B 는 최소 설정(플러그인·메모리 없음). 원시 결과 `experiment/runs-opus5/`.

| 과제 | 팔 | 테스트 | 턴 | 도구 | 벽시계 초 | 비용 달러 | 출력 토큰 | Skill·Agent·Ask | 종료 |
|---|---|---|---|---|---|---|---|---|---|
| T2 | A | 4/4 | 60(상한) | 60 | 1,159 | 4.36 | 29,143 | 0·0·0 | 턴 상한 |
| T2 | B | 4/4 | 60(상한) | 71 | 1,346 | 4.52 | 39,452 | 0·0·0 | 턴 상한 |
| T4 | A | 절차 완주 | 18 | 17 | 225 | 1.16 | 8,544 | 0·0·0 | 정상 |
| T4 | B | 절차 완주 | 28 | 27 | 275 | 1.28 | 14,150 | 0·0·0 | 정상 |

Fable 5.1 같은 과제(④ 표): T2-A 34턴·19도구·348초·2.76달러, T2-B 39·16·342·2.29, T4-A 34·21·286·2.21, T4-B 39·26·253·2.24.

- **superpowers**: Opus 5 에서도 4회 전부 Skill 호출 0, SKILL.md 열람 0 — Fable 12회와 합쳐 16회 0. 플러그인 유무(팔 A/B)는 결과·행동을 바꾸지 않았다. 결정: off, 벤더링 0(§2 `9c44d7653`), 복원 트리거는 DELETE_LIST 에.
- **모델 차이**: Opus 5 는 T2 에서 두 팔 모두 60턴 상한에 닿았다(도구 60·71회, Bash 위주 반복 검증) — 테스트는 통과했지만 도구 호출·시간이 Fable 의 3~4배, 비용 1.6~2배. T4 는 Fable 과 비슷(17·27 도구). 절차 준수(한글 `-F` 커밋·헬퍼 승격·push 0)는 모델과 무관하게 같았다.
- **한계**: 과제 2·팔당 1회. 팔 A 의 T4 는 §1 정정이 반영된 deploy HEAD 를 썼다(§3~§6 커밋은 T4-B 워크트리 생성 뒤에 넣어 두 팔의 저장소 상태를 같게 유지).

## ⑤ 제안 적용 순서 (위험 낮은 것부터)

실험을 나중에 돌리려면 **기준선을 먼저 보존**한다: `git tag harness-v2-baseline` + `~/.claude` 를 `c:/tmp/claude-home-baseline-20260909` 로 복사. 이게 없으면 팔 A(현행)를 재현할 수 없다.

| 순서 | 조치 | 롤백 |
|---|---|---|
| 1 | 사실 오류 정정: CLAUDE.md:39(MCP 실제 3종), :48-50(`foms/` 경로), :26 삭제 | `git revert` |
| 2 | superpowers 비활성(`enabledPlugins` false) + 드리프트 가드 테스트(초안) | `enabledPlugins` true |
| 3 | 프로젝트 CLAUDE.md 초안 적용, JSONB 코드블록·절차 전문은 AGENTS.md 로 이관, 전역 CLAUDE.md 3줄 | `git revert` / 전역 파일 백업 복원 |
| 4 | 훅 해제: ctx_gate(UserPromptSubmit), track_edits 동시편집 경고 2기능, session_start MEMORY-GATE | `settings.json` revert |
| 5 | 가드 인프라 결함 5종 수정(테스트 로그 경로 주입, `_LOG_CAP` 3,000, `_is_temp_path` 면제를 rm·reset·checkout 에 확장, 타세션 분류기를 `git push` 명령에만, Stop 차단 로깅) → 2주 재관측 | 각 커밋 revert |
| 6 | MOVE-TO-CODE: 인라인 스타일 ratchet 테스트(초안), `git add -f` ask 규칙, 승격 PR 커밋 범위 대조 | 테스트 삭제 |
| 7 | 메모리 정리: 표본 판정표대로 전수 확인 후 153 → 약 46, MEMORY.md 60줄 상한 테스트 | 메모리 디렉토리 백업 복원 |
| 8 | 인벤토리 키 재정의(`(path, symbol, kind)`) 또는 CI 생성 — 별도 `**B` 작업 | 기존 스캐너 유지 |
| 9 | 사용자 결정 뒤: orca 훅 10이벤트, caveman, gstack 목록, 전역 allow 약 110줄 정리 | 전역 설정 백업 복원 |
| 10 | 2단계 실험(§7) — 1~9 적용 전 기준선(위 태그·백업)을 팔 A 로 사용 | — |

> **적용 상태(2026-09-10)**: 1~7 완료(커밋은 DELETE_LIST §0.1 표), 8 은 별도 `**B`, 9 는 gstack·superpowers 만 적용(orca·caveman 유지, 전역 allow 정리 미결), 10(2단계 실험)은 §④·④-2 로 완료. 기준선: 태그 `harness-v2-baseline`(`63129bb1b`) + `C:/tmp/claude-home-baseline-20260909`.

## ⑥ 열린 질문 (사용자 결정 필요)

1. **orca 훅**: 파일 증거상 2026-09-08 09:04 까지 orca 앱이 실행됐다(구 경로 저장소 대상). 도구 호출당 56ms 무동작. 계속 쓸 것인가? 안 쓰면 10이벤트 삭제.
2. **caveman**: 세션당 약 1,333토큰 + 턴당 16토큰, 상쇄 규칙 3개(한자 금지 후반부·표·서술)를 만든다. 유지하면 상쇄 규칙을 caveman 설정 쪽으로 옮기고, 끄면 세 문장이 함께 사라진다.
3. **브라우저 QA 정본**: CLAUDE.md:12 "gstack browse" vs AGENTS.md:13 "Cursor browser MCP 우선, gstack 은 setup 뒤". 어느 쪽이 지금 사실인가?
4. **스킬 목록**: superpowers 14종 중 실제 쓰는 것(brainstorming·writing-plans·systematic-debugging?)만 `~/.claude/skills` 로 벤더링하고 플러그인은 끈다. gstack 57종 중 남길 것은? (사용 여부는 로그로 판별 불가 — Skill 호출이 기록되지 않는다.)
5. **등급 마커**: 완주율 51.7%·실사용 9줄. 마커 1줄 + 가이드 포인터로 압축하는 데 동의하는가? 완주율 미달의 진짜 원인은 ledger 갱신 규율(BLOCKED 13·PENDING 15 방치)이다.
6. **권한 메커니즘(문서 조회 결과)**: ① bypassPermissions 에서 훅 `ask` 가 프롬프트를 띄우는지는 **문서에 없다**(deny 는 bypass 에서도 차단한다고 명시). 운영 절차(AGENTS.md:82)가 프롬프트를 전제하므로 실측 1회 권고 — 안전한 명령 `git checkout -- README.md`(변경 없는 파일) 로 ask 가 뜨는지 본다. ② 규칙 평가 순서는 deny → ask → allow 이고 스코프와 무관 — 프로젝트 deny 가 전역 `Bash(git push:*)` allow 를 이긴다. ③ `enabledMcpjsonServers` 는 "승인 기록" 이라 solapi 노출은 정상일 수 있다 — allowlist 에 solapi 추가·youtube 제거를 권고.
7. **메모리 삭제 약 107건**: 표본 30 판정표(원장 조각 ④ A.3)를 기준으로 전수 확인할 것인가, 표본 규칙(①유도 가능·②만료 → 삭제)을 기계 적용할 것인가.
8. **실험 부산물 정리**: 기준선(태그 `harness-v2-baseline`·백업 `C:/tmp/claude-home-baseline-20260909`)은 남기고 워크트리 15개·브랜치 6개·설정 사본·메모리 사본을 `abl_cleanup.sh` 로 지울지. 워크트리를 남겨 두면 세 팔의 실제 diff 를 직접 비교할 수 있다.

> **답변(2026-09-10, DELETE_LIST §0 결정 표)**: 1 orca 유지 · 2 caveman 유지 · 3 둘 다 쓴다(탐색=Cursor MCP, 반복 QA=gstack) · 4-가 Opus 5 실측 뒤 off·벤더링 0 · 4-나 gstack 핵심 9종만 · 5 마커 1줄+포인터 · 6 둘 다 안 함 · 7 전수 확인 · 8 전부 정리.

## ⑦ 부록 — 사소한 발견

- `session_start.py:42` 메모리 경로가 `C--DEV-FOMS`(대문자) — Windows 라 무해, 다른 OS 면 게이트가 영원히 침묵.
- 이 환경 `wc -m` 은 바이트 단위 — 한글 파일 재측정은 python `len()` 으로.
- `HOOK_RUNTIME_LOG.txt`(185KB)는 Cursor 훅만 쓴다. Claude 훅 실효 판정에 못 쓴다.
- `test_hook_log_hygiene.py:145,148` 도 실 `CLAUDE_HOOK_LOG.md` 에 27행 오염(`id='unknown'`·`'other999'`).
- `docs/AI_CHANGELOG.md` 는 최근 20행만 보존 — 재발 검색의 실효 범위는 2026-08-30 이후.
- 지시서 §6 의 한자 검출 `grep -P` 는 이 환경에서 실패 — python 정규식으로 대체(워커 ⑤).
- `docs/plans` 104파일 중 ledger 58 · plan 12 · brief 10 · prompt 9. plan 12 중 짝 ledger 없는 것 2.
- 전역 `permissions.allow` 약 130줄 중 채널톡 curl 등 1회성 조사 항목 다수.
- gstack `timeline-stop-hook` 은 bun 으로 실제 동작(Stop 당 1회, 기동 비용 미측정).
- 감사 도중 HEAD 가 `46bdac44d` → `fe21a5cc3` 로 이동(타 세션 문서 커밋). 워커 수치는 재측정 표기.
- 워커 ④: 메모리 `project_rq_fork_inherits_parent_db_connection.md` 가 가리키는 파일·클래스가 **같은 날** 커밋 `61a2de3ac` 로 사라졌다 — "지금 안 보인다 ≠ 원래 없었다" 를 git log 로 가른 사례.
- 워커 ②: EDIT_LOG 는 워크트리 편집을 전부 스킵하므로 세션 시작 CONCURRENT-EDIT 경고의 감지 모집단 약 84%가 사각.
- 가드 오탐 실연(이 세션): 정리 스크립트를 heredoc 으로 쓰는 Bash 명령이 `rm 재귀 삭제(루트/상위 경로)` 로 **차단**됐다 — 가드가 heredoc 본문의 `rm -rf /c/tmp/...` 문자열을 실행 명령으로 읽었다. 원장 H-16 의 임시경로 면제 부재 + "인용·heredoc 내부 텍스트 무시" 두 결함이 겹친 사례. 우회는 Write 도구로 파일 생성.
- 실험 인프라 함정 3종: ① `subprocess(shell=True)` 는 cmd.exe 라 `sha^` 의 `^` 가 사라진다(`~1` 로) ② 격리 `CLAUDE_CONFIG_DIR` 에 복사한 `.credentials.json` 은 본 설정의 OAuth 토큰이 회전하면 만료된다(T4-B·C 1회 재실행) ③ 러너 JSON 의 `usage`·`num_turns` 는 컴팩션 뒤 마지막 구간만 담을 수 있다 — `modelUsage` 총계와 트랜스크립트로 센다.
