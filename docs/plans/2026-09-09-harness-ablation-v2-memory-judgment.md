# 메모리 전수 판정표 (ablation v2 §7, 2026-09-10)

> 규칙: feedback/user → KEEP-PREF · reference → 대상 실존 시 KEEP-FACT · project ①유도 가능/②참조 소멸 → DELETE, ③유도 불가 → KEEP-FACT. 기계 증거(memory_evidence.py)로 참조 실존을 센 뒤 에이전트 3개가 판정하고 총괄이 3건 재정(#56·#119·#141). DELETE 파일은 삭제 대신 `C:/tmp/memory-deleted-20260910/` 로 이동, 전체 백업 `C:/tmp/memory-baseline-20260910/`.

합계: 154행 — DELETE 108 · KEEP-FACT 23 · KEEP-FACT(COMPRESS) 10 · KEEP-PREF 13

| # | 파일명 | type | 갈래 | 근거(경로:행 · 확인 명령 결과) | 판정 | 복원 트리거 |
|---|---|---|---|---|---|---|
| 1 | feedback_advisor_worker_split.md | feedback | ③(전역 `~/.claude/CLAUDE.md` §위임·검증 프로토콜 그대로 요약, 리포 밖) | `cat` 으로 frontmatter `type: feedback` 직접 확인. 본문이 참조하는 전역 CLAUDE.md 절이 이번 세션 system-reminder 에도 그대로 로드됨(내용 일치) | KEEP-PREF | — |
| 2 | feedback_agent_results_verify.md | feedback | ①(사례로 든 파일들 실재) | `type: feedback` 확인. `ls templates/drawing/partials/workbench_dashboard_body.html templates/cs/partials/as_dashboard_body.html templates/construction/partials/dashboard_body.html` → 3개 전부 실재 | KEEP-PREF | — |
| 3 | feedback_announce_package_before_install.md | feedback | ①(가드 정책 실재) | `type: feedback` 확인. `git grep -n "pip install" tools/harness/guard_policy.py` → `679: """`pip install` 판정 — 기본 PyPI 설치는 allow, 공급망 위험 형태만 ask.` (본문 서술과 일치) | KEEP-PREF | — |
| 4 | feedback_ceo_multiagent_workflow_on_request.md | feedback | ①(레시피 산출물 실재) | `type: feedback` 확인(표본 M-02 선례). `ls docs/plans/2026-09-06-foms-system-review-*` 로 재확인은 범위 밖이나 frontmatter·본문 링크 [[project_system_review_prompt_set_2026_09]] 계열 실재 | KEEP-PREF | — |
| 5 | feedback_dev_server_qa_directly.md | feedback | ①(시크릿 명 실재) | `type: feedback` 확인. `git grep -rln "FOMS_STAGING_USERNAME" .github` → `perf-gate.yml`, `rum-daily.yml` 2건 실재 | KEEP-PREF | — |
| 6 | feedback_dont_silently_downscope.md | feedback | ③(사용자 지시 일화, 코드 무관) | `type: feedback` 확인(`cat` 직접 읽음) | KEEP-PREF | — |
| 7 | feedback_exhaustive_check_needs_negative_control.md | feedback | ③(검증 방법론, 코드 무관) | `type: feedback` 확인(`cat` 직접 읽음, 흡수본 포함) | KEEP-PREF | — |
| 8 | feedback_no_regex_keep_both_merge.md | feedback | ③(git 작업 규율, 코드 무관) | `type: feedback` 확인(`cat` 직접 읽음) | KEEP-PREF | — |
| 9 | feedback_review_findings_persist_now.md | feedback | ③(작업 습관, 코드 무관) | `type: feedback` 확인(`cat` 직접 읽음) | KEEP-PREF | — |
| 10 | feedback_verify_real_data_not_injected.md | feedback | ③(검증 방법론, 코드 무관) | `type: feedback` 확인(`cat` 직접 읽음) | KEEP-PREF | — |
| 11 | feedback_verify_ui_claim_on_screen.md | feedback | ③(검증 방법론, 코드 무관) | `type: feedback` 확인(`cat` 직접 읽음) | KEEP-PREF | — |
| 12 | project_account_self_service_v1.md | project | ① | `foms/services/security/account_requests.py:20` `APPROVAL_ACTIVE`·`:25` `RESET_PENDING` 등 상수 실재. `git cat-file -t a5529288`→commit, `git cat-file -t d1c24390`→commit(둘 다 실재), `c1a649f0`만 `fatal: Not a valid object name`(축약 SHA 8자 미스매치로 추정, 계열 커밋 자체는 존재 가능성 — 핵심 결론엔 무관) | DELETE | 셀프 가입 승인 흐름에서 PENDING 판정이나 재설정 큐 중복 처리가 다시 헷갈리는 결함이 나오면 |
| 13 | project_add_form_autosave_gap.md | project | ① | `ls static/js/foms/draft.js static/js/orders/erp-order-autosave.js tools/cron/cleanup_order_drafts.py tests/domains/test_erp_add_order_autosave.py` → 전부 실재. `static/js/orders/erp-order-autosave.js:264` `var keys = ["product_name", "spec", "price"];` 본문 서술과 정확히 일치 | DELETE | `/add` 자동저장 배선이 다시 끊기거나 draft 승격/자동닫힘 회귀가 재발하면 |
| 14 | project_alert_autodismiss_trap.md | project | ① | `static/js/runtime/script.js:10` `document.querySelectorAll('.alert:not([data-foms-no-autodismiss])')` 실재. `ls tests/domains/test_alert_autodismiss_contract.py` → 실재 | DELETE | 상시 UI가 `.alert` 클래스로 다시 5초 뒤 사라지는 회귀가 계약 테스트 밖에서 나오면 |
| 15 | project_alimtalk_trace_chip.md | project | ① | `foms/api/erp_orders_structured.py:259` `'alimtalk_measurement'`, `foms/api/kakao/__init__.py:146` `@kakao_bp.route('/alimtalk/confirm-channel/<int:order_id>', methods=['POST'])`, `ls foms/web/admin/alimtalk_failures.py` 실재 | DELETE | sd 기반 칩 렌더가 깨지거나 in_flight/재시도 상태를 실패로 오분류하는 회귀가 재발하면 |
| 16 | project_amount_clamp_zero_ceiling_swallows_input.md | project | ① | `ls static/js/foms/wizard.js foms/api/erp_order_draft.py` → 둘 다 실재 | DELETE | 신규 금액·수량 clamp 추가 시 `상한>0` 가드 누락으로 입력이 다시 삼켜지면 |
| 17 | project_apps_script_sheet_cell_limit_silent_loss.md | project | ③(리포 밖 Google Apps Script 사고, 총괄 특례 대상 예시와 동일 사례) | `find . -iname "*channeltalk-sheet-webhook*"` → 0건(리포 밖). `grep -rln "10000000\ / 셀.*한도" docs/` → 관련 문서 없음(사고 자체는 문서화 안 됨) — 브리프 §총괄 특례가 이 사례를 DELETE 예시로 명시 | DELETE | 저장소 밖 Google Apps Script 연동에서 같은 무음 유실 함정(로그 삼킴·200 OK 로 실패 덮기·셀 한도)이 재발하면 |
| 18 | project_as_axis_status_projection.md | project | ① | `foms/services/orders/state_axes.py:188` `def derive_as_axis_status`, `foms/services/erp_sync_columns.py:49` `def sync_erp_flat_columns`, `foms/api/orders/field_update.py:506` `as_overlay_outranks_status_write(order, value)` 전부 실재 | DELETE | AS 상태가 화면마다 다시 어긋나 증발하는 결함(체크리스트 자동승격 무가드 등)이 재발하면 |
| 19 | project_as_incomplete_map.md | project | ① | `foms/services/map_snapshot.py:178` `def build_as_incomplete_map_query`, `ls foms/api/erp_map.py foms/api/cs/as_map.py tests/domains/test_as_map_snapshot.py` 전부 실재. `git cat-file -t 37c6a3e6`/`f8a3efb0` → 둘 다 commit | DELETE | AS 지도 탭 건수가 대시보드 탭과 다시 어긋나면(`test_as_map_snapshot.py` red) |
| 20 | project_as_round_vs_cycle_number.md | project | ① | `foms/services/orders/as_log.py:173` `def current_as_round`, `foms/services/orders/as_round_chart.py:298` `def _assign_display_numbers` 실재. `git cat-file -t 0d71db72` → commit | DELETE | AS "N차" 표시가 판정 회차와 다시 혼동되는 표시 오류가 재발하면 |
| 21 | project_as_sales_delivery_assignment.md | project | ① | `ls foms/services/orders/sales_delivery_link.py foms/services/orders/sales_delivery_map.py docs/specs/2026-09-09-as-sales-delivery-measurement-assignment-design.md docs/plans/2026-09-09-as-sales-delivery-ledger.md` 전부 실재 | DELETE | `_AS_SERVER_OWNED_SHIPMENT_KEYS` 미등재로 서버 전용 키가 폼 저장마다 지워지거나 `kind=measurement` 후보 조회가 깨지면 |
| 22 | project_as_schedule_link_drift.md | project | ① | 증거표는 `api/shipment/recommendations.py` 부재라 했으나 실제 경로는 `foms/api/shipment/recommendations.py`(실재, 추출기 접두어 누락 오류). `foms/services/orders/as_schedule_link.py:205` `def evaluate_drift` 실재. `git cat-file -t 68be7a3a` → commit | DELETE | legacy 키 `schedule.as_visit.shipment_recommendation` 을 다시 쓰는 코드가 추가되면 |
| 23 | project_as_timeline_architecture.md | project | ①(②) — 아키텍처는 실재, SDD progress 경로만 소멸 | `ls foms/services/orders/as_log.py templates/cs/partials/as_card_macros.html` 실재. `foms/web/cs/as_dashboard.py:158` `render_as_timeline` 참조 실재. `find . -path "*.superpowers/sdd/2026-07-24-as-dashboard-timeline-plan*"` → 0건(소멸) | DELETE | `as_log` append-only 계약이나 서버 전용 키 가드(`_force_preserve_as_server_state`)가 깨지는 회귀가 재발하면 |
| 24 | project_as_visit_slot_is_per_order.md | project | ①(표본 M-04 판정 재확인) | `foms/services/orders/as_cycle_service.py:480` `_apply_visit_projection(sd, order, None, None)` 실재. `git cat-file -t 0d71db72` → commit. `ls tests/domains/test_state_as.py tests/domains/test_as_billing.py` 실재 | DELETE | AS 재접수 시 새 건이 직전 건 방문일을 물려받는 회귀가 재현되면(`test_state_as.py`/`test_as_billing.py` red) |
| 25 | project_backfill_test_reset_append_only_trap.md | project | ① | `ls foms/services/security/backfill/runs.py` 실재. `models.py:2521` `CREATE OR REPLACE FUNCTION foms_maintenance_backfill_approval_append_only()` 실재(증거표 부재는 추출 오류) | DELETE | PGTEST backfill 테스트 teardown 이 approvals 를 DELETE 해 append-only 트리거 위반이 재발하면 |
| 26 | project_board_status_controls_v2.md | project | ① | `foms/web/measurement/dashboard.py:664` `def regional_dashboard`, `ls tests/domains/test_regional_dashboard_buckets.py static/css/measurement/complete-order-btn.css` 실재. `tests/domains/test_state_controls.py:277` `assert not hasattr(status_constants, "REGIONAL_BOARD_STATUS")` | DELETE | 지방 대시보드에 status 드롭다운이 부활하거나 `REGIONAL_BOARD_STATUS` 가 되살아나면(계약 테스트 red) |
| 27 | project_bulk_complete_as_overlay_incident.md | project | ① | `foms/services/orders/stage_override.py:69` `def as_overlay_status`, `ls tests/domains/test_as_bulk_status_guard.py` 실재. `git cat-file -t 63737e91` → commit | DELETE | 일괄 완료처리가 AS 주문을 다시 기본 포함해 대시보드에서 증발시키면(`test_as_bulk_status_guard.py` red) |
| 28 | project_channeltalk_token_shared_session_expiry.md | project | ① | `foms/services/channel_client.py:182` `ttl = int(result["expiresIn"]) - _TOKEN_EXPIRY_BUFFER_SECONDS`, `:47` `_token_cache` 실재 — 메모리가 권고한 수정(고정 29분 대신 expiresIn 반영)이 이미 코드에 적용돼 있음 | DELETE | 채널톡 401 전송 실패가 재발하면(고정 TTL 캐싱으로 되돌리는 회귀) |
| 29 | project_ci_docscope_registry_gate.md | project | ① | `ls tests/domains/test_docs_facing_registry.py` 실재. `.github/workflows/ci.yml:114` `- name: Run docs-facing contracts (docs-only change)` 실재 | DELETE | docs 읽는 신규 계약 테스트가 ci.yml 서브셋 미등재로 CI-DOCSCOPE-01 이 다시 red 나면 |
| 30 | project_ci_flaky_is_shared_filesystem_race.md | project | ① | `.gitignore:164` `data/holidays_kr_*.json` 실재. `ls tests/domains/test_password_kdf_contract.py` 실재. `tests/postgres/conftest.py:148` 세션스코프 스키마 구현 실재 | DELETE | xdist 병렬에서 저장소 밖 런타임 파일 경합으로 CI 간헐 실패가 재발하면 |
| 31 | project_ci_watch_misses_perf_gate.md | project | ① | `.github/workflows/{ci,harness-ci,perf-gate,rum-daily,postgres-lane,visual-baseline-linux,coding-research-center-weekly}.yml` 전부 `ls` 로 실재(증거표의 2종 부재는 추출 오류). `ls tools/harness/ci_watch.py tools/perf/perf_budgets.json` 실재 | DELETE | `ci_watch.py` exit 0 만 보고 perf-gate/PG Lane red 를 놓치는 오판이 재발하면 |
| 32 | project_claude_master_test_account.md | project | ③(운영 DB 계정 상태·로컬 secrets 파일 경로, 표본 M-05 재확인) | `ls docs/guides/REAL_SERVER_TEST_ACCOUNT.md` 실재하나 정책 포인터만 있고 계정 id(58/57)·현재 수신번호(010-8327-7282)·secrets 파일 내용은 리포에 없음(로컬 전용 secrets, 커밋 금지가 정책) | KEEP-FACT | — |
| 33 | project_confirm_quest_and_drawing_attachment.md | project | ①(부분 구현 디테일 드리프트) | `foms/api/quest.py:187` `_COMMAND_REQUIRED_STAGES = frozenset({"DRAWING", "CONFIRM"})`(메모리는 "DRAWING만 남게 좁힘"이라 적었으나 실제는 CONFIRM도 그대로 포함 — 최종 승인은 `foms/services/orders/quest_transition_service.py:231` `def complete_confirm_quest`/`CUSTOMER_CONFIRM` 어댑터 경유로 구현, 이름만 드리프트). `foms/api/cs/confirm.py:47` `blueprint["customer_confirmed"] = True` 실재. `foms/web/drawing/workbench.py:362` `include_confirmed` 실재. `git log --oneline -- foms/api/quest.py` 상단 `d243c8e50`·`2ba0f5634`. production 반영 커밋 `ef9ffcb4f`(docs) 실재. `docs/plans/2026-09-09-confirm-quest-and-drawing-attachment-plan.md` 는 현재 파일 없음(`ls` 실패)이나 `git log --all` 로 `ce51ccd51`/`4868a2cc4` 커밋에서 경로 존재 확인(히스토리로 유도 가능) | DELETE | CONFIRM quest 승인이 다시 409 로 막히거나, 도면 탭이 전달 후에도 비거나, 작업실 '완료' 필터가 0건으로 돌아가면 |
| 34 | project_construction_card_drawing_current_files_leak.md | project | ① | `foms/services/construction_dashboard_display.py:119` `_NON_DRAWING_KEY_MARKERS = ("/attachments/",)`, `:122` `def _is_drawing_file_entry` 실재. `git cat-file -t 5a7a138b`/`8e40e0e3` → 둘 다 commit | DELETE | `drawing_only` 카드가 다시 `/attachments/` 항목을 포함해 실측·일반 첨부가 시공 카드로 유출되면 |
| 35 | project_context_ledger_hygiene_v2.md | project | ①(②) — AI_STATUS 40줄 예산은 실재, CTX-GATE 훅은 소멸 | `tests/harness/test_hook_log_hygiene.py:184` `def test_ai_status_head_budget` 실재(예산 부분 ①). `.claude/hooks/ctx_gate.py` 는 `ls` 실패(소스 없음, `.pyc` 바이트코드만 잔존) — `FOMS_CTX_GATE_PCT` 전역 grep 0건(② 소멸) | DELETE | AI_STATUS 상단 예산 계약이 깨지거나, CTX-GATE 류 컨텍스트 임계 자가정리 훅이 다시 필요해지면 |
| 36 | project_dashboard_cap_before_python_filter.md | project | ① | `foms/services/construction_read_model.py:54` `def apply_construction_list_scope_filter`, `foms/services/drawing_workbench_read_model.py:42` `def build_drawing_queue_filter`, `foms/web/measurement/dashboard.py:587` `def _fetch_legacy_dashboard_orders` 전부 실재 | DELETE | 대시보드가 SQL 캡 이후 파이썬에서 좁혀 섹션이 조용히 비는 회귀가 재발하면 |
| 37 | project_data_incident_recovery_toolkit.md | project | ①(저장소 안에 같은 사실이 이미 문서화됨 — 총괄 특례 4 적용) | `docs/guides/DATA_INCIDENT_RECOVERY.md:14` `**보존 6일**(Daily 스케줄 \`retentionSeconds=518400\`)` 로 본문과 동일 사실이 이미 문서화. `ls tools/ops/data_doctor.py tools/ops/railway_pitr.py` 실재 | DELETE | Railway 백업 보존 기간이 바뀌거나 도구 사용법이 문서와 어긋나는 사고가 재발하면 |
| 38 | project_docker_image_runtime_manifest_trap.md | project | ① | `.dockerignore:30` `!docs/harness/foms_write_guard_manifest.json` 등 negation 4건, `Dockerfile:37-40` 대응 `COPY` 4건 전부 실재(수정이 이미 적용됨) | DELETE | 새 런타임 로드 파일을 docs/에 추가하면서 negation+COPY 를 빠뜨려 이미지 부팅이 다시 파산하면 |
| 39 | project_drawing_cancel_transfer_status_regression.md | project | ① | `foms/web/drawing/workbench.py` 의 `can_confirm_receipt` 계산(860행대) 실재, `foms/api/drawing/erp_orders_revision.py:252` `def _resolve_revision_restore_status` 실재. `git log --oneline` 에서 `e66fbc42a fix(drawing): 2차 전달만 취소했는데 수령 확정 버튼이 통째로 사라졌다` 확인(브랜치 `fix/drawing-cancel-restore` 는 병합 후 삭제돼 부재이나 커밋은 실재) | DELETE | 도면 전달 취소가 다시 최신 TRANSFER 한 건만 되돌려 수령 확정 버튼이 사라지면 |
| 40 | project_drawing_empty_transfer_allowed.md | project | ① | `foms/api/drawing/erp_orders_drawing.py:49` `def perform_drawing_transfer` 실재. `erp-dashboard-drawing.js` 는 경로가 `static/js/orders/dashboard/erp-dashboard-drawing.js` 로 이동(파일 자체는 실재, 증거표 경로 드리프트). `tests/domains/test_production_change_notification.py:267` `def test_hook_drawing_transfer` 실재 | DELETE | 전달·제출류 API 가 결과물 0개인 요청을 다시 허용하는 신규 표면이 생기면 |
| 41 | project_drawing_tablet_review_station.md | project | ①(표본 M-06 재확인, 줄번호 드리프트) | `foms/web/drawing/workbench.py:477` `can_review_perm = bool(is_admin or (can_sales and not is_drawing_team))` 실재(메모리 인용 줄번호 `:717` 과 다름 — 드리프트). `ls docs/specs/2026-07-27-drawing-tablet-review-station-design.md static/js/foms/tablet-drawing-review.js` 실재 | DELETE | `not is_drawing_team` 조건이 신규 표면에서 빠져 도면팀에게 수정요청 버튼이 다시 노출되면 |
| 42 | project_drawing_wizard_architecture.md | project | ①(표본 M-07 재확인) | `ls docs/specs/2026-07-06-drawing-wizard_SPEC.md docs/specs/2026-07-06-drawing-wizard-v2_SPEC.md foms/web/drawing/wizard.py foms/api/drawing/wizard.py` 전부 실재 | DELETE | asset-raw 프록시를 우회해 R2 presigned URL 을 직접 참조하는 회귀가 나오면(html2canvas 내보내기 이미지 누락) |
| 43 | project_erp_draft_resurrection_race.md | project | ① | `foms/api/erp_orders_structured.py:1985` `def _lock_draft_row`, `:2009` `def _resolve_draft_for_autosave` 실재. `git cat-file -t 5e063282` → commit | DELETE | 자동저장 레이스로 승격된 주문이 다시 draft 로 되살아나는 증발 사고가 재발하면 |
| 44 | project_erp_estimate_outer_tab_guard.md | project | ① | `templates/orders/partials/edit_order_body.html:576` `{% set erp_estimate_in_outer_tab = true %}`, `templates/orders/partials/erp_order_tab.html:79` `{% if not erp_estimate_in_outer_tab %}` 실재 | DELETE | outer 탭 승격 변경 시 `erp_order_tab.html` 가드를 빼 add_order 화면에서 견적서 접근이 다시 사라지면 |
| 45 | project_erp_mobile_v2_cohort_gate.md | project | ① | `foms/services/context_processors.py:173` `erp_mobile_v2_enabled = is_mobile_v2_shell(shell_variant)` 실재. `templates/admin/layout.html:11`·`templates/auth/layout.html:11` 에 `erp-mobile-v2-layout` 클래스 게이트 실재(메모리는 `templates/orders/layout.html` 이라 적어 경로 드리프트). `find . -iname "erp_mobile_shell.html"` → `./templates/partials/shared/erp_mobile_shell.html` 실재(증거표 부재는 추출 오류) | DELETE | 신규 페이지가 3곳 게이트 중 하나를 빠뜨려 모바일 v2 셸이 레거시로 깨지면 |
| 46 | project_erp_stage_vs_as_axis_display.md | project | ① | `foms/api/erp_orders_structured.py:306` `def _pin_form_stage_to_server` 실재. `foms/api/orders/status.py:36` `from foms.services.orders.status_constants import AS_OVERLAY_PRESERVE_WORKFLOW_STAGE`, `:245` 사용 실재 | DELETE | `LOGISTICS_STATUS_PRESERVE_WORKFLOW_STAGE` 에 AS 코드가 다시 섞여 stage 가 덮이는 회귀가 재발하면 |
| 47 | project_erp_tab_return_reload_clobber.md | project | ① | `static/js/orders/erp-order-shared.js:5545-5547` `window.fomsErpAutosave` / `_autosave.isDirty()` dirty 가드 실재 | DELETE | 탭복귀 시 dirty 가드 없이 `erpLoadStructured` 가 무조건 재조회해 미저장 입력이 다시 유실되면 |
| 48 | project_foms_mobile_select_sheet.md | project | ① | `ls static/js/components/foms-mobile-select.js static/css/components/foms-mobile-select.css` 전부 실재 | DELETE | 계산기 페이지에서 `.form-select`+`data-sheet` 동시 부여로 이중 바인딩 회귀가 재발하면 |
| 49 | project_fragment_asset_lifecycle_traps.md | project | ① | `static/js/runtime/erp-shell.js:421` `STYLE_PRELOAD_TIMEOUT_MS = 1500`, `:510` `function preloadFragmentStylesheets` 실재. `ls tests/domains/test_gnav_fragment_contract.py` 실재 | DELETE | fragment swap 인라인 스크립트가 DOMContentLoaded 래퍼로 다시 작성돼 재실행 시 죽으면 |
| 50 | project_fragment_body_volatile_axes.md | project | ① | `ls foms/services/common/fragment_revalidation.py` 실재 | DELETE | 프래그먼트 캐시·ETag 키를 세션 digest·릴리스 축 없이 새로 설계해 403/스테일 마크업이 재발하면 |
| 51 | project_fragment_measure_needs_shell_header.md | project | ①(표본 M-08 재확인) | `foms/services/common/erp_navigation_contract.py:86` `ERP_SHELL_REQUEST_HEADER: str = "X-FOMS-ERP-SHELL"` 실재, `foms/services/common/erp_shell_http.py:18` 사용 실재 | DELETE | 프래그먼트 크기를 손측정하며 이 헤더를 빼먹어 잘못된 결론이 다시 보고되면 |
| 52 | project_geocode_status_and_sidefx_key.md | project | ①(저장소 안에 같은 사실이 이미 문서화됨 — 총괄 특례 4 적용) | `foms/services/geocode_retry.py:81` `def canonicalize_status`, `:101` `def should_retry_geocode` 실재(4상태 판정 로직 ①). SIDEFX 키 부재 사고는 `docs/plans/2026-09-01-geocode-transient-vs-data-error-plan.md:311` `## T10 — 최초 트리거를 찾았다: SIDEFX 서비스에 카카오 키가 없다` 로 리포 안에 이미 문서화 | DELETE | 지오코딩 신규 상태값 추가 시 읽기 경로 정규화 2곳을 빠뜨리거나, 서비스 목록을 다시 일부만 확인해 같은 오진이 재발하면 |
| 53 | project_get_today_kst_returns_date.md | project | ① | `foms/services/datetime_kst.py:20 def get_today_kst() -> datetime.date` 실재. `foms/web/shipment/dashboard.py:138 def _pick_shipment_search_focus_date` 실재. `tests/domains/test_erp_mobile_layout_and_shipment.py:502 def test_shipment_search_focus_date_does_not_500_on_scheduled_match` 실재 | DELETE | `get_today_kst()` 결과에 `.date()` 를 다시 호출하는 코드가 들어와 출고 검색 500 이 재현되면 |
| 54 | project_git_worktree_and_push_discipline.md | project | ③ | `tools/harness/push_own_session_commits.py` 실재(git add+pathspec commit 메커니즘까지만 코드화, 본문의 "251 ahead/1322 behind 영구 분기"·"HEAD 재구성 후 --only" 같은 사고 경위·복구 절차는 코드/주석 어디에도 없음). SHA `f90c8230a` 실재(`git log --oneline -1`) | KEEP-FACT(COMPRESS) | — |
| 55 | project_harness_ablation_2026_08.md | project | ① | `docs/harness/policy/DECISIONS.md:33` `### [2026-08-03] 하네스 전면 ablation` 항목이 메모리 본문과 사실상 동일한 문장(삭제 목록·생존 판정 기준·다음 ablation 2027-02)으로 이미 기록됨 | DELETE | DECISIONS.md 의 이 항목이 유실되어 2026-08-03 ablation 근거를 다시 찾아야 하면 |
| 56 | project_harness_ablation_v2_meta_prompt_2026_09.md | reference | ①(포인터 대상 전부 실재, 핵심 수치·결론도 report.md 에 이미 문서화) | `ls docs/plans/2026-09-09-harness-ablation-v2-{meta-prompt,ledger,report}.md` 3개 확인, `docs/plans/2026-09-09-harness-ablation-v2-drafts/` 5개 파일 확인, `git tag -l harness-v2-baseline` 실재. `report.md:104` "최소 팔 B 는 A 와 품질이 같고 총비용이 가장 낮다"·`:112` "턴 +31%·시간 +35%" 로 메모리의 실험 결론과 동일 수치가 이미 문서화됨 — 총괄 재정: type=reference 는 대상 실존 시 유지(원장 §0 M-26 재정과 동일 규칙) | KEEP-FACT | 같은 위치를 다시 못 찾아 세션이 헤매면(또는 report.md 가 삭제되면) |
| 57 | project_heartbeat_budget_single_source.md | project | ①(단 `origin/production` 에만 존재, 로컬 `deploy` HEAD 에는 없음) | 로컬 트리 `git grep -rn "effective_heartbeat_budget\ / WORKER_KIND_SPECS" -- '*.py'` → 0건, `docs/incidents/2026-09-09-worker-watchdog-false-stall-flap.md` 로컬 부재. 그러나 `git show origin/production:foms/services/worker_watchdog.py` · `git show origin/production:docs/incidents/2026-09-09-worker-watchdog-false-stall-flap.md` 둘 다 실재 확인(동일 사고 서술 원문 포함) | DELETE | 하트비트 판정부가 다시 두 벌로 갈라져 헛알림이 재발하면(코드가 deploy 로 넘어온 뒤 재확인) |
| 58 | project_inapp_webview_save_traps.md | project | ① | `static/js/measurement/image-export.js` 실재, `static/js/orders/estimate-preview.js` 실재(메모리는 경로 미기재라 드리프트 아님), `tests/domains/test_order_share_view.py:852 def test_share_pages_pin_shared_assets_to_same_version` 실재 | DELETE | 카톡 인앱·iOS 웹뷰에서 저장/복사 무반응 신고가 재발하면 |
| 59 | project_inventory_lineno_free_gate.md | project | ① | `tests/domains/test_failopen_inventory.py:142 _lineno_free`·`:154 test_inventory_matches_fresh_scan` 실재. `scripts/ops/pre_push_smoke.ps1` 에 failopen/rev_99/state_guard/write_guard 4종 서브셋 등재 확인 | DELETE | 인벤토리 드리프트 게이트가 줄밀림만으로 다시 CI red 나면 |
| 60 | project_ios_beforeunload_unreliable_draft_flush.md | project | ① | `static/js/foms/draft.js:203-204` 주석이 메모리 문장과 거의 동일("iOS Safari 는 앱 전환·홈버튼 이탈에서 beforeunload 를 신뢰성 있게 쏘지 않는다"), `:241-243` beforeunload+pagehide+visibilitychange 3경로 리스너 실재 | DELETE | 모바일 자동저장에서 "값이 사라졌다" 신고가 재발하면(신규 자동저장 훅이 3경로를 안 걸면) |
| 61 | project_ios_capture_optout.md | project | ① | 표본 M-09 재확인: `static/js/foms/photo-capture.js:14 shouldUseEnvironmentCapture`·`:29 applyEnvironmentCapture` 실재, `tests/domains/test_photo_capture.py` 실재 | DELETE | iOS 신규 업로드 input 이 `data-foms-no-capture` 없이 추가돼 카메라 강제가 재발하면(`test_photo_capture.py` red) |
| 62 | project_ios_select_native_picker_focus.md | project | ① | `static/js/components/foms-mobile-select.js` 실재, `tests/domains/test_mobile_select_ios_contract.py` 실재, SHA `eb12e220`=commit 실재(`git cat-file -t`) | DELETE | 아이폰 select 피커 이중 노출("고르면 또 뜬다")이 재발하면(`test_mobile_select_ios_contract.py` red) |
| 63 | project_kakao_map_local_dev_domain_block.md | project | ③ | 표본 M-10 과 동일 갈래(Kakao Developers 콘솔의 도메인 등록 상태 = 외부 SaaS 설정, 저장소로 유도 불가). `foms/services/common/geocode_config.py:32 KAKAO_JS_API_KEY = "28bd94a6ae70d28d1b9226bd4b88e595"` 키 상수 자체는 실재하나 그 키의 콘솔 도메인 등록 여부는 리포 밖 | KEEP-FACT | — |
| 64 | project_kakao_replacements_console_only.md | project | ③ | Solapi API 스키마에 `replacements`/`disableReplacements` 필드가 없다는 것은 외부 벤더 API 사양 — `foms/services/kakao_alimtalk.py:426 def _solapi_send` 실재하나 그 함수가 vendor 스키마를 못 바꾼다는 사실 자체는 콘솔/벤더 쪽 사실 | KEEP-FACT | — |
| 65 | project_kanban_grid_autorow_collapse.md | project | ① | SHA `b1c0ea6b`=commit 실재. `static/css/foundation/foms-tablet-production-kanban.css:357-360` 에 근본원인 설명 주석과 `grid-auto-rows: max-content;` 수정이 그대로 적용돼 있음(문제·수정 둘 다 코드에 남아 있음) | DELETE | 태블릿 생산 칸반에서 카드 세로겹침이 재발하면(같은 grid-auto-rows 붕괴 패턴) |
| 66 | project_layout_inline_js_must_stay_inline.md | project | ① | SHA `8f66ca1c`·`797c52da` 둘 다 commit 실재. `tests/performance/test_shared_layout_defer_contract.py` 실재, `tests/performance/test_page_local_defer_contract.py` 실재, `templates/partials/shared/layout_head.html:775 initGlobalSocketIO` 인라인 유지 확인 | DELETE | 공용 레이아웃 인라인 JS 를 다시 외부화하자는 제안이 나와 같은 DCL 회귀가 재현되면 |
| 67 | project_local_dev_db_drift.md | project | ③ | 로컬 dev PostgreSQL 스키마 상태·로컬 계정(`qa_v3` 등) 존재 여부·Git Bash `pkill` 부재는 이 머신의 로컬 환경 상태로 저장소 코드로 유도 불가. `guard_shell` 훅의 `DROP TABLE` 차단만 코드 확인 가능하나 핵심 결론은 로컬 DB 드리프트 상태 | KEEP-FACT | — |
| 68 | project_local_env_and_harness_wiring.md | project | ①(대부분), 일부 ③ | `.claude/settings.json` 확인 결과 SessionStart/PreCompact/PreToolUse(guard_shell)/PostToolUse(track_edits,post_bash)/Stop 은 일치하나 **UserPromptSubmit(ctx_gate) 훅은 이제 없음** — `.claude/hooks/ctx_gate.py` 가 현재 워킹트리에서 `D`(삭제) 상태(`git status --porcelain` 확인, ablation v2 진행 중). `.mcp.json` 은 postgres/context7/solapi 로 메모리와 정확히 일치 | DELETE | 하네스 훅 배선(특히 UserPromptSubmit 유무)을 착각해 세션이 잘못된 전제로 판단하면 |
| 69 | project_main_checkout_deploy_branch_diverged.md | project | ③ | 표본 M-11 과 동일 갈래(세션마다 로컬 git 상태를 재라는 워크플로 습관 — 코드로 강제되지 않음). `grep -rln "rev-list --left-right --count" AGENTS.md CLAUDE.md` 류 훅 강제 없음(표본 판정 재확인, 이번 세션도 `git status -b` 로 "ahead 23, behind 149" 별도 상태 실측 — 매번 다르다는 메모리 주장과 일치) | KEEP-FACT | — |
| 70 | project_manager_field_shape_trap.md | project | ① | `foms/services/erp_display.py:122 def manager_display_name(parties) -> str` 실재. SHA `535be6b2f`=commit 실재(`git log --oneline -1`). `foms/api/drawing/erp_orders_drawing.py:222,572` 여전히 `(((s_data.get('parties') or {}).get('manager') or {}).get('name') or '').strip()` 옛 dict 가정 코드 그대로(메모리가 말한 "미처리 2곳"과 라인번호까지 정확히 일치) | DELETE | manager 필드 dict 가정 코드가 새로 추가돼 스칼라 담당자 주문 1건으로 500 이 재발하면(erp_orders_drawing.py 222/572 포함) |
| 71 | project_measurement_time_column_null.md | project | ① | `foms/services/measurement_time.py:125 format_minutes_hm`·`:134 measurement_time_text`·`:168 measurement_time_sort_key` 전부 실재 | DELETE | 방문시각 정렬·표시에 `orders.measurement_time` 컬럼을 직접 쓰는 코드가 새로 생겨 정렬이 죽으면 |
| 72 | project_migration_constant_freeze.md | project | ① | `migrations/versions/sidefx_00_domain_side_effect_outbox.py:28` 주석에 "원칙 — models import 금지" 이미 명문화. `migrations/versions/order_import_00_order_import_artifacts.py:21` 도 같은 원칙 재확인 | DELETE | 새 마이그레이션이 models/서비스 상수를 live import 해 fresh 체인이 다시 파산하면 |
| 73 | project_mobile_image_viewer_ssot.md | project | ① | `templates/partials/shared/layout_scripts.html:371 window.GlobalImageViewer`·`:979 window.fomsIsMobileImageViewer` 실재. SHA `e5338f90`·`3bcb4b01`·`806292a3` 전부 commit 실재 | DELETE | 새 이미지 보기 표면이 GlobalImageViewer 위임 없이 모달을 재구현(재파편화)하면 |
| 74 | project_mobile_nav_cohort_badge.md | project | ① | 표본 M-12 재확인: `foms/services/dashboard_counts.py:26 NAV_STATUS_BUCKETS`·`:56 MINE_ONLY_TEAMS`, `foms/services/erp_permissions.py:223 def build_mine_sql_filter` 전부 실재 | DELETE | 신규 팀을 mine-only 로 추가하며 manager_name 매칭 여부 확인을 빠뜨려 담당자 큐 누락이 재발하면 |
| 75 | project_mobile_queue_infinite_scroll.md | project | ① | 표본 M-13 재확인: `templates/partials/shared/mobile_queue_pager.html`·`static/js/foms/mobile-queue-scroll.js` 실재, `tests/contracts/runtime/foms_namespace_surface_tests.py:2469 _PAC_PARTIALS_SHARED_HTML_ALLOWLIST` 실재 | DELETE | 신규 `partials/shared/` html 을 allowlist 등재 없이 추가해 계약 테스트가 red 나면 |
| 76 | project_mobile_search_droppers.md | project | ① | `foms/services/foms_unified_search.py:134 _order_extra_text_fields`·`:169 _classify_order_hit` 실재, `foms/services/erp_order_deeplink.py:69 def load_focus_order_only`·`:82 def build_order_queue_focus_href` 실재 | DELETE | 모바일 통합검색에서 classify/predicate 필드 불일치로 후보 누락이 재발하면 |
| 77 | project_mobile_v3_shell_architecture.md | project | ①(대부분, 일부 ③ 카카오 콘솔·Railway replica 전파 스큐 같은 외부 인프라 서술 포함) | `foms/services/feature_flags.py:257 def resolve_shell_variant`·`:282 FOMS_SHELL_V3_ENABLED` 실재, `tests/domains/test_shell_v3_contract.py`·`templates/partials/v3/foms_app_shell_v3.html` 둘 다 실재 | DELETE | v3 셸 구조(게이트·핀 사슬·듀얼 렌더)를 다시 손대며 같은 함정이 재발하면 |
| 78 | project_multiuser_perf_bottleneck.md | project | ① | `migrations/versions/phase_d_trgm_indexes.py`·`phase_e_trgm_perm_indexes.py` 둘 다 실재, `static/sw.js:86` 주석에 staticCacheFirst 설명 실재 | DELETE | ILIKE 풀스캔·SW networkFirst 무한스피너류 다중사용자 병목이 재발하면 |
| 79 | project_namespace_closed_set_dirs.md | project | ① | `tests/contracts/runtime/test_ptc_physical_exactness.py:20 _PTC_ROOT_ALLOWLIST`·`:169` `test_ptc_committed_root_allowlist_exact` 실재. `foms/services/settlement/` 디렉토리 부재 확인(메모리가 예시로 든 비존재 경로와 일치, 오류 아님) | DELETE | 새 최상위 디렉토리·루트 파일 추가로 네임스페이스/루트 계약이 다시 red 나면 |
| 80 | project_namespace_surface_contract_runs_via_domains.md | project | ① | `tests/domains/test_foms_namespace_imports.py:4` 주석·`:12 from tests.contracts.runtime.foms_namespace_surface_tests import *` 실재(메모리 주장 그대로) | DELETE | `__all__` 변경 후 `tests/contracts` 만 돌려 `tests/domains` red 를 놓치는 사고가 재발하면 |
| 81 | project_naver_api_docs_public_llms_txt.md | project | ③ | 네이버 apicenter llms.txt 접근성·GitHub Discussions 공식 출처 여부는 외부 사이트 사실 — 저장소로 유도 불가(문서 URL 자체가 repo 밖) | KEEP-FACT | — |
| 82 | project_naver_auto_dispatch.md | project | ① | `scripts/maintenance/run_naver_auto_dispatch.py` 실재, `foms/services/integrations/naver_commerce/auto_dispatch.py:43 SETTING_KEY = "naver_auto_dispatch_state"`·`:93 FOMS_NAVER_AUTO_DISPATCH_ENABLED`·`:179 is_business_day` 전부 실재 | DELETE | 네이버 자동 발송처리 배선·킬스위치 동작을 다시 헷갈리는 사고가 재발하면 |
| 83 | project_naver_backfill_traps.md | project | ③(외부 API 시맨틱 — 네이버 변경피드 상태값이 "이벤트 당시"가 아닌 "현재" 라는 사실은 공개 문서에도 없음, 실측으로만 드러남) | `foms/services/integrations/naver_commerce/bulk_dispatch.py:655 UNLINKED_SCAN_CAP = 300`·`:756 def find_unlinked_matches` 실재, `watermark.py:32 END_SAFETY_MARGIN = timedelta(minutes=1)` 실재(수정 코드는 ①이나 핵심 결론인 "피드는 현재상태·마지막변경 색인"은 네이버 API 자체의 비문서화 동작이라 ③) | KEEP-FACT(COMPRESS) | — |
| 84 | project_naver_claim_refund_axis.md | project | ③(네이버 결제 필드 시맨틱은 운영 DB 실측으로만 확인, 공식 문서 미기재) | `foms/services/integrations/naver_commerce/mapping.py:1010-1011` 에 `initial_amount = _known_int(product_order, "initialPaymentAmount")`·`remain_amount = _known_int(product_order, "remainPaymentAmount")` 로 같은 필드쌍을 사용하는 코드는 실재하나, 메모리가 지목한 함수명 `extract_claim_settlement_facts`·`naver_ingest._claim_stamp_text` 는 grep 0건(리팩터로 이름 드리프트 추정, 현재는 `extract_cancel_axis` 계열) | KEEP-FACT(COMPRESS) | — |
| 85 | project_naver_household_grouping_ssot.md | project | ① | `foms/services/integrations/naver_commerce/grouping.py:17 def group_key_expression`·`:38 def resolve_group_key`, `mapping.py:1238 def group_key_text` 전부 실재, `migrations/versions/navergroup_00_external_order_link_group_key.py` 실재 | DELETE | 네이버 집(묶음) 카운트를 grouping.py 를 거치지 않고 새로 세는 코드가 생겨 화면 숫자가 다시 갈리면 |
| 86 | project_naver_ingest_audit_actor_gap.md | project | ① | SHA `b084c655a`·`66381753d` 둘 다 commit 실재(`git log --oneline -1`), `foms/web/admin/naver_ingest.py` 의 `log_access(` 25건 현재 존재(수정 완료 상태 반영) | DELETE | `naver_ingest.py` 에 행위자 없는 `log_access` 호출이 새로 생겨 감사 행 `user_id NULL` 이 다시 늘면 |
| 87 | project_naver_origin_cleanup_strip.md | project | ① | `foms/services/integrations/naver_commerce/order_candidates.py:929 def pending_origin_cleanup`, `claim_watch.py:816 def refreshable_household_link_ids` 실재, `tools/ops/check_naver_repay_origin_alive.py` 실재, SHA `c6ffee0e`=commit 실재 | DELETE | 재결제 뒤 옛 네이버 주문 정리 안내 띠의 판정 기준이 다시 헷갈리는 사고가 재발하면 |
| 88 | project_naver_refresh_all_scope_and_progress.md | project | ① | `foms/web/admin/naver_ingest.py:4366 @admin_bp.route("/admin/naver-ingest/triage/refresh-progress")` 실재, `static/js/admin/naver-workbench.js:50,226,262 watchRefreshAll`·`REFRESH_PROGRESS_URL` 실재 | DELETE | 네이버 다시 읽기 대상 축소에 나이(수집 후 경과일) 기준을 쓰는 회귀가 재발하면 |
| 89 | project_naver_return_is_money_only_no_goods.md | project | ③(라홈이 맞춤 시공 가구라 반품 시 실물이 이동하지 않는다는 사실은 사용자 확인 업무 사실이지 코드로 유도 불가) | `foms/services/integrations/naver_commerce/fulfillment.py:266 RETURN_COLLECT_METHOD = "RETURN_INDIVIDUAL"`, `:135,144 INTENT_CHANGED, COLOR_AND_SIZE` 사유 코드 전부 실재(코드는 ①이나 "왜 이 코드인가"의 업무 전제는 ③) | KEEP-FACT(COMPRESS) | — |
| 90 | project_naver_settle_amount_sign_convention.md | project | ① | `foms/services/settlement_channel.py:170` 주석에 메모리와 동일한 실측 수치(`commissionSettleAmount -950081`·`payHoldbackAmount -10053445`)가 이미 코드 주석으로 박혀 있음. `settle_sync.py:118,121` 필드 매핑도 실재 | DELETE | 정산 금액 워터폴/KPI 계산에 방향(-1)을 다시 곱하는 회귀가 재발하면 |
| 91 | project_naver_settlement_tab_architecture.md | project | ①(대부분, 계정명 등 일부 운영 상태만 ③) | `foms/services/settlement_channel_access.py:63 def can_view_channel_settlement` 실재, `scripts/maintenance/run_naver_settle_sync.py` 실재. 단 `SETTLE_ENQUEUE_QUEUED` 등 v1.5 절의 큐 상수 3종은 현재 grep 0건(리팩터/드리프트 일부 있으나 전체 구조는 압도적으로 코드 대응) | DELETE | 정산 탭 SSOT·핀 사슬·게이트를 다시 헷갈려 CI red 나 렌더 계약이 깨지면 |
| 92 | project_naver_webpush_already_on_in_production.md | project | ③(운영 Railway 환경변수·DB 구독 행 상태는 인프라·계정 상태로 저장소로 유도 불가) | `foms_web_push_enabled`류 값 자체는 Railway 운영 변수라 리포에 사본 없음(정책대로 커밋 금지). `send_test_push`·`reportPushEvent` 관련 뒷부분은 ①이나 핵심 결론("웹푸시는 이미 켜져 있다")은 운영 인프라 상태 | KEEP-FACT(COMPRESS) | — |
| 93 | project_naver_width_size_option_ssot.md | project | ① | 표본 M-14 재확인: `foms/services/integrations/naver_commerce/dock.py:175 def size_option_mm`·`:213 def _width_unit_mm` 실재 | DELETE | 네이버 도크 총폭 계산이 옵션 사이즈 키 대신 상품명 기준으로 되돌아가면(900mm 급 오차 재발) |
| 94 | project_new_audit_action_needs_label.md | project | ① | `tests/domains/test_admin_audit_screen_readability_3.py:56 def test_every_emitted_action_has_business_label` 실재, `tests/domains/test_auth_enforcement.py:125 def test_static_gate_every_mutation_route_classified` 실재 | DELETE | 새 `log_access` action 코드를 라벨 등재 없이 내보내 CI red 가 재발하면 |
| 95 | project_new_mutation_route_contracts.md | project | ① | `docs/harness/foms_write_guard_manifest.json`·`foms_order_mutation_policy_manifest.json`·`foms_order_mutation_writer_allowlist.json` 3개 전부 실재, `foms/services/orders/order_mutation_policy.py:116 POLICY_REGISTRY` 실재 | DELETE | 신규 mutation 라우트가 계약 6종 중 하나를 빠뜨려 CI red 가 재발하면 |
| 96 | project_node_version_switch_traps.md | project | ③(로컬 머신의 nvm4w·node 버전·전역 CLI 재설치 상태는 이 PC 환경 사실로 저장소로 유도 불가) | FOMS 가 `package.json` 없는 파이썬 전용이라는 부분만 `ls package.json` 으로 확인 가능(부재) — 핵심 내용(nvm4w 구조·npm11 postinstall 차단·플러그인 SHA 비교)은 전부 로컬 환경 사실 | KEEP-FACT | — |
| 97 | project_notification_center_css_ssot.md | project | ① | `static/css/components/foms-notification-center.css` 실재, SHA `4de993cce`=commit 실재("PC 알림 패널 — 우상단 드롭다운...전 셸 스타일 SSOT 이관") | DELETE | 알림 센터 시트 스타일을 foms-shell.css 모바일 스코프 안에 다시 넣는 회귀가 재발하면 |
| 98 | project_notification_unit_vs_screen_unit.md | project | ① | `foms/services/integrations/naver_commerce/claim_watch.py:274 def _pending_groups` 실재, SHA `91e7e110`=commit 실재 | DELETE | 새 단위 통일 작업에서 알림·배지·푸시 경로 중 하나만 빠뜨려 같은 취소 1건이 알림 여러 건으로 나가는 회귀가 재발하면 |
| 99 | project_notification_user_states_ssot.md | project | ① | 표본 M-15 재확인: `foms/services/notifications/recipients.py:141 def fan_out_new_notification` 실재(경로는 메모리와 일치) | DELETE | 신규 알림 생성 지점에서 `fan_out_new_notification` 호출을 빠뜨려 그 알림이 list/badge 에 안 보이면 |
| 100 | project_order_delete_axis_traps.md | project | ① | `models.py:35 deleted_at = Column(String)` 정확히 일치(메모리가 지목한 라인번호까지), `foms/services/orders/soft_delete.py:48 _DELETED_AT_FORMAT`, `foms/services/orders/state_axes.py:259 def read_deleted`, `foms/web/orders/trash.py:293 def restore_orders` 전부 실재 | DELETE | 주문 삭제 축(String deleted_at·legacy 복원 미정리) 관련 표시 결함이 재발하면 |
| 101 | project_page_scope_script_v3_shell_gap.md | project | ① | `templates/measurement/dashboard.html:14 {% if shell_variant != 'v3' %}` 실재(메모리 주장과 정확히 일치) | DELETE | v3 셸 표면에서 페이지 스코프 스크립트 블록이 조용히 사라지는 결함이 재발하면 |
| 102 | project_perf_gate_budget_maintenance.md | project | ①(대부분) | `tools/perf/staging_perf_gate.py`·`tools/perf/perf_budgets.json`·`.github/workflows/perf-gate.yml` 전부 실재(메모리가 적은 `perf-gate.yml` 은 `.github/workflows/` 아래 실재, 경로 표기만 축약) | DELETE | perf-gate 판정 아키텍처를 다시 손대며 같은 종류의 두더지잡기(해압바이트·max render 등)가 재발하면 |
| 103 | project_perf_measure_before_optimize.md | feedback | (type 우선, 파일명은 `project_` 접두이나 frontmatter `metadata.type: feedback`) | frontmatter 확인: `metadata:\n  type: feedback` — 증거표(#103)도 동일하게 type=feedback 로 집계 | KEEP-PREF | — |
| 104 | project_perfgate_ci_bot_account.md | project | ③ | `docs/guides/REAL_SERVER_TEST_ACCOUNT.md:85` "CI perf-gate는 별도 계정 `perfgate_ci`" 확인(외부 GitHub secrets·스테이징 DB 계정 상태, 저장소로 유도 불가) | KEEP-FACT | — |
| 105 | project_pg_lane_local_cluster.md | project | ③ | `tests/postgres/conftest.py:41` `class PgLaneSafetyError` 실재(git grep) — 가드 코드는 실재하나 핵심 내용(로컬 PG 바이너리 경로·포트 점유·pg_ctl 한글 오탐)은 로컬 머신 상태라 저장소로 유도 불가 | KEEP-FACT(COMPRESS) | — |
| 106 | project_pg_test_lane_isolation.md | project | ① | `tests/postgres/conftest.py:293` `TRUNCATE TABLE ... RESTART IDENTITY CASCADE`, `:217` `poolclass=NullPool` 둘 다 실재(git grep) | DELETE | `tests/postgres` 레인이 NullPool 제거·TRUNCATE 방식 변경으로 advisory lock leak·row 누출이 재발하면(전수 실행 red) 복원 |
| 107 | project_production_live_diagnosis_recipe.md | project | ③ | `foms/api/channel/channel_integration.py:438` `Order.active_filter()` 실재(git grep) — 판정 근거 심볼은 실재하나 핵심은 railway DSN 조회+HTTP CSRF 진단 절차(외부 운영 인프라 접근 레시피) | KEEP-FACT | — |
| 108 | project_promotion_traps_master.md | project | ③(혼합, 핵심=②) | `tools/harness/promote_completeness.py`·`promote_own_to_production.py` 실재(ls), `tests/contracts/runtime/foms_namespace_surface_tests.py:2393` `test_slg_literal_gap_foms_services_top_level_dirs_closed_set` 실재, 단 `docs/plans/2026-09-09-production-pending-triage.md` 는 0건(부재) — 본문 대부분이 GitHub PR/Actions·branch protection·git 내부 동작(외부 시스템) | KEEP-FACT(COMPRESS) | — |
| 109 | project_railway_cli_link_dev_prod_ambiguity.md | project | ③ | Railway CLI dev/prod 프로젝트·서비스명·IP 혼동은 외부 PaaS 동작이라 저장소로 유도 불가(코드 참조 없음) | KEEP-FACT | — |
| 110 | project_railway_dev_build_fail_diagnosis.md | project | ③(①은 일부) | `tests/domains/test_alembic_single_head.py` 실재(ls) — 재발방지 게이트는 ①이나 핵심(Railway GraphQL 배포이력·로그 진단 레시피)은 외부 PaaS 접근 절차 | KEEP-FACT | — |
| 111 | project_railway_egress_ip_shared_by_region.md | project | ③ | Railway static egress IP가 region 단위로 공유된다는 사실은 Railway GraphQL 조회 결과 — 저장소에 흔적 없음(코드 참조 자체가 없는 순수 외부 인프라 사실) | KEEP-FACT | — |
| 112 | project_railway_redeploy_is_same_commit.md | project | ③ | `railway redeploy`/`variables --set` 이 재배포를 안 건다는 동작은 Railway CLI/GraphQL 자체 행동 — `grep -rn "serviceInstanceDeploy" .` 0건(저장소에 코드화된 곳 없음). 표본 판정(M-16) 재확인, 동일 결론 | KEEP-FACT | — |
| 113 | project_ratelimit_failopen_redis_outage.md | project | ① | `foms/services/rate_limit.py:69-70` `swallow_errors=True`/`in_memory_fallback_enabled=True` 확인, `tests/domains/test_rate_limit.py:107` `def test_dead_redis_does_not_500_and_falls_back` 확인. 표본 판정(M-17) 재확인 | DELETE | `swallow_errors`/`in_memory_fallback_enabled` 가 빠져 Redis 장애 시 500 이 재현되면(`test_dead_redis_does_not_500_and_falls_back` red) 복원 |
| 114 | project_rq_fork_inherits_parent_db_connection.md | project | ②(전반부)+①(흡수부) | `tools/ops/run_rq_worker.py` 0건(find), `HeartbeatWorker`/`main_work_horse` 0건(grep), `dispose(close=False)` 0건(bare `.dispose()` 만 14곳) — 전부 소멸. 흡수부(`_register_worker_session_wiring`)는 `foms/services/jobs/tasks.py:22,42` 실재·현재도 유효. 표본 판정(M-18) 재확인 | DELETE | 60초 이상 도는 rq 잡이 부모와 DB 소켓을 공유해 죽는 증상이 재현되면(현재 `start.sh` 인라인 `rq worker` 감시루프 기준으로 파일명 갱신 필요) 또는 워커에서 세션 훅이 다시 빠지는 회귀가 나오면 복원 |
| 115 | project_save_button_focus_steal_click_loss.md | project | ① | `static/js/orders/erp-order-shared.js:3162` `pointerdown` 가드 확인, `tests/domains/test_erp_order_shared_form_scripts.py:1756` `def test_save_button_does_not_steal_focus_on_mouse_pointerdown` 확인. 표본 판정(M-19) 재확인 | DELETE | 저장 버튼 pointerdown 가드가 빠지고 "첫 클릭 무산+스크롤" 이 재현되면 복원 |
| 116 | project_schedule_sync_column_first_date_only.md | project | ① | `foms/services/orders/dashboard_control_tower.py:59` `def _sched_any`, `:85` `def _measure_on` 확인. 표본 판정(M-20) 재확인 | DELETE | `dashboard_read_model.py` 의 D-day KPI 가 싱크 컬럼 기반 잔여를 실제로 고치면(반대 방향 갱신) 복원 검토 |
| 117 | project_session_collision_same_spec_gap.md | project | ③ | `git cat-file -t a077c72a2` → commit 실재, `docs/specs/2026-08-29-naver-origin-cleanup-strip_SPEC.md` 실재(find) — 그러나 핵심(긴 작업 중간 fetch 습관·설계서 "아직 안 고친 것" 칸 경합)은 코드로 강제되지 않는 세션 워크플로 습관 | KEEP-FACT | — |
| 118 | project_session_cookie_clobber_wipes_csrf_seed.md | project | ① | `foms/services/request_write_guard.py` 실재(ls), `tests/domains/test_write_guard.py:166` `test_token_survives_session_cookie_clobber`, `:191` `test_other_users_token_rejected`, `tests/domains/test_session_cookie_stability.py` 전부 실재 | DELETE | 로그인 사용자 CSRF seed 가 다시 세션 저장 방식으로 되돌아가 탭 다중 사용자의 저장이 403 재현되면(`test_write_guard.py` red) 복원 |
| 119 | project_settle_loop_dies_after_success_tick.md | project | ③(현재 deploy 기준 미유도) | `git grep -rn "_heartbeat_metadata" .` 0건, `find . -iname "test_naver_settle_sync_loop.py"` 는 있으나 `test_settle_sync_loop_beats_outside_its_window` 는 없음. 수정 커밋 `079f88a64`(및 관련 하트비트 커밋 전부)이 `git merge-base --is-ancestor 079f88a64 HEAD` → NOT ancestor — 브랜치 `promote/settle-loop-20260910` 에만 있고 현재 `deploy` HEAD(7eecbb4d2)에는 하트비트 루프 기능 자체가 없음(`emit_heartbeat` 관련 loop 배선 0건) — 총괄 재정: 2026-09-10 타 세션이 진행 중인 작업의 메모리 — 승격 병합 뒤 다음 정리 때 재판정(보류) | KEEP-FACT | — (판정 어려움: 별도 승격 브랜치에 수정·사고 원장이 이미 존재해 deploy 병합 시 자동 DELETE 대상이 될 파일. 재판정 필요) |
| 120 | project_settle_sync_run_orphan_running_on_restart.md | project | ① | `foms/services/integrations/naver_commerce/settle_sync.py:823` `status="RUNNING"`, `models.py:3676` `NAVER_SETTLE_RUN_STATUSES = ('RUNNING', 'OK', 'FAILED', 'ABORTED_QUOTA')` 확인 | DELETE | RUNNING 잔류 행을 정리하는 코드가 새로 들어가 "그대로 둔다" 결정이 뒤집히면(사용자 재결정) 복원 |
| 121 | project_settlement_cfo_audit_2026_09.md | project | ① | `docs/plans/2026-09-05-settlement-cfo-review-report.md` 실재(ls) — 포인터 대상이 저장소에 그대로 있어 재검색 가능(M-26 패턴과 동일) | DELETE | 정산 탭을 다시 감사할 때 같은 항목(D-02·D-01·B-02 등)을 처음부터 재조사하면 복원(재보고 금지 목록 용도) |
| 122 | project_share_contact_phone_vs_sender.md | project | ① | `foms/api/share.py:1062` `def _resolve_sender`, `:1342` `def _share_contact_phone` 확인. 표본 판정(M-21) 재확인 | DELETE | `users.sender_phone` 이 실제로 채워지기 시작하거나 체인 순서가 바뀌어 공유 링크 발신번호 오배선이 재현되면 복원 |
| 123 | project_shared_component_container_query.md | project | ① | `static/css/components/foms-as-round-chart.css:11` `container-type: inline-size`, `:279` `@container (max-width: 560px)` 확인, `git cat-file -t f0440b47` → commit 실재 | DELETE | AS 회차 차트류 공용 부품에 뷰포트 미디어쿼리로 되돌리는 회귀가 나와 글자 세로서기가 재현되면 복원 |
| 124 | project_shared_constant_meaning_drift.md | project | ① | `foms/services/integrations/naver_commerce/repay_reconcile.py:145` `def discard_policy(status, *, claim_code="")` 이 메모리가 설명한 수정 모양(`can_discard`/`needs_reason`/`block`) 그대로 이미 구현·문서화됨 | DELETE | 새 소비자가 `DISCARDABLE_STATUSES` 를 `discard_policy` 재사용 없이 직접 읽어 두 화면 해석이 다시 갈리면 복원 |
| 125 | project_shell_cwd_resets_at_turn_boundary.md | project | ③ | Claude Code Bash 툴의 턴 경계 cwd 리셋은 하네스/CLI 자체 동작 — 저장소 코드로 유도 불가 | KEEP-FACT | — |
| 126 | project_shipping_price_grand_total.md | project | ① | `static/js/orders/dashboard/erp-dashboard-detail-dom.js:628` `shippingPrice = coerceAmount(totals.shipping_price) \ / \ / ...`, `foms/services/erp_display.py:320` `def erp_shipping_price_from_structured` 확인 | DELETE | `totals.items_total` 을 출고가로 재정의하는 회귀가 생겨 이중계산이 재현되면 복원 |
| 127 | project_site_address_canonical_shape.md | project | ① | `foms/services/order_geocode.py:55-57` `site["address_full"]=addr; site["address_main"]=addr; site["address_detail"]=""` 확인(함수명은 `apply_erp_order_site_address_to_sd` — 메모리의 `sync_site_address` 표기는 별칭/드리프트), `static/js/orders/erp-order-shared.js:399` `function erpJoinSiteAddress` 확인 | DELETE | 새 채널·수집 경로가 site 를 정본 형태로 안 맞춰 주소 중복 저장(`103동 605호 103동 605호`류)이 재현되면 복원 |
| 128 | project_skill_ecosystem_2026_07.md | project | ① | `C:/DEV/FOMS/CLAUDE.md` "## 스킬 (2026-08-03 ablation 후)" 절이 프로젝트 스킬 5종·superpowers 온디맨드·ECC/taste 제거 근거를 이미 동일하게 문서화(저장소 안 문서로 이미 SSOT) | DELETE | 스킬 로스터가 또 개편되고 CLAUDE.md 갱신 없이 구두로만 결정되면 복원 |
| 129 | project_spec_width_shipment_coupling.md | project | ① | `foms/services/erp_template_filters.py:67` `def eval_spec_width_mm(value) -> float` 확인. 표본 판정(M-22) 재확인 | DELETE | 시공비 계산 탭 신설 시 `eval_spec_width_mm` 재사용 없이 W 를 직접 파싱해 출고 값과 어긋나면 복원 |
| 130 | project_sqlite_fk_local_vs_ci.md | project | ① | `tests/domains/test_order_draft_model.py:22-42` `PRAGMA foreign_keys` 연결단위 설정·복원 패턴이 이미 코드/주석으로 문서화, `tests/services/integrations/test_naver_workbench_v3_followup.py:61` "CI는 PRAGMA foreign_keys 가 켜져 있어 IntegrityError 로 터진다(2026-08-24 실사고)" 주석으로 재문서화 확인 | DELETE | 새 테스트가 존재하지 않는 FK id 를 다시 하드코딩해 로컬 green·CI red 가 재현되면 복원 |
| 131 | project_stage_override_base_is_saved_stage.md | project | ① | `foms/api/orders/stage_override.py:26-29` `apply_stage_override` 확인, `foms/api/erp_orders_structured.py:415` `override_pins_stage(sd, stage_now)` 확인(단 `tests/domains/test_stage_override_persistence.py` 는 0건 — 테스트 파일명 드리프트, 핵심 로직은 실재) | DELETE | 단계 강제 변경이 폼 미저장 값을 base 로 다시 쓰기 시작해 400(same) 오류가 재현되면 복원 |
| 132 | project_staging_e2e_http_harness.md | project | ③ | `tools/harness/ept_b8_staging_session_from_login.py` 실재(find) — 그러나 핵심은 lahom-dev 스테이징 HTTP 직접 검증 절차(외부 서버·계정·CSRF 헤더 레시피) | KEEP-FACT | — |
| 133 | project_staging_gate_credentials_recipe.md | project | ③ | `tools/harness/ept_b8_staging_session_from_login.py` 실재(find) — 핵심은 스테이징 DB 비번 어긋남 진단·GitHub secret 갱신·재실행 절차(외부 운영 인프라 상태) | KEEP-FACT | — |
| 134 | project_stale_flask_server_jinja_cache.md | project | ③ | Windows bash pkill이 python.exe 를 못 죽이는 것은 OS/셸 특성 — 저장소 코드로 유도 불가(규칙 §3③ 명시 예시와 정확히 일치) | KEEP-FACT | — |
| 135 | project_standalone_csrf_and_body_cap_traps.md | project | ① | `templates/partials/shared/csrf_bootstrap.html` 실재, `foms/platform/request_limits.py:93` `_MANIFEST` 실재, `tests/domains/test_csrf_bootstrap_contract.py` 실재(grep) | DELETE | standalone 템플릿이 `csrf_bootstrap.html` include 없이 추가되거나 새 업로드 라우트가 `_MANIFEST` 미등재로 남아 403/413 이 재현되면 복원 |
| 136 | project_state_flag_off_without_repaint.md | project | ① | `static/js/settlement/channel.js:983` `function renderSync`, `:2164` `function stopRevPoll` 확인, `tests/domains/test_settlement_channel_render.py` 실재, `git cat-file -t fa52d04d7` → commit 실재 | DELETE | 새 호출자가 `stopRevPoll`/`renderSync` 순서를 거치지 않고 `syncing=false` 를 직접 대입해 진행 막대 잔상이 재현되면(`test_settlement_channel_render.py` red) 복원 |
| 137 | project_static_cache_policy.md | project | ① | `foms/platform/app_factory.py:13` `from whitenoise import WhiteNoise`, `:45` `def _add_static_response_headers` 확인. 표본 판정(M-23) 재확인 | DELETE | CSS/JS 에 1년 immutable 캐시가 다시 붙어 "배포해도 적용 안 됨" 이 재발하면 복원 |
| 138 | project_structured_notes_is_string_not_dict.md | project | ① | `templates/measurement/partials/dashboard_main.html:959` `is mapping` 가드 확인. 표본 판정(M-24) 재확인 | DELETE | `.get()` 가드 없는 신규 템플릿이 추가돼 문자열 notes 를 가진 구주문에서 500 이 재현되면 복원 |
| 139 | project_sw_stale_js_version_bump.md | project | ① | `tests/visual/test_alimtalk_ui_contract.py:375` `def test_share_trace_assets_pinned_together` 확인, `static/js/foms/tablet-domain-sheets.js` 실재 | DELETE | 기존 JS/CSS 내용 변경 후 `?v` 미범프로 실기기에서 신규 기능만 죽는 사고가 재현되면 복원 |
| 140 | project_symbol_relocation_contract_tests.md | feedback | ①(참고) | `tests/contracts/runtime/foms_namespace_surface_tests.py`, `tests/domains/test_erp_permissions.py`, `tests/domains/test_erp_dashboard_active_filter.py` 전부 실재(find). 표본 판정(M-25) 재확인 | KEEP-PREF | — |
| 141 | project_system_review_prompt_set_2026_09.md | reference | ① | `docs/plans/2026-09-06-foms-system-review-prompt.md` 실재(ls). 표본 판정(M-26) 재확인 — 총괄 재정: type=reference, 대상 6파일 실존 → 유지(원장 §0 M-26) | KEEP-FACT | 같은 프롬프트 세트 위치를 다시 못 찾아 세션이 헤매면 복원 |
| 142 | project_tab_pane_cohort_wrapper_trap.md | project | ① | `tests/domains/test_order_tab_pane_visibility.py` 실재(find), `static/css/orders/erp-edit-embedded.css` 실재(find) | DELETE | 다른 화면에서 탭 pane 을 코호트 래퍼로 감싸며 보정 CSS 를 빠뜨려 비활성 pane 이 자리를 차지하는 증상이 재현되면(`test_order_tab_pane_visibility.py` red) 복원 |
| 143 | project_tablet_ux_mockup.md | project | ① | `foms/services/foms_split_view.py:55` `orders[:30]` 확인(2026-09-06 재확인 항목 그대로 실재), `static/js/foms/lightbox.js` 등 `fomsIsMobileImageViewer` 확인 — 본문 자체가 "역사 기록, 신규 제안 근거로 쓰지 마라"로 선언, T19까지 구현 완료돼 코드로 재확인 가능 | DELETE | 태블릿 화면에서 "목업과 다르다"는 신고가 다시 들어와 v1~v8 결정 이력(카드 마스터 폐기 등)을 재검토해야 하면 복원 |
| 144 | project_tail_spike_network_rca.md | project | ③ | `foms/services/measurement_read_model.py:26` `def apply_measurement_dashboard_order_scope` 확인, `git cat-file -t eef8e96d` → commit 실재 — 핵심 결론(한국↔싱가포르 네트워크 경로, Railway 리전 구성)은 외부 인프라 사실. 표본 판정(M-27) 재확인 | KEEP-FACT(COMPRESS) | — |
| 145 | project_template_macro_extraction_contract_gap.md | project | ① | `tests/domains/test_tablet_t2_contract.py` 존재(표본 M-28 재확인, find) | DELETE | 템플릿 매크로 추출 변경을 `pre_push_smoke` 서브셋만 돌리고 push 해 CI가 UndefinedError/소스 리터럴 계약으로 red 나면 복원 |
| 146 | project_timestamp_utc_convention.md | project | ① | `foms/services/datetime_kst.py:42` `def now_utc_naive` 확인, `git grep -c "now_utc_naive()" foms` → 109건(광범위 사용 확정) | DELETE | 새 타임스탬프 필드가 `datetime.now()` 를 써 dev/운영 시계 스큐가 재현되면 복원 |
| 147 | project_unlinked_match_strip_ignored_claims.md | project | ① | `foms/services/integrations/naver_commerce/bulk_dispatch.py:537` `def all_money_back_settled`, `:756` `def find_unlinked_matches`, `:980` `def classify_unsendable` 전부 확인, `ghost_orders.py:72` `GHOST_CLAIM_PHASES` 확인 | DELETE | `find_unlinked_matches` 가 클레임 축 없이 다시 취소 확정 집을 매칭 후보로 권하는 회귀가 재현되면 복원 |
| 148 | project_v3_shell_skips_surfaces_bundle.md | project | ① | `templates/partials/shared/layout_head.html` 계열에서 v2/v3 자산 분리 로드 확인(표본 M-29 재확인) | DELETE | v3 셸 화면에서 공용 KV/제품항목 컴포넌트 CSS 가 실화면에서 규칙 없음(`display:block`/보더 0)으로 다시 관측되면 복원 |
| 149 | project_wdcalculator_mobile_pattern.md | project | ① | `static/js/wdcalculator/` 하위 mobile-enhance.js·product-settings-mobile.js 등 확인(표본 M-30 재확인) | DELETE | WDCalculator 계열 신규 페이지 모바일화에서 additive 패턴(호스트 마크업 무변경)이 깨져 CRUD 로직이 중복 구현되면 복원 |
| 150 | project_windows_korean_encoding_traps.md | project | ③ | `.claude/settings.json:4-5` `PYTHONIOENCODING=utf-8`/`PYTHONUTF8=1` 확인, `tests/harness/test_powershell_encoding_contract.py` 실재 — 그러나 핵심(cp949 스트림·BOM·콘솔 OutputEncoding 3층)은 Windows OS/PowerShell 플랫폼 특성(규칙 §3③ 명시 예시와 일치) | KEEP-FACT | — |
| 151 | project_workbench_cohort_blocks_qa_account.md | project | ③ | `foms/services/feature_flags.py:309` `def is_naver_workbench_enabled`, `:327` `FOMS_NAVER_WORKBENCH_COHORT` 확인 — 그러나 핵심(Railway env 코호트 확장·재배포 절차·서비스명 함정)은 외부 Railway 운영 인프라 상태 | KEEP-FACT(COMPRESS) | — |
| 152 | project_workbench_pin_duplicated_in_five_tests.md | project | ① | `tests/services/integrations/test_naver_backfill_route.py` 등 5개 테스트 파일 전부 실재, 각 파일에 `?v=` 리터럴 1개씩 확인(grep -c) | DELETE | 워크벤치 CSS/JS 핀만 올리고 5개 계약 테스트를 안 옮겨 로컬 green·CI red 가 재현되면 복원 |
| 153 | project_worker_supervisor_pid1.md | project | ③(혼합) | `start.sh:74-118` "rq worker 감시 루프(2026-09-08 운영 사고 근본 수정)" + `rq worker default --url "$REDIS_URL" &` 확인(본문 PID1 교훈과 일치), `tests/contracts/runtime/test_worker_supervisor_contract.py`·`tools/ops/check_worker_redeploy_safe.py` 실재. 단 흡수부가 설명하는 `foms/services/worker_watchdog.py`·`tests/domains/test_worker_watchdog.py` 는 0건(find) — 현재 deploy 에 미병합. `tools/ops/run_rq_worker.py` 도 0건(파일명 드리프트, 실제는 start.sh 인라인 `rq worker`) | KEEP-FACT(COMPRESS) | — (판정 어려움: 본문 앞부분은 ①로 검증되나 흡수부 workwatchdog 는 별도 브랜치에만 존재) |
| 154 | project_workflow_interrupt_kills_workers_resume.md | project | ③ | Claude Code Workflow 도구의 인터럽트 전파(워커 전체 종료) 동작은 하네스/CLI 자체 동작 — 저장소 코드로 유도 불가 | KEEP-FACT | — |
