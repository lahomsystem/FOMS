# 실측 모바일 체크리스트 — 리뷰 findings 원장 (2026-09-23, 워크플로 wf_830e9bb1-8af)

> 받는 즉시 보존. 상태 칸은 총괄이 커밋 전 직접 확인해 갱신한다.

## 리뷰어 findings
- [P1] `static/js/measurement/image-export.js:428` — 모바일 PNG가 PC PNG와 다를 수 있다(브리프 §5 '완전히 같아야' 위반 가능성, 브라우저로 확인하지는 않음). PC에서는 dashboard-columns.js가 ColumnResizer를 붙이면서 표에 grip-resizable 클래스를 달고, head에 `.grip-resizable > tbody > tr > td{overflow:hidden}` 규칙을 넣는다(column-resizer.js createStyle). 모바일은 applyStaticTableState가 리사이저를 만들지 않으므로 숨은 표와 그 클론에는 이 클래스와 규칙이 모두 없다. 그래서 칸 폭보다 긴 글자가 PC 이미지에서는 잘리지만, 모바일 클론에서는 옆 칸(주소 등)으로 넘쳐 겹쳐 그려진다. 넘칠 수 있는 칸: 발주사·고객 72px, 전화번호 124px. 셋 다 20px·nowrap이라 4글자 이상 발주사명이나 하이픈이 들어간 번호는 쉽게 넘친다.
  - 수정안: offscreen 분기에서도 PC와 같은 결과가 나오게 한다. 가장 좁게 고치는 방법은 새 CSS에 호스트 안에서만 적용되는 규칙 `.foms-meas-export-host [data-meas-export-target] > tbody > tr > td { overflow: hidden; }`을 넣는 것이다. PC 경로는 호스트를 쓰지 않으므로 바이트 단위로 그대로다. 추가로 PC에서 표 클래스(grip-*)와 head에 주입된 규칙을 전부 조사해, 결과에 영향을 주는 차이가 더 없는지 확인한다. 가능하면 같은 데이터로 PC와 모바일 에뮬레이션 PNG를 비교하는 수동 검증도 한다.
  - 상태: 통합자 반영 보고 · CEO 재판정 ship · 총괄이 전체 pytest(10,803 통과)·pre_push_smoke 재실행 확인. PNG 실기기 비교는 스테이징 QA 대기
- [P2] `static/js/measurement/image-export.js:443` — 모바일(offscreen)만 captureScale을 2로 고정했다. 브리프 §5는 '같은 캡처 옵션'을 요구하는데, PC 공식 max(2, min(dpr,3))와 다르다. 결과적으로 HiDPI PC(dpr 2.5~3)에서 저장한 PNG와 픽셀 크기가 달라진다. iOS 캔버스 면적 한도 때문이라는 이유는 타당하다.
  - 수정안: 의도한 차이이므로 브리프·스펙에 예외로 적어 두거나(모바일 해상도 = dpr 1~2인 PC 결과와 같다), 행 수에 따라 면적 한도 안에서 PC 공식을 쓰도록 캔버스 면적을 보고 scale을 고른다.
  - 상태: 통합자 반영 보고 · CEO 재판정 ship · 총괄이 전체 pytest(10,803 통과)·pre_push_smoke 재실행 확인. PNG 실기기 비교는 스테이징 QA 대기
- [P2] `foms/api/orders/measurement_visit.py:155` — no-op 사전 검사를 잠금 밖에서, If-Match·Idempotency 처리보다 먼저 한다. (1) If-Match가 오래됐어도 상태가 같으면 409 대신 200을 준다. (2) 잠금 전 검사와 잠금 후 상태가 경합으로 엇갈리면 _mutate가 changed=False인데도 execute_order_mutation이 mutation_version을 올린다. 이벤트 없이 버전만 오르는 셈이라 스펙 §3 '이벤트·버전 증가 없음'에 어긋난다. (3) 같은 키로 재요청했는데 그 사이 다른 요청이 상태를 뒤집었다면, replayed 분기(210-214행)가 실제 저장값과 무관하게 done=요청값, changed=True로 응답해 화면 체크가 서버와 달라질 수 있다.
  - 수정안: replayed 분기는 다시 읽은 sd로 is_visit_marked(sd, date)를 계산해 done에 넣는다. 잠금 안에서 changed=False가 되는 경합은 롤백할 수 있게 한다(예: _mutate에서 전용 예외를 던지고 잡아서 no-op 성공으로 응답). 잠금 안에서 롤백할 방법이 없다면 이 경합을 스펙에 허용 사례로 적어 둔다. If-Match를 no-op보다 먼저 볼지도 정책으로 정한다.
  - 상태: 통합자 반영 보고 · CEO 재판정 ship · 총괄이 전체 pytest(10,803 통과)·pre_push_smoke 재실행 확인. PNG 실기기 비교는 스테이징 QA 대기
- [P2] `templates/measurement/partials/mobile_list.html:23` — 체크 버튼 활성 조건은 can_edit_erp(ADMIN이거나 팀 권한, role 검사 없음)이고 API는 ERP_EDIT 정책(VIEWER 거부)이다. 둘이 다르다. 그래서 SALES·CS 팀의 VIEWER 계정은 버튼이 켜져 보이지만, 누를 때마다 403이 나서 체크가 되돌아가고 실패 안내만 뜬다.
  - 수정안: 화면과 서버가 같은 잣대를 쓰게 한다. 방법: 뷰에서 evaluate_policy(POLICY_REGISTRY['ERP_EDIT'], current_user).allowed를 계산해 can_mark_measurement_visit 같은 값으로 넘기고, _gv_can이 그 값을 쓴다.
  - 상태: 통합자 반영 보고 · CEO 재판정 ship · 총괄이 전체 pytest(10,803 통과)·pre_push_smoke 재실행 확인. PNG 실기기 비교는 스테이징 QA 대기
- [P2] `static/css/contexts/measurement/measurement-mobile-glance.css:233` — 시간 표시에 오전·오후·종일 색 점(파랑·주황·초록)을 붙였다. 브리프 §2는 '시간은 작은 회색 글자로만'이라고 했다. 시간순 강조는 아니지만, 예정값에 불과한 시간에 색으로 무게를 싣는 쪽이라 계약 문구에서 벗어난다.
  - 수정안: 색 점을 빼고 회색 글자만 남긴다. data-daypart 속성은 남아도 무해하다. 점을 유지하려면 사용자 확인을 받아 브리프에 반영한다.
  - 상태: 통합자 반영 보고 · CEO 재판정 ship · 총괄이 전체 pytest(10,803 통과)·pre_push_smoke 재실행 확인. PNG 실기기 비교는 스테이징 QA 대기
## 리뷰어 findings
- [P1] `foms/api/orders/measurement_visit.py:140` — Lost update. The handler reads the order (db.query(Order).first(), line 140) before execute_order_mutation locks it. The lock query in revision.py:281-287 (with_for_update().all(), no populate_existing) gets that same identity-map object back and does not refresh its attributes. So _mutate (line 167) deep-copies the pre-lock structured_data and writes the whole thing back. Failure case: another writer (form save, worker, etc.) holds the row lock on the same order and commits a structured_data change. Meanwhile this request's pre-read sees the old copy (MVCC), waits on the lock, then overwrites that change. Checked with SQLAlchemy: after pre-read → external UPDATE commit → with_for_update().all(), the object still has the old value; only populate_existing() sees the new one. call_log.py:138 has the same pattern, so the root cause is systemic in revision.py.
  - 수정안: Narrow fix in this file: after the no-op pre-check and before calling execute_order_mutation, run db.expire(order) (or db.expunge(order)) so the locked row is loaded fresh. Root fix (outside the file ownership table, report to the lead): add .populate_existing() to the FOR UPDATE query in revision.py:281-287. Add a regression test: pre-read → external commit → call → assert the other key survives.
  - 상태: 통합자 반영 보고 · CEO 재판정 ship · 총괄이 전체 pytest(10,803 통과)·pre_push_smoke 재실행 확인. PNG 실기기 비교는 스테이징 QA 대기
- [P2] `foms/api/orders/measurement_visit.py:155` — The no-op decision (line 155) happens outside the lock. When two requests with the same state run at the same moment (two people checking the same order), the second one enters the lock with apply_visit_mark() returning changed=False. It still assigns o.structured_data and calls flag_modified (a pointless full JSONB write), and execute_order_mutation still bumps mutation_version by 1 and issues a receipt. No OrderEvent is written. This breaks the spec rule that a no-op means no version bump and no event (OrderEvent parity), and it can cause a spurious 409 for other clients using If-Match.
  - 수정안: In _mutate, skip the assignment and flag_modified when changed is False. Also re-check the no-op state after the lock and use a path that does not bump the version: raise a dedicated sentinel exception, roll back, and return changed:false.
  - 상태: 통합자 반영 보고 · CEO 재판정 ship · 총괄이 전체 pytest(10,803 통과)·pre_push_smoke 재실행 확인. PNG 실기기 비교는 스테이징 QA 대기
- [P2] `static/js/measurement/image-export.js:443` — On mobile (offscreen), scale is fixed at 2, but the canvas height is not capped. The PNG table is 1520px wide, so the canvas is 3040px wide. With iOS Safari's canvas area limit (16,777,216px) the height can be at most about 5,518 device px ≈ 2,759 CSS px. With 20px font and 10px padding, rows are 48-75px, so the limit is exceeded at roughly 35-55 rows. On the mobile screen, the 'all dates' mode (date= empty) or a date range shows up to 300 rows (MEASUREMENT_MAIN_DISPLAY_CAP) and the save button stays enabled. On iPhone this deterministically fails: getContext returns null and throws, or the image comes out blank.
  - 수정안: In the offscreen path only, compute scale before capture as min(2, sqrt(16777216 / (width*height))). Measure the host clone's size after applying the export styles to it. Note: prepareExportTable deletes rows across the whole clonedDoc, so never pass the live document to it; use a separate throwaway clone for measuring. Or disable the glance save button in range and all-dates modes.
  - 상태: 통합자 반영 보고 · CEO 재판정 ship · 총괄이 전체 pytest(10,803 통과)·pre_push_smoke 재실행 확인. PNG 실기기 비교는 스테이징 QA 대기
- [P2] `static/js/measurement/image-export.js:387` — resolveExportDate picks the first input[name="date"] in the DOM. On the mobile v2 surface, that is the filter drawer's #erp-measurement-mobile-date (mobile_filters.html:72), which comes before the desktop shell. This input can be edited by the user and only applies on submit (no JS auto-apply). Reproduction: open the filter drawer, change the measurement date to tomorrow, close it without tapping Apply, then tap 이미지 저장 (Save image). The PNG title and filename say tomorrow while the rows are for the current selected_date.
  - 수정안: Use dateInput.defaultValue (the server-rendered value attribute = selected_date) instead of dateInput.value. On PC, the normal flow gives the same value, so the PC output is unchanged. Or read selected_date from a data-* attribute on the glance section.
  - 상태: 통합자 반영 보고 · CEO 재판정 ship · 총괄이 전체 pytest(10,803 통과)·pre_push_smoke 재실행 확인. PNG 실기기 비교는 스테이징 QA 대기
- [P2] `templates/measurement/partials/mobile_list.html:23` — The check button's enabled/disabled state uses can_edit_erp (erp_permissions.py:292, which checks only ADMIN or the CS/SALES team), but the server uses evaluate_policy(ERP_EDIT), and the two disagree. (a) A VIEWER with team=SALES gets an enabled button from can_edit_erp; the server returns 403, so every tap reverts optimistically and shows the failure message. (b) A MANAGER role outside CS/SALES (or with no team) is allowed by the server but sees a disabled button.
  - 수정안: In dashboard.py, pass a separate flag such as can_mark_measurement_visit=evaluate_policy(POLICY_REGISTRY['ERP_EDIT'], current_user).allowed and use it in the template, so the UI uses the same rule as the server.
  - 상태: 통합자 반영 보고 · CEO 재판정 ship · 총괄이 전체 pytest(10,803 통과)·pre_push_smoke 재실행 확인. PNG 실기기 비교는 스테이징 QA 대기
- [P2] `foms/services/measurement/visit_check.py:132` — A group's manager_phone uses only the first row's value. resolve_manager_phone_for_queue falls back to each order's parties.manager.phone when the name is not in the settings list. If the first order in a group has no phone but later orders do, that group gets no tel: link on the manager name or the call button, which falls short of the user's request (tap the manager name or call button to call).
  - 수정안: When building a group, use the first non-empty manager_phone in the group (fill it in if the current value is empty while adding rows). Add a unit test.
  - 상태: 통합자 반영 보고 · CEO 재판정 ship · 총괄이 전체 pytest(10,803 통과)·pre_push_smoke 재실행 확인. PNG 실기기 비교는 스테이징 QA 대기
- [P2] `foms/services/erp_mobile_order_display.py:675` — (Existing bug, outside the diff, but the manager tel feature depends on this value.) resolve_manager_phone_for_queue does manager = parties.get('manager') or {} and then manager.get('name'). When parties.manager is a scalar (a user id or a name string), it raises AttributeError. Reproduced directly: r({'manager': 12}) and r({'manager': '최진호'}) both raise AttributeError. The builder comment says the 2026-09-09 fix made manager_name safe for scalars, but the manager_phone line right below it still assumes a dict. A single such order returns 500 for the whole mobile measurement queue.
  - 수정안: In resolve_manager_phone_for_queue, treat a non-dict manager as {'name': str(manager)} (or {} if it is a number), and use manager_display_name for the name. Add a scalar-shape test to test_manager_display_name_shapes.py (the file owner needs to be assigned separately).
  - 상태: 통합자 반영 보고 · CEO 재판정 ship · 총괄이 전체 pytest(10,803 통과)·pre_push_smoke 재실행 확인. PNG 실기기 비교는 스테이징 QA 대기
## CEO 판정: fix
- [P1] foms/api/orders/measurement_visit.py:140-167: lost update. The order read at line 140 happens before the lock. execute_order_mutation's with_for_update() (foms/services/orders/revision.py:285, no populate_existing) hands back that same identity-map object, so _mutate deep-copies stale structured_data and writes all of it back. Fix: after the no-op pre-check (line 155) and before execute_order_mutation, call db.expire(order). Also, at the top of _mutate, reload o.structured_data (use sess.refresh(o) if needed) so the deepcopy works on the locked, fresh value. Root fix, outside file ownership: add .populate_existing() to the FOR UPDATE query at revision.py:281-287, and report it to the lead because call_log.py:138 has the same pattern. Regression test in tests/domains/test_measurement_visit_api.py: pre-read, external commit changes another structured_data key, call the API, assert the other key survives.
- [P1] static/js/measurement/image-export.js:428: in the offscreen (mobile) branch, the clone has no grip-resizable class and no td{overflow:hidden} rule, so long text in narrow cells (orderer/customer 72px, phone 124px) spills into the next cell and the PNG differs from the PC one. Fix: add `.foms-meas-export-host [data-meas-export-target] > tbody > tr > td { overflow: hidden; }` to static/css/contexts/measurement/measurement-mobile-glance.css. It applies only inside the host, so the PC path stays byte-identical. Also check the other grip-* classes and head rules that column-resizer.js createStyle injects for any further PC/mobile difference. If possible, compare PC and mobile-emulation PNGs from the same data by hand.
- [P2] foms/api/orders/measurement_visit.py:155/_mutate: the no-op check runs outside the lock. Under a race, changed=False still triggers a JSONB write, flag_modified, a mutation_version bump and a receipt, with no event, which breaks spec §3. Fix: skip the assignment and flag_modified in _mutate when changed=False, and raise a dedicated sentinel exception so the caller rolls back and returns a changed:false success. Also decide whether If-Match is checked before the no-op check, and record that in the spec.
- [P2] foms/api/orders/measurement_visit.py:210-214: the replayed branch hardcodes done to the request value and changed to True. Fix: compute done as is_visit_marked(sd, date_iso) from the freshly read sd.
- [P2] foms/services/measurement/visit_check.py:132: a group's manager_phone uses only the first row's value, so if the first order has no phone the whole group loses its tel: link. This directly misses the user's request (tap the manager name or call button to call). Fix: while adding rows, fill grp['manager_phone'] with the first non-empty manager_phone if it is still empty. Add a unit test.
- [P2] foms/services/erp_mobile_order_display.py:675: in resolve_manager_phone_for_queue, a scalar parties.manager (an int or a name string) raises AttributeError and the whole mobile queue returns 500. Fix: when manager is not a dict, use {'name': str(manager)} for a string or {} for anything else. Add a scalar-shape test to test_manager_display_name_shapes.py.
- [P2] templates/measurement/partials/mobile_list.html:23 + foms/web/measurement/dashboard.py: the button's enabled state (can_edit_erp) and the API check (ERP_EDIT) use different rules, so a VIEWER sees an enabled button and gets 403, and a MANAGER outside CS/SALES sees a disabled one. Fix: in dashboard.py, pass can_mark_measurement_visit=evaluate_policy(POLICY_REGISTRY['ERP_EDIT'], current_user).allowed and use it in _gv_can.
- [P2] static/js/measurement/image-export.js:443: on mobile, captureScale is fixed at 2 with no height cap, so beyond roughly 35-55 rows it exceeds iOS's 16,777,216px canvas area and the save fails. Fix: in the offscreen branch only, use scale=min(2, sqrt(16777216/(w*h))), measuring w and h on a separate throwaway clone, never the live document. Record the scale difference from PC in the brief/spec as an intended exception.
- [P2] static/js/measurement/image-export.js:387: resolveExportDate reads .value of the filter drawer's unapplied date input, so the title and filename can disagree with the rows. Fix: use dateInput.defaultValue, or read selected_date from a data-* attribute on the glance section.
- [P2] static/css/contexts/measurement/measurement-mobile-glance.css:233: the daypart color dots break brief §2 ('time only as small gray text'). Fix: remove the color dots and keep only the gray text; the data-daypart attribute can stay.

근거: Every gate is green: APP_OK, node --check, 236, 467 and 11 tests passing, no inline styles, no leftovers. Two P1s are still open, so the result is fix. First, a lost update: I confirmed in the code that the order is read at measurement_visit.py:140 before the lock and deep-copied inside _mutate, and revision.py:285 uses with_for_update() without populate_existing. This can overwrite and lose another writer's structured_data change, which is a data-integrity problem, but a narrow fix (expire or refresh) closes it. Second, the mobile PNG differs from the PC PNG because the clone lacks overflow:hidden, and one CSS rule scoped to the host fixes it. Neither needs a spec review, so this is not block. The P2s go in the list without holding up ship. The grouped manager_phone taking only the first row, and the scalar manager causing AttributeError that returns 500 for the whole queue (confirmed in the code), both directly weaken the user's actual request (tap the manager name or call button to call), so it is best to fix them in this round too.
